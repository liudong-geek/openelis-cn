# OpenELIS 测试执行记录 — 2026-08-21

## 0. P1 整改复验增补（当前结论）

**当前结论：截至 2026-08-24，中国单语交付及本轮 P1/P2 收口已完成最新前后端门禁、最终前端镜像部署、HTTP 健康检查和真实 Chrome 复验。替换容器后页面 HTTP 200、backend healthy，当前运行环境判定为“业务试用候选”；正式生产仍受本文 P2 边界约束。**

本次使用新批次 `P1-20260821` 完成以下复验：

- 本地 HTTP 登录保持在 `18080`，不再跳转到 HTTPS；
- 中文患者“验收 / 患者二”成功建档，相同身份重复提交后数据库仍只有 1 条；
- 订单 `DEV01260000000000003` 从录入推进至采样、贴标/跳过存储和 4 项质控完成；
- 采样日期按后台地区设置保存为 `21/08/2026`，未来日期在页面不可选，后台保留二次校验；
- 样本请求自动变为 `COLLECTED` 并关联样本项目 ID `3`；
- 质控清单不再为空，4 项全部核对后 `all_required_verified=true`，订单页面显示 `Completed` 和 `4/4`；
- 全量前端回归共 185 个测试文件：2102 项通过、13 项跳过、0 项失败，Duration `340.48s`；Java 21 后端门禁 192/192 通过；
- 中文资源门禁覆盖 1790 个代码实际引用文案键；TypeScript 当前 1168 条错误，不高于 1209 条历史基线，没有新增类型回归；Vite 生产构建共转换 2590 个模块；
- 最新前端镜像 manifest list 为 `sha256:02f23cfc5bd7ca9640a90c0c74ef3c03e9377273e63b8bc91602f110c8500784`；中国版只向用户显示中文，内部兼容资源不作为用户语言选项。

详细证据与剩余 P2 风险见 [OpenELIS-P1整改记录-2026-08-21.md](./OpenELIS-P1整改记录-2026-08-21.md)。当前最终证据见 0.9；0.1～0.8 及后文明确标注为整改前或历史阶段的旧数字仅作追溯，不能再作为当前发布状态解读。

### 0.1 结果录入 P1 与运行环境复验增补（2026-08-23）

本轮针对“首页待录入结果显示 1 条，点击后旧结果页错位且列表为空”完成专项整改：

- 首页入口改为 `/Results?scope=pending`，不再进入旧 `/result` 页面；
- 新增待录入工作清单接口，使用与首页相同的 `NotStarted` 状态并叠加用户实验室权限过滤；
- 统一结果页支持待录入自动加载、URL 筛选条件、加载/错误/空状态和中文业务布局；
- 路由门禁保留查询参数，避免登录或权限跳转后丢失 `scope=pending`；
- 相关前端增量测试 16/16 通过；最终全量前端回归 Test Files 153/153 通过，Tests 962 项通过、13 项跳过、0 项失败，Duration `223.61s`；
- 上一批后端 P1 门禁 94/94 通过（0 失败、0 错误、0 跳过），其中包含 `ControllerSetupLoggingTest` 3 项、ReportController 参数绑定 400、报告打印鉴权、外送角色/实验室单元过滤、分析仪 CSV、标签打印计数聚合及患者历史兼容用例；当批 Java 21 编译和 WAR 构建成功；
- 中文资源门禁覆盖 1269 个引用键；TypeScript 当前 1195 条错误，低于 1209 条历史基线；
- Docker 本地数据重置后重新构建并启动核心环境，四个常驻服务均健康；会话接口 HTTP 200，统一结果路由数据库开关为 `true`；
- 同时关闭 Tomcat 对同一 WAR 的自动二次部署，并将本地可选 FHIR URI 置空；最终运行容器 healthy，启动实测 `53306 ms`。

随后已使用真实 Chrome 在本地纯 HTTP 地址完成管理员登录和有数据页面复验：左侧菜单正常显示；首页“待录入结果”进入 `/Results?scope=pending` 后显示 18 条待处理分析，不再进入旧 `/result` 空表页，也未再出现此前的布局错位。

### 0.2 结果保存与标签打印专项复验（2026-08-23）

- 数据库重置后使用干净验收订单 `DEV01260000000000002` 完成 WBC 结果录入，结果值 `7.2` 保存成功并在刷新后保持一致；该订单与第 2～4 节保留的整改前同编号基线数据不是同一批数据库记录；
- 保存接口返回提交后的分析状态 ID 和更新时间，结果工作台直接回写当前行，状态按中文展示，避免“后台已保存、页面仍显示旧状态”造成重复操作；
- 标签打印计数改为先按订单、样本和标签类型聚合，再对每条计数记录更新一次；
- 对订单 `DEV01260000000000001` 执行真实标签打印后，数据库 `printed_order_count` 从 `0` 增至 `1`；同期后端日志没有 `StaleStateException`，也没有“failed to record printed counts”；
- 以上证据只覆盖本轮指定订单、结果录入和标签打印路径，不代表所有角色、旧式页面或外部接口已经通过交付验收。

### 0.3 前端交付面增补（2026-08-23）

- Service Worker 注册与资源路径已改为跟随应用实际部署基址，本地 HTTP 与非根路径部署不再使用错误地址；
- 旧结果页、电子签名、条码打印确认及患者结果查看器完成本轮中文化，补齐关键操作、状态、空态、加载和错误提示；
- 标签打印确认页明确提示核对打印数量，打印文件不可用或弹出窗口被拦截时在页面内显示可执行错误信息；
- 应用内导航调用已统一，本轮复现的 single-spa 导航告警已消除；
- 通知订阅配置作为可选能力只在布局加载时检查一次；本地未配置通知服务时静默降级为未订阅，并在组件卸载后停止状态回写，不再制造重复控制台错误；
- 当前门禁结果为：前端 Test Files 153/153 通过，Tests 962 项通过、13 项跳过、0 项失败，Duration `223.61s`；中文资源 1269 个引用键完整；TypeScript 1195 条不高于 1209 基线；Vite 转换 2586 个模块并成功生成生产资源。

### 0.4 患者历史与可选外部服务隔离（2026-08-23）

- 真实订单结果 `7.2` 已存在数据库，但患者历史接口因该旧数据缺少 `testResult` 关联而返回空值，同时把未配置范围误报为 `0–0`；
- 两个患者历史接口现统一以持久化结果类型和数据库原值兜底，字典缺失不再返回空白；趋势图仍保留 `<5 → 5` 的数值归一化语义；
- 无 `TestResult` 但 `resultType=N` 的合法 `0–0` 仍作为真实数值范围保留，不与遗留虚假哨兵混淆；多条历史有效范围按确定性规则选择最新记录；没有真实数据来源的危急上下限和绝对下限不再伪造；
- 新增 8 项专项回归，覆盖缺失关联、`resultType=N` 的合法 `0–0`、多历史有效范围确定性选择最新、数值限定符、取消结果、字典命中/缺失和空时间，8/8 通过；上一批合并后端 P1 门禁为 94/94（0 失败、0 错误、0 跳过）；
- 部署后已使用真实 Chrome 复验患者历史：真实结果 `7.2` 正常显示，未再出现虚假的 `0–0` 参考范围；
- 当前本地环境没有独立 FHIR 镜像，本地可选 FHIR URI 已留空，使不可达外部对接不再阻塞 LIS 主系统；该阶段运行容器启动实测 `53306 ms`，当前最终部署证据见 0.9，外部 FHIR 功能仍未通过交付验收。

### 0.5 订单步骤完成态复验（2026-08-23）

- 订单此前已经持久化完成某一步骤时，页面仍按当前表单校验结果显示“仍缺少必填信息”，造成完成状态与提示互相矛盾；
- 现改为优先依据后台持久化的 `stepProgress` 显示已完成步骤，仅对尚未完成的当前步骤显示必填项提示；
- 该修复已纳入最终前端全量回归，避免操作员重复填写或误判流程无法继续。

### 0.6 外送查询、分析仪 CSV 与中文主数据复验增补（2026-08-23）

- 外送检验查询页已按业务入口明确三类查询方式，并补齐加载、失败、未查询和无结果状态；无结果时不再渲染空表与分页；Carbon 表格、筛选和分页辅助文案使用中文；行选择改为稳定记录 ID，避免跨页后选中错误记录；聚焦组件回归 4/4 通过；
- 前端当前门禁为 Test Files 153/153 通过，Tests 962 项通过、13 项跳过、0 项失败，Duration `223.61s`；中文资源门禁 1269；TypeScript 为 1195 条，不高于 1209 条历史基线；Vite 生产构建转换 2586 个模块；
- 分析仪 CSV 导入现优先按精确描述识别检测，保留 `%` 与 `#` 的不同语义身份；兼容模糊升级受到约束；任一行或桥接写入失败即停止并回滚整批；内置 CSV checksum revision 已修订；该专项 6/6 通过，上一批后端 P1 门禁合计 94/94；
- 中文主数据迁移覆盖血液学 1 项、全血 1 项和 CBC 检测 36 项；在真实数据库事务中连续执行两遍均为 0 重复，英文原值保持不变，并验证事务回滚后不残留迁移写入；
- Docker 构建内的迁移集成测试需要嵌套 Testcontainers，但构建容器未挂载 Docker socket，因此该项未执行完成。此为测试基础设施限制，不计作业务失败；迁移幂等、英文不变和回滚已由真实数据库事务验证；
- 实际启动时 Liquibase 071 成功执行；Micros 导入 16 项、Pentra 导入 20 项，日志未出现 `duplicate key`、`test_desc_uk`、`25P02` 或 `transaction aborted`；数据库最终存在 22 个唯一分析仪 CBC 项目，12 组 `%/#` 项均使用独立 ID；最终运行容器 healthy，启动耗时 `53306 ms`；
- 真实 Chrome 中菜单和面包屑均显示“外送检验”，页面标题为“外送检验查询”，日期类型为“发送日期”且日期占位为“日/月/年”；未查询时只显示业务空态、不显示空表；无匹配查询显示“未找到匹配的外送记录”，不显示表格和分页；
- 真实 Chrome `/Results` 显示 18 行中文 CBC 检测及“全血”样本：WBC、RBC、HGB、HCT、MCV、MCH、MCHC、PLT，以及 NEU、EOS、BAS、LYM、MON 的 `%/#` 项。

### 0.7 管理界面、权限与日志复验增补（2026-08-23）

- 真实 Chrome 已验收管理导航、检测目录列表、QC 空态与警报空态、外送检验无匹配空态及报告目录；
- 检测目录编辑器“基本信息”的面包屑、帮助、样本类型、业务域、AMR，以及实验室单元/样本类型组合框的清除和展开辅助文案均为中文；页面中未发现 `Clear selected item`、`Open` 等英文 UI；
- 外送检验 REST 接口现要求 `ROLE_RESULTS`，并按当前用户 Results 实验室单元过滤；跨页选择以稳定 ID 管理，报告参数去重并编码；
- GET/POST `ReportPrint` 对所有非空 `analysisIds` 统一执行登录、`ROLE_REPORTS`、分析存在性及用户 Reports 实验室单元全量鉴权，任一分析不满足条件即整体拒绝；该早期切片现已由 0.8 的统一报告入口策略覆盖；
- 参数缺失、JSON 不可读、媒体类型不支持及访问拒绝等可预期客户端异常由 ERROR 降为 WARN；`AccessDeniedException` 稳定返回 403；`ControllerSetupLoggingTest` 3/3；
- 真实 curl 登录返回 200；`analysisIds=not-an-id` 返回 400；数字型不存在/无权 `analysisIds=999999999` 返回 403；最近日志只有 `ReportAnalysisAuthorizationService` WARN，无 ERROR；
- 上一批后端 P1 门禁为 94/94（0 失败、0 错误、0 跳过）；当时运行容器 healthy，启动 `53306 ms`；启动日志没有 `duplicate key`、`test_desc_uk`、`25P02` 或 `transaction aborted`。该数据是本轮报告权限扩展之前的历史证据。

### 0.8 报告权限、072 中文主数据与查询交互执行增补（历史阶段，2026-08-23）

本节为上一版镜像的阶段性自动化、HTTP 与真实 Chrome 证据；当前最终结果以 0.9 为准。

| 执行项 | 状态 | 阶段结果 |
| --- | --- | --- |
| 前端全量/专项 | 通过 | Test Files 153/153；Tests 962 通过、13 跳过、0 失败；Duration `223.61s`；统计 5/5、样本 6/6、报告相关 22/22 |
| 前端其他门禁/镜像 | 通过 | i18n 1269；TypeScript 1195 不高于 1209；Vite 2586；镜像 `sha256:214413f8...` |
| Java 21 后端门禁/镜像 | 通过 | P1 167/167，统计 5/5；Java 21 编译 2832 源文件、Spotless/WAR `BUILD SUCCESS`；镜像 `sha256:8051d6f5...` |
| 部署与日志 | 通过 | 四容器正常，webapp healthy，启动 `236680 ms`；无 `ERROR`、`SEVERE`、`duplicate key`、`test_desc_uk`、`25P02`、`transaction aborted`；`web.xml` 的 `error-page` 字面量不计错误 |
| 统计报告业务规则 | 通过 | 原空条件因 `priority=null` NPE/`upperYear` 返回 500；现默认全选，主动清空为用户可用全部，空授权/非法优先级 fail-closed，年份 2009～当前年，弹窗阻断可见 |
| 统计报告 Chrome/PDF | 通过 | 10 科室、5 优先级（常规/尽快处理/急诊/指定时间/预定急诊）、2 时间段均中文全选，UI 打开 `ReportPrint`；curl 200 `application/pdf`，2839 字节，PDF 1.5、1 页 |
| 报告安全负向 | 通过 | `CISampleRoutineExport` 403；`patientCollection` 403；受限页显示中文“安全适配中” |
| 072 双语 live API | 通过 | `zh_CN`/`en` 均 200；中文科室为生化/血液学/血清免疫学/免疫学，样本为痰/体液/呼吸道拭子/血清/血浆/尿液/全血；`en` 仍为英文 |
| Chrome Dashboard/结果 | 通过 | 中文左栏、18 条待办；待录入进入 `/Results?scope=pending`，18 条 CBC 中文，显示全血和 10 个中文科室 |
| Chrome SampleType/控制台 | 通过 | 15 项中文主名，分页 aria 为“页码，共1页”；当前页 console error/warn 为 0；英文 aria 和统计优先级英文残留均是真实部署发现后修复 |

下表为当时部署前的阶段性记录，其“待补证”状态已由 0.9 的最终证据取代，仅作过程追溯。

| 执行项 | 当前状态 | 已确认结果 | 待主流程补证 |
| --- | --- | --- | --- |
| 全量前端组件测试 | 通过 | 150 个测试文件，944 项通过、13 项跳过、0 项失败 | 无 |
| 中文资源门禁 | 通过 | 1261 个实际引用键在 `zh`、`zh-CN` 中完整 | 无；资源完整不等于所有业务主数据已经人工审校 |
| TypeScript 基线门禁 | 通过（无新增） | 1195 条诊断，不高于 1209 条历史基线 | 存量诊断仍需后续消减 |
| Vite 生产构建 | 通过 | 转换 2585 个模块并生成生产资源；最终前端镜像已构建 | 新镜像部署后的页面证据 |
| 样本类型管理 | 代码与全量回归通过 | 列表/详情中文化；加载失败可重试；筛选无结果、后端异常、错误详情、表单校验和响应式布局均有明确反馈，不再无限转圈 | 部署后真实 Chrome 列表、无结果和异常分支 |
| 报告查询页面 | 代码与全量回归通过 | 按 ID、实验室编号、患者状态和日期查询统一安全参数构造、互斥模式、中文日期比较、加载/错误/空态及弹窗拦截反馈；不再双重编码参数或显示假成功 | 部署后真实 Chrome 查询和弹窗分支 |
| 报告目录安全交互 | 代码与聚焦回归通过 | 尚未完成安全适配的报告显示“安全适配中”且不可点击；直接 React 深链接显示中文阻断提示；已证明安全的统计/聚合入口保持可用 | 部署后菜单、直达深链及安全报告入口 |
| 报告后端统一鉴权 | 代码与独立安全复核通过 | 所有报告创建入口要求会话用户和 `ROLE_REPORTS`；批量患者导出额外要求 Global Admin；未分类创建器默认拒绝；按实际 `analysis.testSection.id` 过滤；登录科室缺失、为 0、删除或角色不相交时 fail-closed | 本轮 Java 21 最终精确总数、部署后 HTTP 正负请求和多角色账号 |
| 患者选择型报告 | 代码与独立安全复核通过 | `patientCollection`、`patientAssociated` 在不能建立完整授权快照时 fail-closed；患者编号、注入 `analysisIds`、注入高实验室编号均在读取业务数据前拒绝 | 部署后 403/页面阻断证据 |
| COVID 日期范围 | 代码与专项测试通过 | 使用 `[startInclusive, endExclusive)` 半开范围；结束日结果纳入，下一日零点排除；按业务时区计算边界并复用授权候选快照 | Java 21 最终精确总数和部署后边界日期数据 |
| Liquibase 072 真实数据库事务 | 通过（未正式部署） | 临床科室 13/13、样本类型 13/13、面板 4/4、生化 16/16；包含 071 后血液学 53/53；活动临床科室缺失中文由 9 降为 0；同一事务连续执行两遍均无重复 locale，英文和核心值不变，回滚后恢复基线、无存量污染 | 正式数据库执行 072、启动日志和真实浏览器中文展示 |
| 后端 Java 21 发布门禁 | 等待主流程补证 | 独立安全复核未发现新的可复现 P0/P1；本节不得继续引用上一批 94/94 作为本轮结果 | 精确测试总数、失败/错误/跳过数、编译/格式/WAR 或运行镜像结果 |
| 部署与真实 Chrome | 等待主流程补证 | 未把上一批 `53306 ms` 容器或旧 Chrome 页面当成本轮证据 | 新镜像部署健康、072 实际执行、HTTP 登录、菜单状态、深链阻断及安全报告可进入性 |

### 0.9 中国单语交付最终执行证据（2026-08-24）

#### 自动化、构建与部署

| 执行项 | 最终结果 | 判定 |
| --- | --- | --- |
| 前端完整回归 | 185 个测试文件；2102 项通过、13 项跳过、0 项失败；Duration `340.48s` | 通过；明确跳过的 13 项不计失败，其覆盖场景也不据此计为通过 |
| 中文资源完整性 | 1790 个代码实际引用文案键通过检查 | 通过；资源完整不代表机构自定义主数据已全部审校 |
| TypeScript 历史基线 | 1168 条，不高于 1209 条基线 | 通过“禁止新增”门禁；存量类型债务仍存在 |
| Vite 生产构建 | 成功转换 2590 个模块 | 通过；既有构建警告仍在遗留范围内 |
| 后端 Java 21 | 192/192 通过 | 通过；仅代表本轮纳入 Java 21 门禁的测试 |
| 前端交付镜像 | manifest list `sha256:02f23cfc5bd7ca9640a90c0c74ef3c03e9377273e63b8bc91602f110c8500784` | 当前最终镜像；摘要为 manifest list，不是旧版本地镜像 ID |
| 容器替换后检查 | 页面 HTTP 200，backend healthy | 通过；只证明当前本地 HTTP 交付环境可访问且后端健康 |

#### 真实 Chrome 中国版验收

| 场景 | 最终结果 | 判定与边界 |
| --- | --- | --- |
| 工作台术语 | 工作台可见区及当前菜单未出现“订单”“样本”“患者历史”“研究”“更多功能”“安全适配中”或“WHONET” | 通过；检查对象为当前部署和本轮可见区域 |
| 岗位菜单组 | 当前会话配置的八个岗位菜单组期望项均齐全 | 通过；不等同于八类独立账号权限矩阵已经全部验收 |
| 患者页面 | 入口为“历史结果查询”，页面标题为“患者历次检验结果” | 通过；仅覆盖本轮可达历史查询入口及页面 |
| 报告查询 | 仅显示“常规检验报告” | 通过；未显示的旧报告或未适配报告不计为已经交付 |
| 中国单语呈现 | 用户界面只显示中文；内部兼容资源不作为用户语言选项 | 通过；标准缩写、后台自定义数据和兼容格式资源按业务边界保留 |
| 控制台 | 上述验收期间 console error 为 0 | 通过；不扩大为所有角色、所有页面和外设场景均无错误 |

最终剩余边界：本轮浏览器证据基于当前最终前端镜像、当前管理员会话和现有测试数据；尚未完成八类独立账号的逐一权限矩阵、QC/外送/审核/报告全部有数据分支、真实 FHIR 双向 TLS、仪器外设、机构全量主数据、备份恢复及并发容量验收。TypeScript 仍有 1168 条存量诊断，Vite 既有警告仍需后续治理。因此，本节支持“业务试用候选”判定，不支持直接判定为正式生产交付。

## 1. 首轮基线结论（整改前）

**发布结论：不通过，当前为红色，不建议接入真实患者数据或作为正式 LIS 交付。**

系统已经能够完成“患者 → 订单 → 采样 → 标签 → 结果录入 → 结果审核 → 报告 PDF”的技术链路，核心接口在本机的查询性能也较好。但本轮发现多项会污染主数据、造成前后台状态不一致或误导操作人员的 P1 缺陷，必须修复后重新执行核心回归。

当前更适合继续作为本地开发/演示环境，不适合生产试运行。

## 2. 环境与测试数据

- 地址：`http://127.0.0.1:18080`
- 应用版本：`3.2.1.11`
- 浏览器：Google Chrome 桌面版
- 语言：简体中文
- 数据批次：`QA-20260821-CORE-01`
- 测试患者编号：`QA-20260821-SUBJ-0001`
- 测试身份编号：`QA-20260821-NID-0001`
- 测试订单：`DEV01260000000000002`
- 测试样本：全血，样本项目 ID `2`
- 测试项目：`WBC(Whole Blood)`，检测 ID `379`
- 模拟结果：`5.5`
- 说明：由于重复患者缺陷，本批次意外产生患者 ID `2`、`3` 两条重复记录；均为可识别的 QA 数据。

## 3. 执行概况

| 范围 | 结果 | 说明 |
| --- | --- | --- |
| 页面导航与菜单 | 基本通过 | 左侧菜单已显示；顶级菜单可展开；核心 URL、标题、面包屑和高亮基本一致 |
| 核心业务闭环 | 部分通过 | 数据可推进至已审核结果和报告 PDF，但采样请求、QA 清单和前后台状态存在 P1 问题 |
| 前端组件测试 | 通过但有警告 | 两批共 95 项最终通过；首次有 1 项 5 秒超时，放宽至 15 秒后通过 |
| 前端生产构建 | 通过但有风险 | Vite 构建成功，用时 44.90 秒；存在非法 CSS 选择器、直接 `eval`、超大分包警告 |
| TypeScript 门禁 | 失败 | `tsc --noEmit` 共 1209 个错误，当前不能作为发布门禁 |
| 接口安全负向测试 | 大部分通过 | 错误密码、CSRF、SQL 注入、XSS 均被阻止；未登录跳转地址错误 |
| 性能冒烟 | 通过 | 核心查询接口 5 次采样均低于 0.30 秒 |
| 审计与 FHIR 落库 | 通过 | 患者、订单、样本、分析、结果有历史记录；FHIR 资源已生成 |
| Chrome 自动化 | 部分阻塞 | Chrome 连接插件多次超时；项目 Playwright 本地运行镜像因 Docker Chromium 解压 I/O 错误未建成 |

## 4. 核心业务流程实测

| 记录 ID | 对应用例 | 状态 | 实际结果 / 证据 |
| --- | --- | --- | --- |
| RUN-001 | B-01 | 通过 | 顶栏、左侧菜单和工作区均显示；用户已在 Chrome 确认侧栏恢复 |
| RUN-002 | B-02 | 通过 | 订单、患者、结果、验证、报告、更多功能均可展开并显示子菜单 |
| RUN-003 | B-03 | 通过 | “采样或接收”URL、面包屑、页面标题和菜单高亮一致 |
| RUN-004 | B-08 / E-02 | 通过 | 无检测项目时显示明确空状态，贴标和质控步骤禁用，并可返回订单录入 |
| RUN-005 | B-10 | 失败（P2） | 报告菜单存在“审计 Trail”“Turn Around Time”等半中半英文案 |
| RUN-006 | C-01 | 失败（P1） | 中文姓名“验收 / 患者一”被后台 `ValidName` 拒绝，无法按国内真实姓名建档 |
| RUN-007 | C-01 | 通过（替代数据） | 改用英文测试名后创建成功，患者 ID 为 `2` |
| RUN-008 | C-05 / D-10 / I-07 | 失败（P1） | 完全相同的身份编号和患者编号再次提交仍返回 200，并创建患者 ID `3`；搜索返回 2 条重复记录 |
| RUN-009 | D-03 / D-09 | 通过 | 创建订单 `DEV01260000000000002`，患者、全血样本类型和 WBC 请求关联成功 |
| RUN-010 | E-03 | 通过 | 采样数据保存成功，生成样本项目 ID `2`，订单 `collect=true` |
| RUN-011 | E-03 / I-07 | 失败（P1） | 样本已创建且订单采样完成，但 `sample_type_request` 仍为 `REQUESTED`，未关联样本项目 |
| RUN-012 | E-03 / I-01 | 失败（P1） | 调用完成请求接口返回 HTTP 500 / lazy proxy no Session，但数据库实际已改为 `COLLECTED`；客户端失败、后台成功 |
| RUN-013 | E-05 / I-07 | 失败（P1） | 后台接受略晚于当前时间的采样时间；数据库中 `started_date=00:55` 晚于 `completed_date=00:54` |
| RUN-014 | E-06 / E-07 | 通过 | 订单标签和样本标签均返回 200 `application/pdf`，大小分别为 1741、1827 字节 |
| RUN-015 | E-10 / E-12 | 通过（跳过存储分支） | 设置 `storageSkipped=true` 后刷新仍保留，订单 `label=true` |
| RUN-016 | F-01 / F-02 / F-10 | 通过 | WBC 结果 `5.5` 保存成功，重新查询仍为 `5.5` |
| RUN-017 | G-01 / G-02 | 通过 | 待审核队列出现 1 条结果；接受后重新查询队列为 0 条，数据库 `released_date` 已写入 |
| RUN-018 | G-08 | 通过（文件级） | 患者结果报告返回 200 `application/pdf`，1 页，3269 字节；尚未完成人工逐字排版核对 |
| RUN-019 | G-12 / I-07 | 通过 | `history` 中存在 PATIENT、SAMPLE、SAMPLE_ITEM、ANALYSIS、RESULT 的插入/更新记录 |
| RUN-020 | 订单 QA | 失败（P1） | QA 清单配置为空；前端 `[].every(...)` 判定为完成，提交后显示成功，但后台 `allRequiredVerified=false`、`qa=false` |

## 5. 安全、会话与异常测试

| 记录 ID | 对应用例 | 状态 | 实际结果 / 证据 |
| --- | --- | --- | --- |
| RUN-021 | A-01 | 通过 | 默认管理员登录成功，会话包含 10 个角色 |
| RUN-022 | A-02 | 通过 | 错误密码返回 401，随后会话 `authenticated=false` |
| RUN-023 | A-07 | 部分失败（P1） | 未登录访问受保护接口返回 302，但重定向到 `https://127.0.0.1/...`，丢失 HTTP 端口 `18080` |
| RUN-024 | I-02 | 通过 | 已登录但缺少 CSRF 的写请求返回 403，提示 token 缺失或无效 |
| RUN-025 | I-03（SQL） | 通过 | 姓名搜索输入 `' OR 1=1 --` 返回 0 条，无数据泄露和 500 |
| RUN-026 | I-03（XSS） | 通过 | `customNotes=<script>...` 返回 400，提示 Unsafe HTML tags included |
| RUN-027 | 安全响应头 | 失败（P2） | HTTP 首页未返回 CSP、X-Frame-Options、X-Content-Type-Options、Referrer-Policy 等常用安全头 |
| RUN-028 | CORS | 通过 | 恶意 Origin 的预检请求返回 403，无宽松跨域响应头 |

## 6. 中文化与国内适配

| 项目 | 结果 |
| --- | --- |
| 中文资源键数量 | 7408，与英文键数量一致 |
| 缺失中文键 | 0 |
| 中文文件中仍与英文原文完全相同的条目 | 2344，约占 31.6% |
| 已观察到的混合文案 | “审计 Trail”“Turn Around Time”“Show on patient report”等 |
| 中文姓名 | 后台默认姓名字符集不接受汉字 |
| 基础字典 | 地址仍为海地省份，国籍选项为 Ivoirian / West African 等，尚未完成中国化 |

结论：当前属于“中文框架 + 局部翻译”，不是完整的中国 LIS 本地化版本。

## 7. 性能采样

以下为同一已登录会话、本机环境各 5 次 GET 采样：

| 接口 | 最小值 | 最大值 | 结论 |
| --- | ---: | ---: | --- |
| 首页指标 `/rest/home-dashboard/metrics` | 0.034 s | 0.049 s | 通过 |
| 菜单 `/rest/menu` | 0.011 s | 0.012 s | 通过 |
| 订单搜索 `/rest/order/search` | 0.216 s | 0.294 s | 通过 |
| 结果录入 `/rest/LogbookResults` | 0.188 s | 0.255 s | 通过 |

这只是单用户、小数据量的接口基线，不代表 1000 条列表、并发或长期运行性能。

## 8. FHIR 与外部对接

- 首轮历史批次曾确认核心流程可生成 Patient、ServiceRequest、Specimen、Observation、DiagnosticReport、Task 等 FHIR 资源；该证据不代表当前外部服务可连接。
- FHIR 启用时的 compose 端口已从错误的容器 `8080` 修正为 TLS `8443`。
- 当前本地环境没有 `openelis-cn-fhir` 镜像。继续保留 `fhirstore.uri` 会使主系统在启动监听前等待不存在的外部服务，因此本地交付已把该地址留空，确保 LIS 主流程独立可用；本段记录的阶段启动实测为 `53306 ms`，当前最终部署证据见 0.9。
- 后续启用 FHIR 时必须提供可达镜像或服务、客户端证书、信任链和真实外部系统联调证据。

结论：当前仅交付“可配置的外部对接边界”，未交付可用 FHIR 接口，不得按“外部系统可连接”验收。

## 9. 自动化与代码质量

### 9.1 组件测试

- 整改前第一批：65 项，首次 64 通过、1 项首页测试 5 秒超时；单独复跑后 3/3 通过。
- 整改前第二批：患者、订单校验、标签、结果防误操作共 30/30 通过。
- 当前结果：共执行 185 个测试文件，2102 项通过、13 项跳过、0 项失败，Duration `340.48s`；其中外送检验、检测目录、样本类型、报告查询和报告安全导航等聚焦回归已纳入完整回归门禁。
- 此前测试中出现的 Service Worker 路径/就绪对象错误已完成路径修复并纳入全量回归；其余历史 React 异步卸载告警仍需按实际复现单独跟踪。

### 9.2 类型检查

- 整改前 `tsc --noEmit` 失败，共 1209 个类型错误，覆盖 Playwright 测试、管理模块、分析仪/串口服务和 React 组件类型等。
- 当前为 1168 条，不高于 1209 条历史基线；已恢复“禁止新增”门禁，但存量错误尚未清零，不能描述为类型检查全绿。

### 9.3 生产构建

- 最新 Vite 生产构建成功，共转换 2590 个模块并生成生产资源；整改前一次构建用时 44.90 秒，仅保留作历史基线。
- 主要警告：`:global(...)` 被 CSS 压缩器判为无效伪类；计算公式模块直接使用 `eval`；最大分包约 7.37 MB；多个动态导入失效。

### 9.4 浏览器自动化基础设施

- 真实 Chrome 最终验收已完成：工作台术语、八个岗位菜单组、患者历史查询命名、报告查询入口和中国单语呈现符合 0.9；本轮页面 console error 为 0。
- 现有 Cypress 镜像基于 Alpine，Cypress 二进制与 libc 不兼容。
- 新建本地 Playwright Chromium 镜像时，Docker 在解压 Chromium 层发生 I/O error；未执行全量 Playwright E2E。
- 因此，0.9 的真实 Chrome 人工验收可作为本轮已访问路径的页面证据，但不能替代八类独立账号、全部数据分支的全量 Playwright/Cypress E2E。

## 10. 缺陷清单

| 缺陷 ID | 严重度 | 状态 | 描述 |
| --- | --- | --- | --- |
| QA-DATA-001 | P1 | 新建 | 后台允许相同 nationalId + subjectNumber 重复创建患者，已生成 ID 2、3 |
| QA-I18N-001 | P1 | 新建 | 后台姓名校验不接受中文字符，中文患者无法建档 |
| QA-I18N-002 | P1 | 新建 | 2344 条中文资源仍为英文；基础字典仍是海地/西非数据 |
| QA-WF-001 | P1 | 新建 | 采样保存后样本类型请求不自动从 REQUESTED 变为 COLLECTED |
| QA-API-001 | P1 | 新建 | fulfill 接口事务已提交却返回 500，产生“后台成功、前端失败”状态错觉 |
| QA-QA-001 | P1 | 新建 | QA 清单未配置时前端可显示提交成功，但后台 qa=false |
| QA-DATA-002 | P1 | 新建 | 允许未来采样时间，导致 started_date 晚于 completed_date |
| QA-AUTH-001 | P1 | 新建 | HTTP 调试环境未登录跳转强制变为错误的 HTTPS 地址并丢失 18080 端口 |
| QA-FHIR-001 | P1 | 新建 | 中文 compose 把 FHIR 28081 映射到未监听的 8080，而服务实际监听 8443 |
| QA-ENG-001 | P1 | 新建 | 全量 TypeScript 检查有 1209 个错误，发布质量门禁失效 |
| QA-UI-001 | P2 | 本轮范围已完成 | 管理导航、检测目录列表与编辑器基本信息、QC 空态/警报空态、外送无匹配空态和报告目录已完成真实 Chrome 中文验收；其余业务主数据及有数据角色分支保留 P2 |
| QA-UX-001 | P2 | 已完成 | 已持久化完成的订单步骤仍提示“仍缺少必填信息”；现依据后台 `stepProgress` 显示完成状态 |
| QA-FE-001 | P2 | 已完成 | 通知组件仅在布局加载时检查一次；未配置推送服务时静默降级，卸载后停止状态回写；最终真实 Chrome 控制台复验无重复订阅错误 |
| QA-FE-002 | P1 | 已完成 | Service Worker 注册与资源路径在非根路径部署下不匹配；已按应用部署基址修正并通过全量回归 |
| QA-I18N-003 | P1 | 已完成 | 旧结果、电子签名、条码打印确认及患者结果查看器残留英文；本轮交付关键文案已完成中文化 |
| QA-NAV-002 | P1 | 已完成 | 应用内导航触发 single-spa 运行时告警；导航调用统一后告警已消除 |
| QA-HISTORY-001 | P1 | 已完成 | 缺少旧结果配置关联时患者历史静默隐藏真实值并显示虚假 `0–0` 范围；真实 Chrome 已确认结果 `7.2` 恢复，合法 `0–0` 与多历史范围边界纳入 8 项专项回归 |
| QA-DEPLOY-002 | P1 | 已完成 | 本地没有 FHIR 镜像仍配置 FHIR URI，导致可选外部服务阻塞 LIS 启动；URI 置空后本轮最终运行容器 healthy，启动实测 `236680 ms` |
| QA-REFERRAL-001 | P2 | 本轮范围已完成 | 外送检验查询已明确查询类型及加载/失败/空态，中文化 Carbon 辅助文案，稳定跨页选择，无结果不显示空表分页；4/4 及真实 Chrome 未查询/无匹配分支通过 |
| QA-IMPORT-001 | P2 | 本轮范围已完成 | 分析仪 CSV `%/#` 精确身份、失败整批回滚及内置 checksum revision 已完成；专项 6/6；真实启动无唯一键/事务中止错误，数据库身份结构正确 |
| QA-MASTERDATA-001 | P2 | 本轮范围已完成 | 071 的血液学/全血/CBC 已部署验收；072 再覆盖 13 个临床科室、13 个样本类型、4 个面板、16 个生化项目和 17 个补充 CBC 别名，真实数据库双执行、回滚、正式部署、双语 API 与浏览器复验通过；其他机构主数据保留 P2 |
| QA-REFERRAL-002 | P2 | 已完成（自动化） | 外送接口强制 `ROLE_RESULTS` 并按用户 Results 实验室单元过滤；真实有数据与不同角色浏览器分支待账号/数据后验收 |
| QA-REPORT-001 | P2 | 本轮安全收口已完成 | 所有旧式报告创建入口统一要求 `ROLE_REPORTS`，按实际分析科室判权；Java 21 P1 167/167；`CISampleRoutineExport` 与 `patientCollection` 部署后均为 403；`patientCollection`/`patientAssociated` 仍安全禁用，聚合科室范围、小计数政策及未适配批量报告保留 P2 |
| QA-REPORT-UX-001 | P2 | 本轮范围已完成 | 报告查询和样本类型页面补齐中文、互斥条件、安全参数、加载/错误/空态、弹窗拦截、重试、校验和响应式布局；最终镜像部署及关键页面 Chrome 复验通过，其他报告页逐页验收保留 P2 |
| QA-COVID-001 | P2 | 代码与门禁完成 | COVID 完成日期改为 `[startInclusive, endExclusive)` 半开范围并按业务时区转换；结束日纳入、次日零点排除；已纳入 Java 21 167/167 门禁，真实边界日业务数据复验保留 P2 |
| QA-LOG-001 | P2 | 已完成 | 正常客户端异常及访问拒绝降为 WARN，`AccessDeniedException` 返回 403；`ControllerSetupLoggingTest` 3/3 |
| QA-SEC-001 | P2 | 已完成（本轮范围） | 本地 HTTP 已加入 CSP、X-Frame-Options、X-Content-Type-Options、Referrer-Policy；CSP 因既有公式计算能力仍含 `unsafe-eval` |
| QA-BUILD-001 | P2 | 新建 | 生产构建存在无效 CSS、直接 eval、超大分包和动态导入失效警告 |

## 11. 下一轮发布门槛

1. 先修复全部 P1，尤其是患者唯一性、中文姓名、采样请求联动、QA 假成功和 fulfill 事务/响应问题。
2. 初始化适用于中国机构的患者、地区、科室、样本、检测、参考范围和 QA 清单字典。
3. 补齐核心页面中文文案；中文资源与英文完全相同的条目必须逐项分类处理。
4. 建立可运行的 Playwright 核心回归，并让 `typecheck`、生产构建、单元测试成为 CI 强制门禁。
5. 清理本批次 QA 数据后，用新批次重新执行：中文患者、重复提交、完整采样联动、QA、异常结果、临界值、报告内容和角色权限。
6. 修复前不得接入真实患者主索引，也不得开放外部 FHIR 端口给业务系统。

## 12. 同类页面与入口回归（历史阶段，2026-08-23）

本节保留统计报告和 072 部署之前的阶段性证据；其中旧门禁数字、镜像和启动时间已由 0.9 的最终结果取代。

| 执行项 | 结果 | 说明 |
| --- | --- | --- |
| 首页岗位权限入口 | 通过 | Reception、Results、Validation、Reports 入口按实际角色展示 |
| 首页待录入跳转 | 通过 | 进入 `/Results?scope=pending` |
| 首页待审核跳转 | 通过 | 进入 `/validation?type=routine`，不再出现无查询条件的空搜索页 |
| 首页待报告跳转 | 通过 | 进入 `/RoutineReports`；空参数 `/Report` 自动归一到报告中心 |
| 科室选择 | 通过 | 可键盘操作；保存成功后才跳转；失败、加载和空状态均有提示 |
| 审核空状态 | 通过 | 无结果时不渲染英文空表、分页和电子签名提交按钮 |
| 审核宽表布局 | 通过（代码/构建） | 宽表限制在内容卡片内横向滚动，服务端分页不再使用 110% 宽度 |
| 增量组件回归 | 通过 | 5 个核心文件、13 项相关测试全部通过 |
| 中文交付门禁 | 通过（覆盖门禁） | `zh`、`zh-CN` 覆盖 1261 个已引用文案键；未覆盖业务主数据英文同值仍列入 P2 人工审校 |
| 订单筛选 | 通过（代码/构建） | 搜索增加 300ms 稳定期；筛选变化回到第 1 页；接口失败不保留旧数据；日期为日/月/年 |
| TypeScript 回归门禁 | 通过 | 当前 1195 条，低于 1209 基线且无新增诊断类别 |
| 前端生产构建 | 通过 | Vite 转换 2585 个模块并生成生产资源 |
| 本地部署（上一批） | 通过 | `http://127.0.0.1:18080/login` 返回 HTTP 200；四个核心服务在线；不代表本轮新增代码已部署 |
| 真实 Chrome 登录与导航（上一批） | 通过 | 纯 HTTP 环境成功登录；左侧菜单可见；待录入入口进入统一结果页并显示 18 条真实分析；本轮报告/072 页面证据另行待补 |
| 结果保存状态回写 | 通过 | 订单 `DEV01260000000000002` 的 WBC 结果 `7.2` 保存并刷新保持；页面状态中文显示 |
| 标签打印计数 | 通过 | 订单 `DEV01260000000000001` 打印后计数 `0 → 1`，日志无 `StaleStateException` 和计数写入失败 |
| 后端 P1 门禁（本轮） | 通过 | Java 21 167/167 通过；统计报告专项 5/5 |
| 前端全量回归 | 通过 | 共执行 150 个测试文件，944 项通过、13 项跳过、0 项失败 |
| Service Worker 路径 | 通过 | 注册与资源地址跟随应用实际部署基址，相关错误已纳入全量回归 |
| 旧式交付页面中文 | 通过（本轮范围） | 旧结果、电子签名、条码打印确认和患者结果查看器关键文案已完成中文化 |
| single-spa 导航 | 通过 | 本轮复现的应用内导航告警已消除 |
| 患者历史旧数据 | 通过（真实 Chrome） | 真实结果 `7.2` 已恢复显示，虚假 `0–0` 范围被移除；无 `TestResult` 且 `resultType=N` 的合法 `0–0` 保留，多历史有效范围确定性选择最新；8/8 专项回归通过 |
| 可选 FHIR 启动隔离 | 通过（配置/实测） | 本地可选 FHIR URI 为空，最终运行容器启动实测 `53306 ms`；真实 FHIR/TLS/容器仍未验收 |
| 订单步骤完成态 | 通过 | 已完成步骤依据持久化 `stepProgress` 显示完成，不再误提示“仍缺少必填信息” |
| 可选通知服务降级 | 通过 | 本地未配置推送服务时只检查一次并静默显示未订阅；组件卸载后不再回写状态，浏览器控制台不再重复报错 |
| 外送检验查询页 | 通过（组件/真实 Chrome） | 菜单、面包屑、标题、发送日期和日/月/年均为中文；未查询及无匹配分支均无空表分页，无匹配提示正确；聚焦 4/4 |
| 检测目录与管理导航 | 通过（真实 Chrome） | 管理导航、检测目录列表和编辑器基本信息已验收；面包屑、帮助、样本类型、业务域、AMR、组合框清除/展开均为中文，无 `Clear selected item`、`Open` 等英文 UI |
| QC 与报告空态 | 通过（真实 Chrome） | QC 空态、警报空态及报告目录均为中文并可正确进入目标页面 |
| 外送权限 | 通过（自动化） | 接口要求 `ROLE_RESULTS` 并按用户 Results 实验室单元过滤；真实有数据及不同角色浏览器分支待数据/账号后验收 |
| 报告统一鉴权 | 通过（本轮安全收口） | Java 21 P1 167/167；旧入口统一 `ROLE_REPORTS`、实际分析科室与未分类默认拒绝；`CISampleRoutineExport` 和 `patientCollection` 部署后均返回 403 |
| 客户端异常日志 | 通过 | JSON 不可读、缺少请求参数、媒体类型不支持和访问拒绝降为 WARN；`AccessDeniedException` 返回 403；专项 3/3；最近日志只有授权服务 WARN、无 ERROR |
| 分析仪 CSV 导入 | 通过（门禁/真实启动/数据库） | Micros 16 项、Pentra 20 项；无 `duplicate key`、`test_desc_uk`、`25P02`、`transaction aborted`；数据库 22 个唯一 CBC 项目，12 组 `%/#` 为独立 ID |
| 中文主数据迁移 | 通过（真实数据库/真实 Chrome） | Liquibase 071 成功；血液学 1、全血 1、CBC 36；事务两遍 0 重复、英文不变、回滚有效；`/Results` 显示 18 行中文 CBC 与全血；嵌套 Testcontainers 因无 Docker socket 未运行，不计业务失败 |
| 中文主数据 072 | 通过 | 已正式部署；`zh_CN`/`en` live API 均 200，中文科室/样本正确，`en` 仍为英文；Chrome 结果页和 SampleType 主名验收通过 |
| 样本类型与报告查询交互 | 通过（本轮范围） | SampleType 15 项中文主名与中文分页 aria 通过；报告弹窗阻断可见，统计页实际打开 `ReportPrint` |
| 报告目录“安全适配中” | 通过 | 受限入口不可点，受限页显示中文阻断；安全统计报告可生成 PDF |
| COVID 半开日期范围 | 通过（自动化） | `[startInclusive, endExclusive)` 覆盖结束日且排除次日零点，纳入 Java 21 P1 167/167 |
| 后端启动（本轮） | 通过 | 四容器正常，webapp healthy，启动 `236680 ms`；日志无 `ERROR`/`SEVERE`/唯一键或事务中止特征 |

说明：本轮已解除此前的纯 HTTP 自动登录验证阻塞，但尚未完成所有岗位角色矩阵、所有旧式页面和所有有数据审核分支的逐页验收；未覆盖项仍不得计作通过。

## 13. 当前遗留风险与下一轮范围（2026-08-24）

1. 管理导航、检测目录列表与编辑器基本信息、QC 空态/警报空态、外送无匹配空态和报告目录已完成真实 Chrome 中文验收；QC/外送有真实数据及不同角色的浏览器分支仍需后续复验。1790 个实际引用文案键完整不等于全部业务主数据中文验收通过。
2. TypeScript 当前 1168 条错误虽未突破 1209 基线，但存量仍需分模块消减，不能描述为类型检查全绿。
3. 旧 PatientReport 未过滤分支已由显式安全能力和默认拒绝机制收口；仍需消除非 COVID 选择器“授权后再次查询”的时序窗口，并统一“空/不存在”与“存在但无权”的外部响应，避免存在性侧信道。
4. 本地试用已将可选 FHIR URI 置空，主系统启动不再依赖不可达服务；真实 FHIR 镜像/容器、异步外发、双向 TLS 证书链及外部联调仍未完成，不得对外宣称接口可交付。
5. 分析仪 CSV 冲突与事务回滚已通过 6/6、真实启动和数据库结构复验；真实仪器串口/文件传输、厂商异常报文及重复文件并发导入仍需外设联调。
6. 072 已正式部署并完成双语 API/Chrome 验收；其他检测、单位、参考范围、机构同义词和旧管理页仍需按机构目录逐页验收。
7. 显式安全的聚合报告尚未按实验室单元过滤，HIV 等小样本计数需最小展示阈值；SSO 登录科室仍使用展示名称和 `-` 分隔，重名/连字符场景应改为稳定 ID；批量导出策略也应从结果范围能力中独立出来。
8. 个别空角色映射输入可能返回 500；控制器仍需补真实会话集成；统计报告空条件已修复，非 COVID 日期 DST、无界范围及大数据 N+1 仍需可靠性/性能测试。`patientCollection`/`patientAssociated` 仍安全禁用，其他未适配批量报告不计已交付。
9. Vite 仍有既存的 `:global(...)`、直接 `eval`、大 chunk 和动态导入警告；正式交付前还需完成角色权限矩阵、审核/报告有数据分支、备份恢复、并发容量和外部接口证书链联调。
10. 中国版当前只向用户显示中文，内部兼容资源不作为用户语言选项；这些兼容资源仍用于上游兼容或格式兜底，不应把“未删除内部英文资源”误判为用户界面仍支持语言切换。
11. 0.9 的真实 Chrome 证据覆盖当前最终镜像、当前管理员会话和现有数据；八个岗位菜单组期望项齐全不等于八类独立账号权限矩阵、全部深层页面或所有有数据分支均已完成验收。

## 14. 最新临床闭环回归（2026-08-29）

| 执行项 | 结果 | 说明 |
| --- | --- | --- |
| 后端 Java 21 P1 | 通过 | 210/210，0 failure/error/skip；中文日期时间严格解析专项已纳入门禁 |
| 中国临床闭环 E2E | 通过 | 2/2；申请号 `DEV01260000000000020`，从申请到正式 PDF 报告完整通过 |
| 页面导航与标签 | 通过 | 10 次业务导航正常；标签请求命中 `/api/OpenELIS-Global/LabelMakerServlet`，未跳回工作台，无 404 |
| 结果与审核 | 通过 | 检验结果 `7.2` 保存后只读显示，审核接口返回 200，正式患者检验报告返回 200/PDF |
| 日期数据库一致性 | 通过 | E2E 新增接口持久化断言；数据库 `received_date=2026-08-29 13:49:00` |
| UI 运行告警 | 通过 | 核心链路未出现 Carbon Tag/DatePicker 已废弃契约告警；全局静态守卫防止同类回归 |

运行日志无 ERROR/Exception。仍存在可选 FHIR 未配置时构造空机构引用的 WARN，以及旧地址对象缺版本字段的 WARN；当前未导致业务失败，后续应以“关闭即不执行”和实体版本完整性为目标清理，不能据此宣称 FHIR 外部对接已经交付。
