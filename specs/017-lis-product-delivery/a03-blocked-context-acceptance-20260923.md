# A-03 受限标本说明：当前候选验收卡

补记：本卡保留初始候选与待验场景。后续1280/390、键盘、行定位及紧凑间距实页记录见 [整合验收](a02-a03-integration-acceptance-20260923.md)；最终提交与部署状态仍待该记录补齐。对象交接和完整A-03没有据此关闭。

## 范围与来源

基线 `3e1e3b402fc549236c28446cc53cd652c913385b`。承接A-03原因/入口核查；在现有Results工作台上集中相同实管共同原因，保留项目独有/未知/身份缺失行内警告。默认全表折叠概览，选中标本后直接显示；行内“受限说明”按groupKey+reason定位并聚焦。每组显示当前筛选内distinct Analysis，不以多组件重复计任务。

没有新建替代页面，没有修改业务准入、签名或后台写保护。Reception岗位、会话与查询有效且无任何草稿/未知提交/保存中时，才可进入现有申请列表人工核对；按钮不携对象深链，不创建交接。提示改为具体操作语句：按实验室编号查找，核对后返回重查。

新增countsAvailable控制：加载/失败时不把左队列、选中任务和概览受影响数显示成0；已经加载的受限事实仍保留，不能因刷新失败让人误认为已解锁。

## 开发与独立检查

- 原隔离候选基线4f3、11文件，独立审查通过；以diff合入3e，保留A-02不同Analysis计数、队列第7测试及新文案。
- 原隔离3文件30测试通过；主树合入后3文件31测试通过。
- 第一次类型门禁发现Carbon InlineNotification不接受role=note；保留外层note/id/focus，内层改status并aria-live=off，独立复核后再次31通过。最终类型门禁401既有诊断、无新增，并非完整TypeScript零错误。
- 11文件格式及diff检查通过；主负责人随后仅将两条中英文引导改为具体操作，不改变权限/路由。最终格式/完整回归/构建/实页状态待追加。

## 实页必须检查

1. 正式源码构建产物，1280×720默认概览不堆叠8组长说明；表格与主要操作可达。
2. Tab/Enter/空格可展开及收起；收起内容不参与Tab；行按钮选中实际标本并将焦点移至对应原因；更换标本不串原因。
3. 390px窄窗口不挤出整页，表格局部滚动；未知/analysis级原因仍说明。
4. 未保存及未知提交保留离页保护；无Reception不提供查询入口；所有受阻任务继续不可写。实际浏览器不写入旧临床记录，合成单元场景承担本批草稿/保存保护回归。
5. 新包发布后正式入口版本、资产SHA和截图一致。

当前实页待核；完整读屏软件验收、对象级合法交接与院方责任制未完成。不能用该UI子批关闭A-03整项。
