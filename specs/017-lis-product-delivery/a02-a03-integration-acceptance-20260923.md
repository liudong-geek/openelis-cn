# A-02 / A-03 待办与受限说明整合验收（2026-09-23）

本记录对应固定需求 A-02 / A-03 的限定子批；A—E 共14项的分母和最低验收不变。尚未关闭任何整项，最终状态以工作区执行台账及本文件后续发布记录为准。

## 当前可核对的实现

- 首页结果与审核待办分别读取当前用户权限下的轻摘要，显示任务单位和查询时间；未知不显示成零。审核入口自动查询全部有权待审核任务。
- 审核查询将整次查询的检验任务、受理号组、展示行和质控受阻数分开；分页沿用查询上下文。未指定条件、真实空队列、失败分别呈现。原批准、退回、签名及发布后只读规则保留。
- 结果页按真实标本身份集中共同受限原因，默认概览可展开，行内按钮选择对应标本并聚焦原因；保留项目特有原因及未知身份的警告。
- 同一会话的 CSRF 掩码轮换不再阻断首页和结果只读查询。读取前后仍核验服务器身份与权限，账号/权限变化隐藏旧数据；真实401/403仍撤销访问。已有结果写入和签名校验没有放宽，写态会话的全局改造仍属A-01/C-02后续。
- 手册4.5、4.6、5.1及保存未知状态说明同步到实际页面。打开申请列表仅为人工核对，不表示已实现对象交接。

## 已执行的后端验证

独立后端树编译2992主源、674测试源，15类121项定向测试全部通过，0失败/错误/跳过；runner生命周期6例另计。最终39个后端/合同文件逐字节合入主树，独立复核签收。

已测WAR为232623499字节，SHA256 `66def8a85693b31addfd6dcd863b38d11e5774c31070c4bd3dcb1ce9706204b7`。候选镜像 `sha256:cc1e3a62703eee5b045d2200fa61e3e85264fc5a4d2cf0f2716ae118948a7758` 内最后一层WAR与其一致，23项运行配置核对无漂移。

2026-09-23 22:14:35替换后端用于真实接口验收，22:18:09 Tomcat启动完成，随后健康检查通过。前端正式容器此时仍为3e旧包；候选前端使用18082的实际生产构建连接该后端。

只读接口实际结果：

| 核对         | 实际结果                                                                        |
| ------------ | ------------------------------------------------------------------------------- |
| 结果队列     | 25个distinct Analysis、8份有身份标本、25展示行，ID集合与替换前一致              |
| 审核队列     | 2个distinct Analysis、2个受理号组、2展示行、0个质控受阻任务，ID集合与替换前一致 |
| 审核轻摘要   | 2项/2组，展示行及QC计数保持null/partial，未虚填0                                |
| 审核未查询   | searchFinished=false，无queryId，空列表，summary=unqueried，四个计数为null      |
| 伪造actor    | 忽略请求中的伪造用户，保持当前登录用户范围                                      |
| 非法范围/页  | pending和筛选混用400，越界查询页409                                             |
| 匿名         | 302，未泄露计数                                                                 |
| 指定申请筛选 | 返回filtered范围，所有行符合指定受理号                                          |

本机只有一页审核数据；多页/权限变化/失效上下文由定向测试覆盖，不把本机一页读回称为真实大队列验证。首次接口脚本以直接索引读取未查询queryId而失败；已按AppConfig.java的NON_NULL配置校正为“缺失或null”，保留失败日志后重新完整执行通过，未修改应用以迁就脚本。

## 已执行的实页验证

- 首页显示25项待录入、2项待审核；点击审核自动加载2项队列。按专业组切换先显示“请先选择查询范围”；生化返回1项，分子生物学返回0项，四项统计均为真实0。
- Results相同账号双标签：第二页刷新后，旧首页进入结果成功读取25行，再手动查询仍25行；没有错误登出/加载失败，写状态未确认提示保留。此项仅证明读取修复。
- A-03行按钮选择18项标本并把焦点交给其原因；返回全部25行。Enter展开8组原因，Tab进入有界滚动区，Space收起后Tab跳过隐藏内容。
- 1280×720稳定默认选中态、无额外告警时，首行底部719.4px；额外协同/写暂停提示会占用高度，未承诺所有异常态都在首屏。对比失败版首行761.4px已保留。修改只减少提示组件内部及表格上方重复留白。
- 390px结果页document宽390px，表格内部330px容器承接865px内容；选中受限说明宽332px且焦点正确。审核页统计和模式区域也未撑破390px页面，窄屏沿用既有卡片展示。
- 本机数据均按现有模拟数据进行只读核对，未在正式模拟库执行本批结果保存、审核提交、签发或打印。

## 整合门禁与尚待发布

本节在最终门禁完成后补实数。此前670项分组定向覆盖及Dashboard23/23、类型回归（401历史诊断，1209基线，无新增）、1896字典引用和生产构建已通过；不以旧3712全绿替代本次最终全套。

本次全前端单worker已结束：302文件中300通过、2失败，3871例中3850通过、8失败、13原有跳过，耗时2049.67秒。6个TypeScriptBaseline用例是5秒子进程ETIMEDOUT；SystemAuditEvents.patientScope先出现15000ms超时，再出现后续选择患者按钮缺失。两个测试及依赖均未修改，随后在同一冻结源码上按原文件、原阈值单worker复跑完整两文件：42/42通过，89.29秒。1314个前端输入无变化。结果支持该次运行的时序/资源问题，但未证明具体由沙箱或某一进程造成；不能将首次全套记录改写为一次性全绿。综合本候选实际覆盖为3858项通过、13项原有跳过。

Cypress完整fail-fast在独立临时数据库/后端执行，原34spec含写密码/患者记录，禁止对现有业务库运行；工具已具备，不用“工具缺失”描述。隔离环境准备后开始执行，尚未取得完整Cypress通过或豁免。

本记录保存源代码提交前的实测状态；下一步先形成可追溯的本地源代码提交并部署前端作实页核验，远程推送仍须以完整Cypress结果为准。后续实际提交和运行版本写入发布记录，不预填尚未执行的结果。

46个前端变更文件按frontend目录既有Prettier配置检查通过。public手册是既有生成目录，按原忽略规则处理；从仓库根误用Prettier所产生的手册格式警告不代表产品变更。1314输入/208产物保持冻结一致。独立隐私预检确认48个新增文件均为源码、测试或说明，私有运行配置、认证信息和数据库dump未纳入提交。

## 证据和回退

临时原始证据位于 `/private/tmp/lis-active-20260923`，最终交付时同步非云盘长期目录。私有容器配置、认证信息、数据库dump不进入Git。

- `a02-review-release-preflight.json`：源码、WAR、镜像与23项配置一致性。
- `a02-review-runtime-api-before.json` / `a02-review-runtime-api-after.json`：审核数量/ID集合指纹及负向校验。
- `a02-runtime-api-after.json`、`a02-results-on-review-runtime.log`：结果队列回归。
- `a02-a03-candidate-browser-checks.json`及screenshots：真实页面和尺寸验证。
- 独立审核见本目录的review/read-session复核文档。

后端回退镜像保留为`openelis-cn-webapp:before-a02-review-20260923`；私有compose的旧image字段为`:a02-candidate`，部署和回退均须使用明确image override。数据库结构和内容未因本次查询子批迁移。

后续仍需A-01写态会话、A-03对象交接、B/C工作区及D/E业务闭环；本批不解决医院真实制度与接口验收。
