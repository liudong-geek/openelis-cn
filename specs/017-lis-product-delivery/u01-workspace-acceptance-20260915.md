# U01 菜单与结果工作区限定验收

日期：2026-09-15。基线：`64ef61bfe6187bf223e0a5114ec95633eed2c831`。状态：前端功能开发已验，随本批提交；未部署，完整系统发布门禁未通过。

## 本批内容

按已确认原型组织八个国内工作区，只重组服务器已授权菜单，保留叶节点身份、原路由及报告文档入口。导航采用站点配置和部署默认值，与用户显示语言独立。结果工作区增加实管队列及详情区，保留草稿、遮罩、会话、未知保存、签名和独立回读保护。多选浮层定位适配宽表；原多选读回测试补充真实可见尺寸，不跳过选项替换及新结果编号重新编辑断言。补齐工作区翻译，并修正八个英文包误填中文的既有文案，使语言交付检查可执行通过。

从原项目 HEAD 构造独立候选，只纳入本功能及必要导航/结果显示基础。未将请求恢复、App 会话改造、其他管理页面样式和后端历史积压混入提交。原工作树其他变更保留。

## 验证结果

| 检查 | 结果 | 实际范围 |
| --- | --- | --- |
| 最终功能回归 | **573 / 573，通过，零失败/跳过；22 文件** | layout、统一结果目录、共同选择器；包含真实 Carbon 浮层、保存回读和未知状态等保护 |
| 正式前端构建 | 通过 | 实际 Vite 产品构建；仍有大分包警告 |
| 语言交付检查 | 通过，1856 个源码引用键 | zh、zh-CN；保留最终原始日志 |
| 项目 Prettier | 本批源文件限定格式化通过 | 通过项目 format 脚本执行，不全量重排其他页面 |
| TypeScript 实际基线比较 | **HEAD 464 条，候选 464 条，新增 0 条** | 同一编译器，按文件/错误码/消息对照；原仓库类型门禁仍失败，未改基线或删除诊断 |
| 浏览器模拟验收 | 通过 | 独立候选真实 Layout/Header/UnifiedResults，六份显式 SIM 标本，内存接口。0 输入跨标本切换保留，保存后独立回读为 0/待审核；页面宽度 1280，body 无整体横向溢出 |
| 独立代码复核 | 未发现本批未关闭的业务/权限回归 | 导航权限和叶路由、实管隔离、最严格患者遮罩、草稿及签名保护、屏幕/打印样式范围 |
| Maven Spotless | 限定前端范围执行通过 | 本批没有 Java/XML 修改；按实际正则参数重试后成功，未全量重排后端 |
| Maven 完整构建（后续补验） | 通过，约100秒 | 同一未修改的Java/POM，隔离缓存补入原有版本元数据后，Java21编译2919源码、WAR/classes JAR及clean install成功；后端测试按参数跳过。早期阻塞日志保留 |

首轮 563 项中一条旧测试因新增队列重复显示条码而失败，改为只查询结果表格；最终 573 项为补充浮层/选择器覆盖后的单次结果，未累加不同轮次成绩。

## 验证边界与接续

本片满足对应前端增量工程条件；全量类型、隔离数据库、完整认证/权限链、临床审核与签发、设备/打印及医院 UAT 尚未由本次验收覆盖。模拟数据不构成真实业务验收，不更新运行版或里程碑完成状态。

下一批为 W01：保存并独立回读后，按审核角色和未完成输入保护进入既有审核页，传递完整申请号；再接续 W02 的实际值/版本与待审核队列一致性。

本地完整证据：`/Users/ld/Documents/lismyself/docs/verification/u01-20260915/`。截图：`/Users/ld/Documents/lismyself/prototype/screenshots/integrated/u01-clean-candidate.png`。功能队列见 [逐批开发计划](development-roadmap-20260915.md)。
