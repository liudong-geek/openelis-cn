# 当前申请核对后登记剩余标本：限定验收

日期：2026-09-13。执行唯一计划 v3 的当前下一主批；基线 `ef9ee6d07`。证据目录：`tmp/collection-resume-20260913-cmsT6Z/`。

## 本批实际变化

- 同一 current 读取事务附带类型、项目、组合、单位与状态名称/活动事实，以及实验室日期格式、时区和已有知情同意记录。缺失或停用的历史主数据不伪造有效名称；未知同意不当成已同意。事实不是写权限，原后台采集入口仍重新校验。
- 原核对面板内明确“确认本次申请，登记采集”，只列当前适用待采集管；已采集、已取消记录仍完整展示。日期、时间、采集人员、实际采集量按四列/两列排列；使用明确年月日、24小时格式及中文时区名称，避免浏览器美式日期和AM/PM混杂。日期/时间由本次服务器查询预填，可编辑；没有自动勾选标本或自动保存。
- Provider 私有核对快照决定申请与逐管身份，不接受页面伪造ID；采集只提交 collection-only 三个顶层字段，不回填/解锁原整单表单、不重开单、不自动完成整个采集步骤或跳转贴签。单次专用POST禁止重定向、可取消、限制正文1 MiB，成功还需current逐管类型/项目/组合/数量/时间/人员一致读回。
- 发送前保存当前标签页的不透明核对摘要，仅含原提交核对码、随机盐及哈希，不保存患者、申请/管ID、日期、人员或采集报文原文。超时后的REQUESTED并非失败证明，不能解除待核对保护；同页重查和刷新都须读到原选中管完整匹配的COLLECTED才能消解。没有首单marker时，也会恢复采集核对码；普通整单保存不能绕过未核实采集。

## 验证与证据边界

- 后台 `java-green3`：146/146，14.246秒。精确Java21编译HEAD依赖与本批两份reader源/测；输入/依赖指纹稳定。覆盖缺上下文、nullable/false同意、配置日期三格式、停用/空名称/缺主数据/错ID及既有完整current、身份与读取事务回归。DAO与配置为SIM；没有SQL或真实数据库。
- 独立 `candidate-v3`：HEAD只叠本批19份源/测/语言，八套前端限定测试140/140，0失败/跳过；完整前端构建34.48秒。包含实际Provider+Carbon/Intl操作、真实传输适配器+模拟fetch/流、真实webcrypto最大1000管摘要、刷新/会话变化/重复提交/错读回。较早当前工作树140/140，最后日期呈现调整由最终候选和浏览器再次验证。测试数不累加为全系统成绩。
- 候选SIM静态夹具使用真实Provider、核对面板和专用表单/传输，所有业务接口由内存模拟，CSP禁止业务网络；1440与768两种宽度验证部分采集、逐管选择、未改原表单、未知后刷新阻止再提交及整页无横向溢出。最终截图人工复核；不是实际开单整页、Safari、200%缩放或数据库端到端证据。
- 有效早期后台红轮143项2失败（缺上下文）；后续测试夹具DateUtil初始化及默认false同意问题修正。首次Flow缺新模块未进入用例，不冒称业务红轮；后续实际Carbon复选框可访问名称与初始化GET断言问题已修正。独立审查指出重定向、选中计划比对、未知重试、摘要上限、无首单marker恢复问题均补对应测试。首次浏览器因沙箱无法启动，保留失败证据，取得临时本地静态页面/浏览器权限后再测。
- 仅指定前端文件格式及diff-check；完整Maven/Spotless、历史全量类型436条诊断及全项目门禁未闭合，没有重复未变化重型失败。构建大包/插件耗时及SLF4J NOP警告保留。

## 未完成与下一主批

1. 当前为“查询原申请→明确采用→登记所选剩余管→核对采集事实”的源码与SIM检查点，不是医院交付，也不是18080新运行版。AC09/AC14、报告安全P0与M0～M7父里程碑均不关闭。
2. 本标签页摘要不覆盖关闭标签页、清理站点存储及跨客户端；后台逐管事务锁的真实并发、撤权、迁移、回滚、提交断连及审计仍须获准隔离库验证。长期未知或明确拒绝后缺可消解事实会继续阻断，不提供“清除保护再试”按钮；受控失败处置仍须收口，不称所有异常已可用。
3. **下一唯一主批：在同一申请工作区收口采集明确失败/长期未知的可核实处置及采集后进入签收/贴签的状态衔接；不得恢复旧整单保存或新增平行菜单。** 获准隔离数据库时优先验证首次保存→刷新核对→部分采集→再次恢复及失败处置，再接后续结果、审核、报告。不能继续循环扩展本批摘要工具或用更多局部测试冒充闭环。
4. 未启动Docker或业务数据库、未处理真实患者/设备/打印/签发/外部发布。18080没有替换；待同一发布候选全部门禁、授权DB/配置备份及固定可运行回退齐备再更新。源码存档不等于DB备份/运行回退。

## 定时与保存

只读核实仍唯一 `lis`、每小时ACTIVE，读取最新v3及顶部检查点；未变更频率/启停或另建计划。定时不是开发或交付必需条件，仅负责接续；仍有安全可验证工作可暂留，只剩外部阻断时应暂停。已通过独立终审，限定本地提交前核对候选逐字一致和源码存档；不全量暂存，不推送，保留其他历史修改。
