# W02a 待审核查询数据准确性限定验收

日期：2026-09-16。基线：`38031c0c29e862e6ab3c674f9f1c156fb018fb33`。状态：限定开发已验，随本文件所属提交归档；未部署。

## 本批范围

复用既有 `/rest/AccessionValidation` 及原审核页面。精确号与范围/日期入口使用同一待审核状态白名单；补齐实际实管、申请内部编号、状态、分析版本、原始值与聚合行完整结果成员。多选和级联按任务及真实组件归并，历史 NULL 主组件与显式主组件保持同一保存作用域，不串其他组件。保留旧展示值、POST 字段和数组顺序；新增证据字段只读，不能由客户端伪造后覆盖缓存。

前端新查询和后端翻页开始清除旧列表，只接受最新请求；取消和卸载后的迟到成功/失败均不更改列表或通知。已勾选审核、复检或修改备注时阻止新查询，保留原结果和操作。无修改的新批次重建旧表格控件和本地分页，避免索引相同的另一结果继承旧勾选。原有完整申请号跳转保持不变。

## 验证

- 最终前端相关回归626项全部通过，0失败/跳过；包括查询生命周期16项、真实 Index/SearchForm/Validation 集成6项及旧查询9项。
- 实际 HEAD 类型对照464→464，新增0。完整类型门禁仍未清零。
- 前端生产构建53.14秒通过；语言检查覆盖1859个引用键；npm格式及Maven Spotless通过。正式Maven专项9项通过，2919生产源码及628测试源码编译成功。完整Java21离线clean install打包94.84秒通过，该打包命令跳过测试，测试独立执行；格式化后相关回归37项通过，其中新增专项9项、既有ResultEntryConstructionTest 28项。扩展的旧ResultsValidationUtilityTest 4项依赖Testcontainers，因“Could not find a valid Docker environment”无法初始化，未算通过，保留失败报告；不启动未知旧数据库环境或修改测试规避依赖。
- 浏览器使用真实 Layout、Index、SearchForm、Validation 与内存模拟接口：SIM-UI-001勾选后搜索SIM-UI-002，被提示阻止且保留旧勾选；撤销后查询002显示新结果11.2且不继承勾选。首次预览数据pastNotes错误修正为服务端字符串格式；依赖预编译完成后刷新。没有真实审核POST。

## 构建环境修复

原Maven的wagon-http JAR只剩iCloud占位，Classworlds启动停滞。使用现存Maven3.8.8文件准备隔离离线工具副本，省略不可用的HTTP transport；设置仅本地cache的offline mirror，以既有真实mirror metadata解析json-smart范围依赖。未改产品POM、原工具或原依赖缓存。恢复后正式Maven测试和完整打包均成功。

## 尚未完成

本批是 W02 的第一片，不代表完整待审核详情或临床审核闭环。会话共享分页缓存仍未绑定查询，多标签页覆盖、权限变化后重新校验及分析实际实验组过滤留在下一主批 W02b。旧表格原始值/版本详情、宽表操作可见性也在下一批完善。

W03继续处理旧审核表格的受控状态/批量分页、审核及退回事务、签名前后上下文和版本核对、旧Logbook入口状态一致性。报告P0、真实隔离数据库的SQL/审计/并发、多岗位/设备/HIS/打印及医院验收没有关闭。

本批没有推送、部署运行版、改真实患者数据、临床审核、签发或连接仪器。证据存于本地工作区 `docs/verification/w02a-20260916/`。提交时仅纳入本批受测差量，保留原项目其他历史修改。
