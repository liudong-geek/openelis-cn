# 逐管首次验收：原申请页面限定验收

日期：2026-09-14。基线：`a3715d9d2`。本批为既定逐管验收主批的页面接入，不另建菜单或平行业务模型。状态：**开发及隔离模拟验证通过，未部署、未完成真实数据库或临床交付验收。**

## 本批完成

- 原申请 `EntryRecoveryPanel` 内显示实际管列表，明确选管→接收或选择有效拒收原因→核对患者／条码／类型／采集签收时间→人工确认→独立读取原 current。初始不代选管、决定或原因；每次只写一管，两管可分别接收／拒收。
- 已记录内容在同表只读展示，操作列明确“已记录（只读）”，不是无效的“查看”按钮。选中管显示“正在核对”。窄屏只在表格内横向滚动，条码和时间不拆行，确认区完整展示当前患者和条码。所有新增可见文案均为中文。
- 原已授权、只读 REPEATABLE_READ current 补充有效拒收原因目录（固定 `DICTIONARY:resultRejectionReasons`、稳定ID、微秒版本、原字典文案）及已存首次事实摘要。目录无效／缺失不允许自由输入或用QA事件代替；历史原因仍可保留其原来源。
- 前端严格核验投影、全管集合、署名、版本与归属，按Java合同排序并保留微秒生成摘要。拒收改变管版本后，用已存摘要独立核对，不将当前版本重算为旧首次证据。
- 操作能力绑定私有current、实际操作者、冻结CSRF及会话；异步准备、提交和回读后均复核。单次POST及回读均核对决定、原因、身份、署名和时间。旧采集／签收／标签／整单QA及此入口共用互斥。
- 未知结果在会话存储只保留操作号、提交码、随机盐和完整事实／操作者摘要，不保存患者正文。原记录匹配前不清除，不因“未查到”而重复提交。超时／迟到成功不复活旧操作；同凭证迟到401／403在丢弃过期业务响应前清患者视图并锁写，旧凭证不影响新凭证。同步卸载会使尚未发送的摘要准备失效。

## 实际验证

证据目录：`tmp/intake-ui-20260914-PPkyZ8/`。

| 范围 | 本轮结果 | 限定含义 |
| --- | --- | --- |
| 最终前端 `candidate-v4` | **390/390，19套，0失败／跳过**；测试前后摘要不变 | 原Provider／真实Carbon组件＋模拟网络，含旧采集、签收、标签、QA兼容；不是全项目前端测试 |
| 最终后台 `final` | **370/370，18套，17.376秒**；59源／资源和依赖摘要稳定 | 7份本批Java源测，其余取同Git基线；原真实Hibernate零连接映射／MVC／事务测试同轮重跑；目录HQL为绑定测试，没有SQL执行 |
| 完整前端构建 | **通过，36.88秒** | 有既有大包／插件耗时提示；不等于TypeScript历史诊断清零或完整发布门禁 |
| 本机隔离浏览器 | **5/5**；1920、1440、1366、1024、768 | 每宽度接收第一管、拒收第二管、未知刷新零重发；0页面异常、0禁止请求；目视确认截图 |
| 格式与独立复核 | 通过 | 本批Java按项目Eclipse规则，前端候选Prettier；独立只读复核代码、指纹、测试和截图；未跑完整Maven Spotless |

初轮前端379通过／4失败保留：发现连续第二管状态锁、会话核实后旧视图、危险按钮默认英文描述和一项无效主数据夹具。独立复核进一步发现冻结CSRF遗漏及显式GET迟到拒权漏接，补真实Provider回归后最终通过。初次浏览器因沙箱禁止监听／Chromium进程启动失败已留档，随后仅对本机纯模拟页面申请权限后通过；没有绕过真实业务环境权限。

候选从Git基线提取，历史脏 `OrderContext` 只应用本批增量，三个语言文件只加入本批前缀；其他历史修改未混入。24文件（16前端、7后台、此验收）分别归档工作树与精确候选，暂存、提交后逐文件核验；最终清单为 `source-final-integrity.json`／`commit-integrity.json`。本地提交、不推送；源码归档不等于数据库／配置备份或可运行回退版本。

## 未关闭的边界与下一步

1. 本次“首次接收／拒收记录”不等于当前完整验收合格；`currentAcceptanceVerified`仍为false，不自动开放结果录入、整单QA、上架或报告签发。真实操作者目前显示编号，不虚构姓名。
2. 400／409也保守保留未知凭证；NOT_RECORDED不是回滚证明。明确冲突后的可安全重试交互、跨客户端／长期未知、撤销更正及重采完整闭环仍待；不能声称所有失败都可页面自行恢复。
3. 未连接Docker、真实数据库、18080、设备或打印机，未改真实患者或签发报告。083迁移、真实SQL／并发／审计、多岗位完整认证链、主数据、临床规则、报告P0、类型及发布门禁、医院现场验收均未因此通过。
4. **下一唯一主批：同一结果工作站的逐管首次决定与当前结果录入准入联动。** 复用现有记录和既有结果保存／查询入口，区分未验收、接收、拒收、归属或事实变化的当前状态，补界面与后台一致的阻断／核对；不得仅凭历史ACCEPTED或未拒收放行，不另建菜单、平行模型或重复回执工具。真实隔离库恢复后再作包含首次记录的完整模拟主流程联测；不以本批SIM代替正式交付。
