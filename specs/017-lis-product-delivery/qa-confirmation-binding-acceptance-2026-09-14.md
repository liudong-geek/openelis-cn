# 当前申请：全部实管核对确认与事实绑定（后台限定验收）

日期：2026-09-14。接续业务提交 `8cb68d61c`，基线 `cf803c4a1`。唯一主批仍为同一 current 工作区的明确验收及保存后读回。本批完成其后台持久字段及写读合同，不新增菜单，不重开另一套验收模型。

## 产品结果及边界

- 现有 `SampleQaChecklist` 行新增 nullable `confirmation_id` 和 `confirmed_context_json`；复用原 `last_updated` 乐观版本，不新增第二个 JPA 版本、不回填旧记录、不把旧勾选升级为逐管结论。
- 新明确确认入口 `/rest/qa-checklist/confirm-current` 要求本次核对码、所见事实摘要、所见清单版本、完整实管集合及完整核对项目。保存前重新锁定并核对真实身份、权限和实际标本；不因客户端自己算出摘要就跳过业务校验。
- 冻结的内容包括申请/患者身份及显示信息、逻辑管与实管关联、数量、采集/签收原始时间、采集人、状态、检验项目、类型/项目版本及名称、知情同意、实际展示的核对配置。保存后及外层提交前完整比对，合法时间改成另一个合法时间、换采集人、主档晚变也不能蒙混通过。
- 原样同核对码重放不再次保存、不改变署名时间；同核对码不同内容、清单版本已变、缺管/多管、缺版本、主档已合并、未签收或不具备核对条件均拒绝。存档 JSON 重复键、布尔强转、整数溢出、错署名及坏绑定不会被当作成功；新旧正常入口均不能覆盖坏绑定，须管理员独立处置。
- 旧正常保存明确清除有效的当前确认绑定，不能复制继承一份已过期确认；保留原实体后由原审计保存机制处理副本。本批未验证真实审计落库和不可变历史，不把此行当不可变逐管事件。
- 原 `EntryCurrentStateReader` 在既有授权、同一只读 REPEATABLE_READ 事务内追加 `qaReview`。分别投影未确认、当前匹配、已失效、坏历史、条件不足；坏配置和未收齐只阻断核对区，不遮掉已有患者/采集签收事实。拒绝查看原 current 时不查询 QA。
- POST仅回核对码及 `readbackRequired=true`，不能直接显示“已验收”。独立 current 重读可核实本次确认是否与当前事实一致。两者均保留 `currentAcceptanceVerified=false`，范围明确为 `ALL_CURRENT_TUBES_CHECKLIST`：**这是整申请全部当前实管的核对快照，不是每管独立合格/拒收结论，不是检验或报告放行凭据。**
- 存放只绑定原聚合义务，不声称记录了具体库位事实；组合、单位的完整主数据治理、逐管不同结论、撤销原因和不可变历史仍在后续范围。没有自行设定临床阈值、生成真实主数据、签发、打印或连接设备。

## 本批真实证据

证据目录：`tmp/qa-confirmation-20260914-2xz1dk/`。

- 最终 `final1`：Java21精确编译；**273/273，12套，0失败/跳过，19.566秒**。36份Java源/测及2份XML，输入/本地依赖前后指纹稳定，`integrity.json` 为 `stable=true/status=0`。
- 实际MVC→真实服务/Guard/Actor/Spring事务；资源为明确SIM内存，包含提交前复核、回滚、重放与实际current接线。只读共享事务夹具实现 rollback-only，验证坏配置不会污染整个 current。没有真实 PostgreSQL/Hibernate 写入、行锁或并发证据。
- 15份指定Java源测按项目Eclipse配置局部格式、限定diff检查通过，不冒充完整Maven/Spotless构建。
- 082迁移只做XML/字段/nullable/唯一约束/注册与原JPA版本契约4项测试，**未执行数据库迁移**。回退明确拒绝直接删确认依据，须另外批准保留证据的迁移/恢复方案，不能把源码存档作为数据库回退。
- 独立审查通过：38候选输入已核验，37份逐字等于当前文件；`base.xml` 精确使用 `cf803c4a1 + 082`，工作树历史080导航差量仍保留，未并入候选。未纳入其他历史修改或危险Hibernate数据库夹具。
- 源码归档、逐文件回读及提交指纹记录由本批 `source-final-integrity.json`、`commit-integrity.json` 保存。未全量暂存、推送或修改定时计划。

失败/早轮记录保留：前批工具的 `binding-red` 在旧guard上186项中2失败，证明合法签收时间/采集人晚变仍会放行；本批 `compile1` 的getter/Iterator调用编译失败已修。`green1` 255项中7失败发现存档申请ID为数字、严格解码要求文本，修正序列化而未放宽HTTP；`green2` 257项、`green3` 263项通过。`green4` 269项中1失败为新接线测试嵌套Mockito调用，修正夹具；`green5` 273项通过后局部格式再跑上述最终轮。不同轮次不累加为系统测试总数。

## 下一唯一主批与未关闭项

下一批在**原 current 核对区**接入本批合同：展示所核对的完整实管和中文核对项目，冻结本次所见版本，用户明确确认后一次提交，随后用原 current 独立读回核对码及事实；处理失效、权限/会话变化、未知结果和刷新保护。不能把旧 `allRequiredVerified`、一次HTTP成功或 `MATCHED_CONFIRMATION` 当逐管临床验收，不新增菜单、不恢复旧整单写入。优先补真实组件及多宽度中文交互，报告安全P0保留。

本批**没有前端/浏览器验收、数据库迁移执行、真实SQL/审计/并发、跨客户端持久恢复、多岗位、独立逐管验收或拒收撤销**。旧通用CRUD及旧列表/完成布尔旁路仍须专项收口；这份有限确认不覆盖所有主数据或专科。真实HIS/厂商、医院主数据、临床规则、打印和UAT继续待授权资料与现场验收。

未启动Docker/业务数据库/未知设备，未操作真实患者，未更新HTTP18080。完整候选门禁、授权数据库/配置备份与固定可运行回退尚未齐备；M0～M7及G0～G11不增加完成勾选。
