# M0 原源码与依赖恢复记录（2026-09-11）

执行依据：交付执行计划 v3 的 M0-02；用户要求“你之前不是有计划吗？按计划进行”。本记录是环境恢复证据，不是 M1 完成、数据库验收或发布许可。

## 1. 已完成的关键文件恢复

本轮使用当前 macOS Foundation 提供的 `NSFileManager.startDownloadingUbiquitousItemAtURL:error:`，仅请求现有指定文件的原云端内容下载。没有替换旧快照、重置 Git、修改 iCloud 设置、删除文件或覆盖配置。

- 首个恢复文件：`Sample.java`；随后精确请求另外 17 个既有占位文件，合计 108,606,558 字节。
- 07:25 的请求收尾证据仍是 17 个占位文件；请求接受不等于完成，该历史记录未追改。
- 07:30 的独立后验：清单内全部 18 个文件已本地化，大小及修改时间保持一致。17 个保护输入的 SHA-256 与请求前一致，涵盖 Git HEAD/index、既有生产代码/迁移、5 份 A1/A2/A3 测试和恢复工具。
- 完成文件包括 `Sample`、`Analysis`、3 份 Hibernate 映射、外部连接测试夹具、Vite 原依赖文件、2 份候选检查测试、Git pack/rev、原 `.env` 和 3 份原容器配置、JAXB runtime、Maven 资源插件 POM 及其校验文件。未展示 `.env` 内容。

恢复后的哈希是对当前原文件的标识；没有声称取得恢复前不可读内容的哈希。大小和修改时间一致本身不等于内容校验。

## 2. 已执行的完整性检查

- 恢复的 Git pack 经 `git verify-pack` 实际校验退出 0；未将统计模式 `-s` 当作完整性验证，也未声称整个对象库 fsck 通过。
- JAXB 3.0.0 原缓存 SHA-1 为 `cd48ab07b2d6143a78a3f10732289930ac6b71e9`，与原校验文件一致；与从 Maven Central 精确版本地址下载到独立暂存目录的文件 SHA-256 一致，均为 `f4a9be9fc70cad8eb703be03b8d9b293ceba35d079dadd3953c8f0b9f4347882`。原缓存未被暂存文件覆盖。
- Maven 资源插件 2.6 POM 的 SHA-1 与原校验文件一致。
- 4 份 XML 离线良构检查、3 份 JavaScript 语法检查通过。这些不是业务用例或数据库回归。
- 同一限定环境探针现在为 25 PASS / 0 BLOCKED / 2 UNKNOWN；未知项是选定本机 Docker socket 缺失和 HTTP 不可达。此探针只检查有限关键文件，不证明完整源码或依赖就绪。
- 既有 M1-A3 冻结包及 integrity.json 校验通过。一次校验从错误工作目录执行导致路径不存在，随后从项目根目录重跑通过；没有修改原冻结包。

证据目录：`tmp/m0-recovery-20260911/request-01/`、`verification-01/`、`known-input-validation/`。恢复工具首轮经独立只读复核，保护指纹与逐请求记录一致。

## 3. 完整扫描发现的剩余范围

07:30 仅元数据扫描结果，不读取占位文件内容：

| 范围 | 占位文件数 | 名义字节数 |
| --- | ---: | ---: |
| 后端 src | 1,284 | 15,020,757 |
| 项目 scripts | 5 | 16,137 |
| Git objects | 274 | 749,381 |
| 前端 src | 195 | 3,730,335 |
| 前端 public | 3 | 7,168 |
| 前端 node_modules | 15,592 | 121,437,389 |
| 原 Maven 缓存 | 4,669 | 321,972,616 |

前五项合计 1,761 文件、19,523,778 字节，已生成固定原文件清单并完成受控恢复。清单 SHA-256：`1c40297413f9eaa38799d26db543db330a44553e57f78e4b100785cabf89af6b`。Maven 是共享缓存盘点，不将其中每项都认定为本项目必需依赖，也不直接全量重装。

### 3.1 固定源码批的实际结果

- 原生批量工具只接受编译期 SHA-256 固定的清单，限定上述 5 个目录、最多 2,000 项/32 MiB；先全量核对原路径、非符号链接、inode、大小及修改时间，再请求下载。严格编译通过。
- 4 项实际安全边界检查通过：无参数、非法模式、错误清单指纹的下载请求均拒绝；正确清单的 1,761 项只读预检通过。该检查未提交任何下载请求，不冒充完整工具测试覆盖率。
- 07:39:54，1,761 个下载请求全部接受，零拒绝/错误；此时逐项即时记录仍是占位。21 个保护输入前后指纹一致，独立复核确认记录与清单同序无重复。
- 07:41:20 后验：619 个已恢复、1,142 个仍等待；该记录保留。
- **07:43:07 后验：1,761 个全部本地化并记录 SHA-256；原 inode、大小及修改时间均稳定，21 个保护输入仍一致。** `src`、`scripts`、`frontend/src`、`frontend/public` 及主仓库 `.git/objects` 无占位文件。
- 随后主仓库 `git fsck --full --no-reflogs --no-dangling` 实际退出 0，无错误输出。主对象/引用目录预检无占位、无 alternate 对象库；未验证子模块仓库和 reflog，不把结果称为整个工作区 Git 全部恢复。
- 恢复后的两组既有认证/申请验收工具单元共 **30/30 通过，0 跳过**，447.58 ms；使用内存假传输，不访问运行服务或真实患者；4 个源/测试输入前后指纹一致。不与 A3 的另一组 30 项混算。

新增证据：`source-manifest.json`、`native-safety-checks.json`、`request-source-01/`、`source-verification-01/`、`source-verification-02/`、`main-git-fsck.json`、`restored-candidate-tool-tests.json`。

独立审查还发现一个本轮未触发的恢复编排复用风险：异步写日志发生异常时，不能保证进入原收尾逻辑。本次日志/汇总齐备且指纹核对通过；冻结脚本不原位修改、不重跑已接受请求。后续新恢复批需先在新版本修正该异常收尾路径，再执行。

### 3.2 仍未恢复的范围

前端依赖和共享 Maven 缓存仍存在表中占位问题；主 `.git` 目录的额外 77 个占位属于子模块、reflog、示例 hook、旧 index 副本等，未删除或覆盖。另有构建/容器/浏览器测试配置等未纳入本批白名单，仍须按拟构建范围恢复。不能以这批主源码恢复代替完整 M0-E、子模块恢复或全量构建。

## 4. 本轮未执行及下一出口

- 未启动 Docker。原业务栈静态配置声明自动重启，但实际容器及策略尚未核验；启动桌面引擎可能恢复已有应用及对外接口，因此已向用户确认“是否仅本地测试、无真实医院/仪器连接”。答复前不执行可能恢复未知业务的启动操作。
- 未创建或改动业务库，未连接设备，未登录或签发报告，未更新 HTTP 运行版。
- 未执行完整构建、完整业务回归、真实数据库迁移/并发验证；M0、M1 及报告技术 P0 均仍未关闭。
- 下一步按原计划：补齐拟验证范围精确依赖和剩余构建输入 → 盘点当前改动并取得同一源码回归 → 在确认本机引擎安全后建立独立 SIM 数据库 → 推进 M1 持久报告组解析及真实事务验证。没有绕回一般页面美化或另建平行计划。

现有源文件归档不等于数据库备份或固定可运行回退版本；尚未满足本地候选替换门禁。

本轮恢复记录/工具/验证证据的归档位置：`tmp/m0-recovery-20260911/freeze/recovery-evidence.tar.gz`，校验及限定范围见同目录 `integrity.json`。这是恢复操作证据归档，不是可运行回退包；未替换或删除既有 M1-A3 冻结证据。

## 5. 11:13～12:19 前端原依赖恢复完成及遗留回归

本节更新上述历史剩余状态，不覆盖早前请求接受或未完成证据。

- 固定清单仅含 15,592 个现有 `frontend/node_modules` 占位文件和原 `eslint.config.js`、`playwright.config.ts`、`Dockerfile.local-runtime`，共 15,595 项、121,451,776 字节；清单 SHA-256：`4960aa91310b82fe695aa90e3db7fbddb4c5b6961b71973a9733d6f524de7549`。
- 新恢复编排已处理异步输出、短写、JSON 本地性、结果收尾等边界，24 项工具模拟回归通过；原生错误清单/非法参数拒绝检查及 `inspect-02` 只读检查通过。这些不是 LIS 业务测试。
- 11:13:37 `download-02` 实际提交 15,595 个原文件恢复请求，全部 accepted、0 错误；没有重装或升级依赖、替换旧源码、修改云设置。接受请求当时不代表下载完成。
- 11:14:18 `verify-01` 仅 193 项已本地，15,402 项仍占位；原元数据及保护指纹稳定，结果如实为未完成，保留原记录。随后等待系统后台恢复，没有重复提交下载请求。
- **12:13:19 `verify-02`：15,595 项全部本地、原 inode/尺寸/mtime 稳定，逐项记录 SHA-256；7,702 项保护输入一致。** 独立审查重新核对全部恢复文件元数据与哈希、7 个工具输入和保护源码，均通过。
- 使用原项目 Vitest 配置补跑既有患者日志联动及兼容：6 项新候选组件测试、2 项既有组件测试、11 项本地化单元，19/19 通过、0 失败/跳过，耗时 12.78 秒。使用模拟服务，不连接实际患者或数据库；没有用依赖替代映射取得通过。
- 12:15:11 `verify-03` 再次确认全部恢复文件和保护源码稳定；与 `verify-02` 的 15,595 个 SHA-256 逐一相同。后续计划/台账文档为有意更新，不倒改原保护基线。
- 既有前端组件、6 项配套测试及限定验收记录 3 文件已本地提交 `1856cff46`；原 2/11 项兼容文件已在 HEAD，无需重复纳入。详见[实际回归与边界](patient-audit-scope-acceptance-2026-09-11.md)。

主要证据在 `tmp/m0-frontend-recovery-20260911/`：固定清单、`inspect-02`、`download-02`、3 次后验、`driver-and-collector-final.tap` 和 `frontend-tests-01.json`。未覆盖此前源码恢复归档。

本批32个操作证据文件已归档至 `tmp/frontend-recovery-freeze-20260911-dvsWyd/evidence.tar.gz`，完整逐文件回读一致，SHA-256 `1c842a20db9fd9b38bc93bb2f49938e9c4f4526b9b25f5b61a6416d0b59c933f`。这是恢复和限定测试证据，不是数据库备份或运行回退包。

本批定向格式核对发现 `.prettierignore` 的 `reports/` 会跳过同名业务源码目录；首次默认检查不计通过。随后仅对两个精确文件使用项目配置、`--ignore-path /dev/null` 实际格式化和检查通过，均 unchanged。全量格式忽略范围列入 M0-03，未擅自修改全局规则。真实患者查询组件还产生既有 `maxDate` 属性类型警告，列入 M3 浏览器兼容复核，未隐藏告警或宣称视觉验收通过。

剩余：共享 Maven 缓存/子模块及其他后端构建输入尚未完整恢复；本次不宣称完整依赖已齐、全量构建已过或 M0 已完成。Docker、隔离数据库、HTTP、报告 M1/P0 和真实医院验收仍未通过；没有启动引擎或更改运行版。
