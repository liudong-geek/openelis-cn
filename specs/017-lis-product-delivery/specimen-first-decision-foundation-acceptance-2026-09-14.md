# 逐实管首次验收决定：持久合同与只读基础限定验收

日期：2026-09-14。基线：`b6e7266e8`。继续唯一逐管验收主批。本轮是首次决定的持久合同与原查询接线，不是已经能在界面接受／拒收，也不是主流程交付完成。

## 产品含义与本批范围

- 一份申请可有多管同类型标本，首次决定必须属于实际管ID；不能把“未拒收”、已经签收或整申请清单勾选当作该管验收合格。
- 在原SampleItem下补充首次人工决定事实，不复制患者主档、不建立第二套标本生命周期。旧SampleItem继续拥有当前状态；旧拒收原因列混有字典与QA事件来源，本批不猜测、不迁移或回填历史记录。
- 首次事实包括原申请、患者、请求及实管ID、操作UUID、明确ACCEPTED／REJECTED、拒收原因来源／ID／版本／当时文案、服务端操作人及毫秒时间、决定前所见事实。一个操作UUID只属于一管，不作为多管批次号；每管只有一条首次记录。主键采用旧审计兼容的正Integer范围数值String＋有界序列，UUID单独唯一。
- 所见事实保留原微秒版本、采集／签收时间及该管分析／项目集合；严格JSON解码、明确schema、摘要一致和时间顺序验证。不接收未知字段、重复字段、标量强转、重复分析、超界ID或决定之后的“先前版本”。该摘要是内容一致性检查，不是电子签名或防管理员篡改证明。
- 原`current`在已有完整申请授权及同一只读REPEATABLE_READ事务后，按实管批量查询最小只读DTO。区分无首次记录、旧拒收、已有首次决定、需复核、无效记录。原归属或患者不一致不返回旧原因、操作UUID或署名。后续正常状态／结果版本推进不抹除首次历史；当前拒收／作废等仍在原实管字段中独立显示。
- **所有`currentAcceptanceVerified`始终false。** 本批不增加写入API、不开放验收按钮、不改变检验／报告准入。现有前端白名单不会传播新增`specimenDecisions`字段，因此没有用户界面交付结论。

## 持久化与审计边界

新增注解实体在原`persistence.xml`显式注册；只读DAO没有继承通用新增／修改／删除API。原实体生命周期回调拒绝修改／删除，083迁移定义一管／操作UUID唯一、正整数范围、合法决定／必填原因约束和五个原实体的RESTRICT外键，另定义拒绝UPDATE／DELETE／TRUNCATE的数据库触发器。不级联复制或删除；回退定义拒绝自动丢弃首次决定依据，需授权的保全数据迁移。

同一迁移登记唯一、明确启用历史的`reference_tables`项；已有禁用或歧义注册会中止，不静默启用。**这些是映射与迁移定义，不是已执行SQL或已验证审计写入。** 本轮无数据库连接。实际新增时仍须在同一业务事务核对唯一启用的审计注册，调用既有审计并使失败整体回滚。数据库所有者仍能改变权限／触发器，本轮不声称抗管理员篡改。

拒收原因当前只验证命名空间与快照格式；真实字典／QA事件存在性、活动状态及是否为该场景允许的拒收原因，必须由下一片首次写服务查询主数据验证。不能将全部QA事件默认作为有效拒收原因，医院临床规则也没有在本批自行确定。

## 验证证据

目录：`tmp/specimen-decision-20260914-oDxP3e/`。

- 最终`check2`：**298/298，16套，0失败／跳过，10.756秒**。其中25项新增／扩展覆盖首次决定合同、独立真实只读投影、DAO参数绑定、原current接线及权限隔离、实际Hibernate映射回调和迁移结构；原相关273项同轮重跑。不同历史批次不累加。
- 48份Java与4份资源共52份明确候选，Java21限定编译；121个源码／依赖输入测试前后稳定。真实Hibernate SessionFactory包含新实体，检查数值主键、原版本字段、唯一键、无关联级联及真实注册回调，拒绝连接provider确认零连接请求。
- 原current测试为真实EntryCurrentStateReader＋mock子reader的接线及拒权零调用；新子reader另有真实投影测试。不是完整HTTP／数据库GET联测。DAO测试只证明HQL与绑定合同，不证明真实查询／并发锁。
- 初轮`check1`的292项中1项失败是新ORM测试运行classpath缺少既有本地Hibernate日志依赖；补齐原模型测试已经验证的本地依赖后全绿，没有联网下载。SLF4J未配置输出实现的警告保留，不以此宣称完整日志部署已验。
- 独立复核要求补上未来所见版本／原因版本负例，已修复并在最终候选通过。最终源代码、映射／回调、跨患者隐藏及受测来源复核无本批未闭合问题。
- 11份Java使用项目Eclipse配置局部格式，差量检查通过；不是完整Maven／Spotless门禁。083仅作结构断言，未执行迁移／FK／触发器／审计／回退。
- 14份源测资源＋本验收文档在提交前分别归档精确候选与工作树；原已跟踪文件留基线备份。`base.xml`只提交HEAD＋083，原080未提交差量保留；不全量暂存、不推送。准确清单及SHA见本目录`seal.json`、`commit-integrity.json`。

## 下一唯一操作与未完成事项

**直接实现同一记录的首次写入，不再扩展前置任务：** 复用原申请及实管锁，核验真实身份／岗位、逐管所见版本与主数据；同一事务保存明确决定、必要原标本拒收状态和既有审计。一管拒收不能让其他合格管因旧整单前置而无法独立处理。相同操作须可靠核实重放，冲突与未知不得自动重发。随后接原工作区选管、原因、确认、保存后独立读回，并补相应失败回归。

首次写接口、真实原因资格、岗位授权、重复请求／并发、SQL迁移／审计及中文界面仍未完成。未启动Docker、数据库、18080、设备或打印，未操作真实患者或签发报告；未更新运行版。源码归档不等于DB／配置备份或可运行回退。隔离库、完整类型／构建／发布门禁、报告P0、医院主数据／临床规则／厂商／打印和现场UAT仍待，M0～M7／G0～G11不增加正式完成结论。
