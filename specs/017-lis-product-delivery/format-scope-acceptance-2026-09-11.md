# M0-03：报告源码格式检查范围修复

日期：2026-09-11。承接[前端恢复记录](environment-recovery-2026-09-11.md)和 v3 工程门禁，不新增业务功能或扩大报告签发权限。

## 问题与最小修复

原 `frontend/.prettierignore` 的 `reports/` 不仅排除根级生成目录，还跳过 `src/components/reports`。因此格式检查可能显示成功，却没有实际检查报告业务源码。

仅把规则锚定为 `/reports/`，其余生成目录、第三方依赖、认证产物及压缩文件排除规则不变。新增 `FormattingScope.test.js`，直接调用项目原 Prettier API，无 mock、不创建或读取模拟患者/认证文件。

实际检查新纳入的报告目录后，发现两份原有 study 组件不符合格式要求；仅机械折行同一 `getFromOpenElisServer` 调用并加尾逗号，未改 URL、参数或控制流。两文件修改前均与 HEAD 一致；去除位置、注释等解析器元数据后，前后 AST 相同。

## 实际回归

| 检查 | 实际结果 | 不能代表什么 |
| --- | --- | --- |
| 旧规则红轮 | 14 项中 3 失败、11 通过；3 个真实报告源码被错误忽略 | 不是运行器启动失败 |
| 新规则绿轮 | 14/14 通过 | 不是医疗业务测试 |
| 最终限定回归 | 14 项范围保护＋原19项患者日志组件/本地化，33/33 通过，0失败/跳过；17.84秒 | 不是完整单元套件、后台或浏览器验收 |
| 报告目录格式检查 | 首轮暴露2文件问题；机械格式化后报告目录及新增测试实际检查通过 | 未检查全部前端其他目录 |
| 前端构建 | 首轮12.61秒；最终源树输出独立 `final-dist`，20.78秒成功，2636模块 | 不是启动、部署或全项目通过 |
| 静态中文键检查 | 原脚本覆盖1836个引用键，两份中文包通过 | 不覆盖所有动态键、所有页面或用词业务适配 |
| 原类型基线门禁 | 返回通过，仍有436条诊断、基线1209，未增加文件/诊断类别数量 | 不是类型错误清零，不能作为完整类型检查通过 |
| 原完整类型命令 | `npm run typecheck` 实际退出2，44个文件共436条诊断 | 此门禁失败；基线未增加不能替代原命令通过，不能放行运行版 |

最终构建/回归前后，1,060个前端源码、公开资源及7个精确构建/格式配置文件哈希一致。这里没有声称校验所有依赖、后端、数据库或全部历史材料。

## 证据与剩余问题

证据根目录：`tmp/frontend-gates-20260911-oeWU0p/`。

- 红/绿回归：`format-scope-red.json`、`format-scope-green.json`；初次报告格式失败：`reports-format-check.log`；修正后：`reports-format-green.log`。
- 最终33项：`final-tests.json`，SHA-256 `4d6c4c5963940e7417c4549b165ad2655aaa81a00762c2255ba0dcc0dd0717e9`；含警告的完整输出保存在 `final-tests.log`。
- 最终构建：`final-build.log`，SHA-256 `32ea0da940db50649bd66923278b7db8bb8b980666a40432b926a9a51cf06156`；`final-dist/index.html` SHA-256 `3feea5e4261ae3c15ef43a48dda508e2039d5a6be3771bbc7ba1a09e226f2d2d`，不是整个构建目录的摘要。
- 代码输入：`final-source-inputs.json`、`final-input-check.json`；两处纯格式变更：`format-ast-equivalence.json`。
- 原类型失败：`final-typecheck.log`、`final-type-summary.json`，保留退出2、436条诊断和44个文件的事实；没有调整历史基线。

没有屏蔽已有患者日期控件 `maxDate` 警告、超大前端分块或构建插件耗时告警；它们分别进入 M3 浏览器兼容和 M6 性能评估，不因构建成功自动关闭。

原类型基线脚本还需独立补充退出状态/非逐文件错误的失败保护。此项来自源码静态复核，本轮未制造运行器故障或将其记为已修复；不改历史诊断基线来取得通过。完整类型、全量回归、后端环境、数据库和运行版门禁继续分别记录。

独立差量审查确认本批规则与正反断言、两处换行没有扩大业务或权限。版本检查点限定为规则、14项测试、两处机械格式和本记录；提交号以唯一台账为准。

## 14:18 提交前收口复核

本轮承接用户关于代码保存和进度不清的反馈，不新增功能。对同一候选重新执行上述4份测试，33/33通过、0失败/跳过，耗时11.54秒；不是新增33项业务验收。限定格式化3份源码均为 unchanged，报告目录及新增测试格式检查通过。再次核对1,060份输入，与上述已成功构建的候选完全一致；本轮未重复完整构建、类型命令或启动服务。

提交前证据：`tmp/format-scope-checkpoint-20260911-Za7oOa/`，包括 `precommit-tests.json`（SHA-256 `fe651cd3fb240718da73654d26343f3fba7b1d81caa8a74f6b4ebd26659fa6ee`）、`precommit-tests.log`、`candidate-input-check.json`、`scoped-format.log` 和 `reports-format-check.log`。独立复核确认冻结输入、原红绿回归、构建及原类型失败事实一致。

另对本轮收口前的主前后端源码、脚本、指定配置及进度文档重新备份，共7,710个文件；先确认非云占位，再校验备份前后哈希与完整归档回读。归档为同目录 `current-source-before.tar.gz`，SHA-256 `200064065b52ed4f9486b448e1c28f0e7c98dde6dac1605daa9142eb0ddeef4d`，逐文件清单见 `source-backup-integrity.json`。这是同机源码备份，排除数据库、依赖、Git历史、子模块及未列入文档；不是运行回退包或异机备份。本节及随后进度记录是备份后的明确文档更新，另随本地提交保存。

未写入已配置的 `frontend/dist`，未启停 Docker、访问真实患者、签发报告、连接设备、推送代码或更新 HTTP 运行版。独立构建输出不是已验证回退版本，不能直接替换运行环境。
