# 已有页面改造批次验收

日期：2026-09-23。承接 `iteration-2026-09-23.md`。本批只交付申请列表、结果工作台布局和必要的前端工程修复，关联 B-01 邻接入口、B-02/C-02 的局部验收；14 个总需求尚无整项关闭。

## 具体改变

- 申请列表：压缩页头、查询和行内操作的占用；保留七列及状态说明；中窄屏查询换行，日期尾部为日历图标留出空间。仅表格外层横向滚动，避免整页溢出和双层滚动。
- 结果工作台：当前患者和标本置于录入区域前，队列与明细布局更紧凑；保留检验项目、参考范围、结果、状态、操作以及全部受阻依据。五列结果明细在窄屏通过表内横滚访问。
- 既有 JS/TS 类型合同补齐，没有增大类型错误基线。连接测试缺失响应进入错误状态；面包屑跳转前重检链接。没有更改结果保存、审核、验收或报告服务。
- 校准四份过时测试：导航域及旧入口上下文、独立展开和“全部退回”的现有语义；保留行为断言，没有新增跳过。

## 工程验收及时间线

1. 完整 `npm run check:delivery` 通过：中文关键文案 1,887 项检查通过，290 个测试文件通过，3,584 条测试通过，13 条原有跳过。类型回归门禁通过，仍有 401 条历史诊断；这是不增加错误的门禁，不能称为完整类型检查零错误。原 1,209 条基线未修改。
2. 完整门禁结束后，真实填写日期暴露 1200px 日期文本与图标重叠；仅补申请页 CSS：1279px 换行断点、日期右侧 36px 留白。随后独立生产构建成功，真实页面复测通过。没有把两轮构建混成同一产物。
3. 最终样式为 `index-JKVUQGMt.css`，SHA-256：`e2daaeb6069e24c9bf023bd3d680a1779960827252db37804f19685d2c9d5a0e`。最终申请 SCSS SHA-256：`eee95bbefb49b95ae97ee489748585f30563d2c74f126d77f67e9537174431e9`。
4. 改动文件格式化及 `git diff --check` 通过；独立审查 15 个前端文件无提交阻塞。Maven Spotless 指定本批前端范围执行成功；后端没有修改，没有以此冒称后端编译或测试通过。

完整测试前一次红色运行的五个失败、随后的修复及绿色日志均保留。13 条跳过来自三份与原 HEAD 逐字节一致的旧测试文件。构建仍有大文件体积警告，性能优化继续记录。

## 真实页面操作

以实际生产构建连接本机既有后端，使用明确的模拟记录进行只读验收：

| 场景                   | 实际观察                                                                              |
| ---------------------- | ------------------------------------------------------------------------------------- |
| 申请搜索、无匹配、重置 | 指定申请得到一条；无匹配显示空结果及说明；重置恢复列表                                |
| 分页                   | 每页 25 条时下一页只含最后一条，返回上一页恢复正确申请                                |
| 查看原申请             | 进入原有详情，申请编号与列表一致、患者匹配，表单处于只读状态                          |
| 结果切管               | 18 项标本切换到 1 项标本，患者、标本和项目同时切换；键盘 Tab/Enter 可换管，再返回原管 |
| 受阻依据               | 原管 18 条项目警告保留，未因压缩布局消失，也未放开录入                                |
| 结果响应布局           | 实测 1056、1024、390px；body 宽等于视口，五列保留，两个内层包装无横滚，外层负责横滚   |
| 最后日期修复           | 1200px 实际填写日期，框宽约 438.90px，右侧 36px 留白，页面无横向溢出                  |

申请页早期三条被误命名为 finalOrder 的尺寸记录实际全是 1280px，已改标为历史无效响应尺寸证据，不据此宣称三个宽度通过。最终部署尺寸与截图另记部署验收文件。

## 边界与下一步

当前模拟结果均因原验收依据变化或未签收受阻；本批不伪造可录入数据绕过保护。因此没有浏览器实测结果写入、草稿冲突、签名、打印或完整跨岗位链路。全量浏览器 E2E、后端/数据库验收、医院制度和 HIS/仪器现场联调仍待后续。直接浏览器只读检查不替代全量 E2E；本批推送到用户功能分支，不合并默认分支。

详情页的“新建申请”标题与只读已有申请语境不一致、日期控件月份英文、首页计数单位与岗位队列不一致，分别记入 D-03、D-03、A-02，不能用此批布局掩盖。下一实现批次是 A-02/A-03。

原目录的 61 个净工作变化和 165 个新增文件保全后独立处理，不混入这批经核对的部署基准。正式部署记录必须注明源码提交、远程读回、运行镜像、访问入口、前后截图和回退方式；本文件创建时尚未替换 18080 前端。
