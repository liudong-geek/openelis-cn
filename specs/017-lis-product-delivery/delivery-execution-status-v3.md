# 交付执行台账 v3

更新：2026-09-14，检验前验收清单安全基础检查点。唯一执行计划：[交付计划v3](delivery-execution-plan-v3.md)。简明入口：[当前交付进度](../../DELIVERY-STATUS.md)。此处记录当前任务状态，旧验收记录保留历史事实；没有新证据不得改为完成。

状态：待开始／进行中／开发已验／本地已部署／现场已验／阻断。任务完成不代表父里程碑完成。

## 当前检查点：旧整申请验收清单受控写入与配置投影（2026-09-14）

业务提交 `8cb68d61c`，接续逐管签收，修复未真实签收仍能勾选完成的问题。既有QA Controller/Service和最小前置DAO经专项验证纳入本批，没有夹带旧整单前端、OrderSearch或其他菜单积压。

- 实际HTTP请求贯穿服务事务，在目标/字典查询前检查clean上下文及真实身份；双标识、严格布尔/整数、逐管关联/签收/主数据/异常、完整项目权限与原存放义务均核对。保存后及beforeCommit复核患者/编号/域/登记时间、成员集合、配置与权限；标量核对不refresh/clear待写上下文。
- 旧QA行使用副本更新；相同勾选不沿用旧署名，草稿清除完成归属。GET/POST区分storedChecklistComplete与currentAcceptanceVerified=false，新配置缺项不再返回矛盾完成；仍不是逐管持久验收证明，历史列表布尔不成为新流程放行条件。
- 最终 `final2` **184/184，8套，0失败/跳过，12.18秒**，Java21精确编译及17源测局部Eclipse格式通过。实际MVC→服务/actor/Spring事务＋SIM资源；严格输入、查询中换同账号会话、dirty零目标/字典查询、后续撤权/变更/外层回滚均含回归。无真实SQL/Hibernate、浏览器或完整发布门禁结论。
- 独立终审通过；26候选/当前输入52次哈希核对无差异。18文件（17源测＋验收）归档逐文件回读，SHA-256 `e542197c8b75a2d6c8685149b15b0190f78fe8cad8af08cb3341d087ce285f68`；本地提交，未推送，其他历史修改保留。
- 失败历史保留：旧服务红轮110项中51失败；green2缺本地jsonpath依赖8项未通过；green3旧DAO调用次数断言1失败，调整为保存前/末尾复核后重测。不同轮次不累加为全系统通过。
- **下一唯一主批：同一current工作区的明确验收与保存后读回，先绑定本次管集合/事实及清单配置至持久验收记录。** 不只接旧勾选按钮、不凭签收或allRequiredVerified放行检验、不新增菜单；继续报告P0优先级。
- **未闭合：** 逐管验收版本/不可变事件、撤销原因、跨客户端长期未知、真实DB/审计/外部并发、完整岗位授权、旧UI/全页面及报告P0。环境/分装场景需独立合同，不能猜测合格。本批未启动Docker/业务库/设备/打印/签发、未更新18080；完整候选门禁、DB/配置备份及固定运行回退仍需恢复，M0～M7未关闭。

详见[限定验收](qa-completion-guard-acceptance-2026-09-14.md)，证据 `tmp/qa-completion-20260914-3ieZkP/`。以下为历史检查点，接续以上方为准。

### 上一检查点：同一申请明确选管、确认签收及逐管核对（2026-09-14）

业务提交 `d03a73e9f`。接入既有专用后台，不新增菜单、不写旧整单。当前核对区明确选管、展示冻结的确认信息、一次签收、再读逐管事实；采集原始 UTC 微秒保留，签收时间取确认页创建时的本机时间并以实验室中文时区展示，后台仍作最终校验。

- 已签收/异常管不重写；坏旧管不影响其他正常管。签收与采集、标签、旧保存/加载及查询互斥；会话暂停、换账号、重查、迟到响应均不能复活旧确认。
- 未知保护只持久化匿名核对码及带盐摘要；刷新不重发，全部所选管的身份/采集/目标签收时间一致才消解。成功查询后同步更新Provider，解除旧提示并允许明确处理剩余管；不清其他操作保护，不自动QA或报告。
- 最终HEAD `6b494988b`＋15份源/测/语言候选 `candidate-v6`：**261/261、13套、0失败/跳过**；完整前端构建**1分18秒**、11文件限定格式通过。1440/768真实组件纯SIM浏览器**2/2**，并复核清晰管码、中文时区、管数和页面/卡片无横向溢出。未访问真实业务后台。
- 独立审查发现“坏旧管误挡正常管”和“消解后提示未刷新”，修正并补回归；截图发现确认管码浅灰，改为高对比只读管码后复验。早轮计时/查询预期/原生隐藏复选框目标及沙箱失败均保留，不当作完整业务或后台失败。
- 16文件（15源测语言＋验收）源码归档逐文件回读、候选/暂存/提交/工作树指纹通过。SHA-256 `58a7ace152420229863ae12fe9c9b02f4415b7691cd2ba593a2c0a40569298a3`。历史未提交修改保留，没有推送；临时纯SIM服务已停止，没有更新18080。
- **下一唯一主批：同一申请已签收标本的检验前验收。** 先审查现有QA的整申请契约，按真实逐管签收、标本/项目主数据、异常和岗位权限收口明确验收与读回；不将整申请勾选直接当每管合格，不新增菜单、不恢复旧整单写入。继续保留报告P0优先级。
- **未闭合：** 真实数据库/Hibernate/审计/并发、完整认证链、多岗位、长期未知与跨客户端、QA、报告P0、全页面与正式UAT。完整后台依赖/类型历史问题、DB/配置备份和固定运行回退仍未恢复；未启Docker/真实患者/设备/打印/签发，不关闭AC09/AC14或M0～M7。

详见[本批限定验收](specimen-receipt-ui-acceptance-2026-09-14.md)，证据 `tmp/receipt-ui-20260914-FDnrGx/`。以下均为历史检查点，下一步仅以上方为准。

### 上一检查点：逐实管签收专用后台（2026-09-14）

业务提交 `e2121a8d5`，接续同一采集后处理主批。收敛到明确的逐管签收，不将申请登记时间、标签生成或整申请QA勾选误当签收；未夹带历史QA/整单或全局认证修改。

- 新POST仅写明确所选已采集实管的receivedDate，保留患者、申请、采集、检验项目、QA、库位及报告。全批核对真实身份/会话、患者及四项业务ID关系、状态、原始采集时间、版本、活动主数据和项目权限，校验全部通过才开始写；原签收时间不可覆盖，同值重放保守限定当前可签收流程。
- 稳定顺序锁定并刷新；写前拒绝dirty和默认只读Session，不清空他人上下文。通过现有EMF获取Spring事务绑定共享EM，无需不存在的EntityManager bean。完整detached copy保留lastupdated微秒，旧审计看到原值，提交前复核；失败/断连不提供假成功或完整回滚声明。
- 局部64KiB/100管、严格UTF-8/JSON、重复键/尾文档/未知字段/异常日期拦截，无全局converter变更。中文明确区分签收与检验前验收；无receivedBy独立字段，不伪造落库归属。
- 最终独立HEAD `b49fb180d`＋9文件，`final3` **96/96，13.074秒**，Java21编译、输入/依赖指纹稳定。真实MVC、ActorGuard、Spring事务和局部容器装配配合SIM资源；旧audited service源码的版本/旧值/审计失败验证已补。DAO仅契约及共享代理，不是SQL/Hibernate真实写入。
- 缺版本真实红轮76项中1失败；移除状态/前置权限的反向验证95项中5失败。初期夹具、缺依赖、Mockito泛型、共享代理事务夹具失败保留。首次局部格式工具因云占位依赖终止143，不认其stdout通过；使用已有本地依赖按项目Eclipse配置格式后再跑最终回归。不是完整Maven/Spotless门禁。
- 独立只读终审通过；10文件（9源测＋验收）源码归档逐文件回读、候选/暂存/提交/工作树指纹通过。SHA-256 `79bcb06acc33fcdd69a96ba921b7f6f3b7ef6c8b6a85797ac4a4f936bc003916`。没有全量暂存、推送；其他历史修改保留。
- **下一唯一主批：同一current工作区的明确选管→签收确认→逐管current再核对。** 前端保留原始collectionDate UTC微秒串，冻结本次管身份及会话，未知不得自动重试、解除防重或重开单；不新增菜单，不进入旧整单/QA。签收后再独立收口检验前验收，不能全勾选自动通过。
- **未闭合：** 前端衔接/浏览器、真实SQL/Hibernate写入/并发/审计、完整认证过滤链/多岗位、患者关联与主数据外部竞争、长期未知权威恢复/跨客户端、报告P0及全页面未验。完整Maven/Spotless、全量类型历史问题、DB/配置备份和固定运行回退仍缺，不部署18080。未启动Docker、业务库、未知设备，不操作真实患者、打印或签发；本批不改变定时任务。AC09/AC14、M0～M7不关闭。

详见[当时限定验收](specimen-receipt-write-acceptance-2026-09-14.md)，证据 `tmp/specimen-receipt-20260914-UwlkpC/`。下一步以上方当前检查点为准。

### 上一检查点：已采集实管到标签处理（2026-09-14）

业务提交 `c24b52f7f`。在原current核对区域接入既有标签面板，明确选择后生成临床已采集实管标签；不新增菜单、不进入旧整单页面、不回填患者或保存整单。

- Provider私有当前快照及实际管ID/持久条码后缀绑定操作；部分已采集、待采集和取消记录保留，无实管/日期、无效主数据、作废/拒收或环境申请不伪造放行。查询/采集/旧保存与标签共享操作互斥，会话校验/错误、换账号、快照替换及卸载使旧操作/PDF失效。
- 一次明确POST，冻结请求及期望条码，响应限长/60秒超时/不重定向；完整核对数量与PDF包络。匿名标签页marker在可预览文件建立后才确认消费；未知、坏应答、401/403/400/409、预览建立失败不自动解除，刷新重查也不重发。成功文件重复预览零新POST，不等于出纸、贴签、签收或验收。
- 最终HEAD `9101f70ce`＋14文件候选 `candidate-v5` **205/205** 回归，0失败/跳过；前端完整构建 **1分23秒**。10份源测/局部样式限定格式通过；工作树早轮84项不累计为全系统成绩。
- 候选真实Provider/Carbon/Intl组件SIM浏览器1440/768 **2/2，20.4秒**；禁止实际业务网络，验证生成/预览/会话/未知刷新、无旧写和整页溢出。人工截图发现窄屏80像素右留白吞掉数字，新断言在旧样式下1失败，修复后复测并看图通过；不将可点击等同视觉合格。临时18788已停止。
- 独立只读复核通过，审查发现的清标记过早问题已修复并补真实组件回归。15文件提交（14源测语言＋验收），候选/提交/工作树指纹一致；备份逐文件回读SHA-256 `9309dfa98a2a910a40fa9ec26e93fdf72cc398992421d18be24ba47ee1db9f35`。Context只取标签差量，旧App/OrderLabel/历史CSS不夹带，其余历史工作保留。无推送。
- **下一唯一主批：同一申请已采集实管的签收/验收。** 先审查收敛既有专用写入和逐管状态回读，再接明确操作，不恢复旧整单保存、不自动完成。获得安全隔离库运行条件后优先验证首次保存→部分采集→恢复→标签/签收。
- **未闭合：** 长期未知结果权威恢复、跨客户端/关闭标签页后持久防重、旧标签GET写旁路、真实数据库/认证/并发撤权/审计、中文PDF及实际打印、报告P0。旧App不提供全部刷新开始代数，本批不宣称全站会话合同已完成。完整Maven/Spotless、历史全量类型436条、DB/配置备份和固定运行回退未齐，不更新18080；没有重新启动Docker、业务库、真实患者、设备、签发或打印，不修改定时任务。AC09/AC14与所有父里程碑不关闭。

详见[本批限定验收](recovered-labels-ui-acceptance-2026-09-14.md)，证据 `tmp/label-ui-20260914-NCJ6iA/`。以下历史“下一批”不覆盖本段。

### 上一检查点：标本标签生成操作人及事务前置（2026-09-14）

业务提交 `e0aae3a15`。继续已核实采集实管的后续处理主批；确认原现代标签生成包/面板及查询依赖未进入HEAD，旧页面仍走整单加载/保存，因此先冻结后台最小闭包，不把直接增加跳转当作流程完成。

- 实际入口与生成事务共享绑定的操作人/会话/认证/专业组；允许同会话包装，拒绝同账号换会话。真实交互认证、活动账号、当前标签模块及角色状态复核，渲染后、写入后和外层提交前保持检查。
- SAML/OAuth按既有IdP authority映射角色模块，不要求本地密码账号/登记岗位；本地及SSO停用角色不能保留旧打印权限。生成失败、SIM撤权、审计或提交失败不返回PDF成功，模拟资源回滚计数及审计；生成数量不是实物打印/贴签证明。
- `java-green5` **76/76，10.635秒**；20份候选生产/测试显式Java21编译，5份兼容源码来自HEAD，Jar/输入指纹稳定。反向移除实际服务绑定/复核后9项失败，改回本地角色处理SSO后2项失败。真实MVC＋Spring事务代理，业务资源仍为SIM；无SQL、真实数据库、真实认证过滤链或浏览器联测。
- 独立只读终审通过。21文件（20源/测＋验收）提交与候选/实际工作树指纹一致，源码归档回读通过，SHA-256 `a6fcdf21afac5df2b095c4afe6a232b31627bf600d08f5c50d0d871c423c7a9e`。未夹带字体、旧页面或整单保存历史差量，无全量暂存、推送或部署。
- 并行只读运行核对：01:05时18080无监听、Docker desktop-linux socket不存在；`dataexport/pom.xml`和旧WAR仍为云占位。已有test profile的Testcontainers可用于新的隔离数据库专项，但通用测试配置模拟了部分服务/审计；旧Compose固定资源和自动选库夹具不能直接当作隔离环境。启动Docker是否恢复真实连接已向用户提问，尚待答复。
- **下一唯一主批：同一申请已核实实管→既有标签面板的明确操作。** 先收口会话变化/未知结果防重，核对当前事实与实际管ID/条码，不恢复旧整单保存、不自动贴签确认或打印、不新增菜单。补真实组件多视口模拟及错误/返回恢复交互；获准隔离库后优先验证首次保存→部分采集→恢复→后续处理。
- **未闭合：** 真实库并发及一级缓存中的账号/角色撤权可见性、持久审计/去重/断连、旧标签GET写旁路、中文PDF和实际打印未验；本批不宣称即时外部撤权或完整标签功能已交付。前端尚未衔接，完整Maven/Spotless、历史全量类型436条、DB/配置备份及固定运行回退仍待；18080未更新。AC09/AC14、报告P0及M0～M7父里程碑不关闭。
- 初期依赖/编译与Mockito final record夹具失败保留，不当业务红证据。未运行Docker、业务数据库、真实患者写入、设备、签发或打印，不修改定时任务。

详见[本批限定验收](label-generation-actor-acceptance-2026-09-14.md)，证据 `tmp/label-handoff-20260914-ZzBGVF/`。以下历史“下一批”不覆盖本段。

### 上一检查点：采集明确拒绝后保留填写并重新核对（2026-09-14）

本地业务提交 `9c229153d`，执行同一采集失败处置主批。没有新菜单、数据库迁移或平行持久回执模型。

- 真正采集事务完整回滚且无活动外层事务时，对允许的校验失败返回绑定本次请求标识及原始正文摘要的明确拒绝。未代理、提交/回滚失败、身份变化、超时迟到、错标识/摘要及旧普通错误不能解除保护；不以400/409猜测未保存。
- 原表单保留输入并提供“重新核对，保留填写内容”；再次读回、明确确认及重新勾选后才能修正保存。变化的逐管计划不沿用旧填写，同一私有快照不能重试，不改患者资料、不重开单。浅色警告和蓝色操作按钮有明确间距，最终双视口截图已人工复核。
- 后台 `java-green2` **130/130，9.680秒**；真实Spring代理/MVC＋SIM资源，精确Java21编译和输入/依赖稳定。7份最终Java候选指纹与已测版本一致。移除实际服务观察调用后130项中2失败，验证确实接入业务入口，不是只测helper。
- 独立HEAD `2a0069ed0`＋18份源/测/语言 `candidate-v3`：前端八套 **160/160，21.062秒**，0失败/跳过；完整前端构建 **41.21秒**，7份指定前端源/测格式及diff-check通过。当前工作树和早轮160项成绩不累加为全系统验收。
- 最终SIM浏览器1440/768 **2/2，13.8秒**：拒绝→保留→重新核对/确认/选择→新标识保存→current核实，以及503未知→刷新→零新POST均通过；无业务网络漏出、页面脚本异常或横向溢出。仅真实组件＋隔离内存接口，不是全开单页、Safari、真实数据库或全流程；18787临时服务已停止。
- 独立只读终审通过。19文件（18源/测/语言＋验收）提交逐字匹配候选；混有历史差量的Context、Controller、Service及语言只提交本批段。19份工作树源码归档逐条回读、提交和工作树指纹通过，SHA-256 `23e20fb83430c5f8f96e663ca13a9691218dd67cf0bde945c4894e3c3e66560b`。无全量暂存或推送，其余历史修改保留；只是同机源码存档，不是数据库/配置或固定运行回退。
- **下一唯一主批：同一申请中“已核实采集的实管→既有签收/贴签入口”衔接。** 先核对完整当前状态、真实管ID和岗位权限，再提供明确下一步；不自动跳转、签收或打印，不恢复旧整单保存，不扩张菜单或继续重复回执/摘要工具。获准隔离库后验证首次保存→部分采集→再次恢复→签收/贴签及失败处置。
- **未闭合：** 长期未知仍阻断；没有本次完整回滚证明不得清marker。关闭标签页/清站点存储/跨客户端、真实DB锁/并发/撤权/迁移/提交断连/持久审计、签收贴签联测及报告P0仍待。完整Maven/Spotless、历史全量类型436条、授权DB/配置备份与固定运行回退条件未齐，不部署18080。无Docker、业务库、真实患者、设备、签发或打印操作；AC09/AC14和M0～M7父里程碑均不关闭。
- 本批未修改定时任务；接续只认本段唯一下一主批。早轮传输有效红证据、临时复制顺序ENOENT及预期负向事务异常/既有构建警告保留，不把夹具问题当正式业务缺陷，不重复无变化重型失败。

详见[本批限定验收](collection-rejection-acceptance-2026-09-14.md)，证据 `tmp/collection-rejection-20260913-iR6j0j/`。以下历史检查点的“下一批”均不覆盖本段。

### 上一检查点：核对当前申请后登记剩余标本（2026-09-13）

本地提交 `f60066a54`，完成上一顶部要求的采集上下文、主数据名称、明确采用和专用保存接入。没有新建平行菜单或回执模型，也没有开放旧整单编辑。

- 同一current读取事务附带实际主数据名称/活动事实、实验室日期格式/时区/时间和已有知情同意；缺失/停用历史数据不伪造，未知同意不当成同意。这些是事实，后台保存仍重新验证权限与状态。
- 原面板明确确认后才显示可登记剩余管；历史已采集/取消管完整保留。日期、时间、人员、采集量按四列/两列布局，明确年月日/24小时格式和中文时区；不自动勾选、保存或跳转。私有快照固定申请与逐管身份，不接受页面伪造ID，不回填患者/旧整单表单、不清首单保护或误标整步完成。
- 专用POST不跟随重定向，响应限制1 MiB、超时不重试；ACK后必须逐管核实原计划/数量/时间/人员等当前事实。发送前仅存当前标签页的不透明码/盐/哈希，无患者或原始采集正文。未知后REQUESTED不是回滚证明，重查/刷新须匹配实际COLLECTED才消解；没有首单marker也恢复核对码，普通保存不能绕过。
- 后台 `java-green3` **146/146，14.246秒**，Java21精确编译及输入/依赖指纹稳定；独立HEAD `ef9ee6d07`＋19文件 `candidate-v3`前端八套 **140/140，24.5703秒**，0失败/跳过，完整前端构建 **34.48秒**。13份前端指定文件格式通过。不是完整Maven/Spotless或全量类型门禁。
- 真实候选Provider/面板/专用表单SIM浏览器1440/768两项 **10.5秒**通过：只选剩余管、未改原表单、未知后刷新不重复提交及整页无横向溢出；最终截图人工查看。浏览器接口全部内存模拟，CSP禁止业务网络，不是实际数据库联测、全开单页、Safari或全UI验收。临时18786已停止。
- 后台缺上下文红轮、实际Carbon复选框可访问名称及初始化GET断言、候选传输格式检查、浏览器沙箱启动失败如实留档并修正/授权后重跑；首轮缺模块未执行用例不冒称业务红轮。独立终审确认本范围无剩余提交阻断，早轮与最终成绩不累加。
- 20文件（19源/测/语言＋验收）本地提交逐字匹配最终候选。Context只纳入恢复链，语言仅各28键；20份实际工作树源码归档逐条回读、提交及工作树指纹通过，SHA-256 `2f6160a3c0631eeccd828f050ee917c9635177eeb0ca61f0361dad2fd93b4c2d`。保留其他历史修改，不全量暂存、不推送；仅同机源码存档，不是DB/配置备份或可运行回退。
- **下一唯一主批：在同一申请工作区收口采集明确失败/长期未知的可核实处置及采集后进入签收/贴签的状态衔接；不得恢复旧整单保存或新增平行菜单。** 获准隔离库时优先验证首次保存→刷新核对→部分采集→再次恢复及失败处置，再接结果/审核/报告。不能继续扩展本批摘要工具或把更多局部测试当作完整闭环。
- **未闭合：** 当前标签页保护不覆盖关闭标签页、清站点存储或跨客户端；真实数据库锁/并发取消、撤权、迁移回滚、断连和审计仍待。长期未知/明确拒绝缺可核实消解途径会继续阻断，不提供清除保护绕过按钮。完整Maven/Spotless、历史436条类型诊断、授权DB/配置备份与固定运行回退仍未齐备，未部署18080。无Docker、业务库、真实患者、设备、签发或打印操作；AC09/AC14、报告P0、M0～M7均不因本批关闭。
- 定时只读核实仍唯一 `lis`、每小时ACTIVE，读取同一v3及顶部检查点，无冲突。本轮未新建/暂停/改频率；不是开发必需，当前仍有安全工作可暂保留接续，只剩外部阻断时暂停，无变化不重复通知。

详见[本批限定验收](collection-resume-acceptance-2026-09-13.md)，证据 `tmp/collection-resume-20260913-cmsT6Z/`。以下历史检查点的“下一批”均不覆盖本段。

### 上一检查点：采集专用保存与逐管绑定（2026-09-13）

本地提交 `9452fdead`，接续上一批发现的采集恢复阻断；复用并收口现有 collection-only 写入，没有另建菜单、回执或普通整单保存模型。

- Controller在任何普通表单初始化之前分流，原HTTP请求进入已有身份门禁；只写采集字段、实际管和计划内分析，不改患者/送检方/申请资料。每管显式绑定请求ID，同类型多管及剩余管不靠类型/排序猜测。
- 全批先核对现有申请/请求/实管归属及状态、当前主数据、数量/单位/时间和项目权限，再写入；已保存展开计划为准，变化组合不能新增未申请或其他标本类型项目。第一管写入后失败、绑定失败、身份变化及提交前撤权由事务退出回滚。
- 修复已存在但未采集实管被当成只读重放的漏洞：真实采集仍完整校验并调用detached审计更新，不新增分析；已采集记录核对原绑定/时刻/实际分析后零改写，保留停用主数据对应的历史事实。作废实管条码序号不复用，重复显示排序不误作身份冲突。
- 独立候选 `candidate-v2`（HEAD `c81ccbcf2`＋12份源/测）`candidate-test2` **107/107，6.905秒**；当前树含3套历史兼容回归 `working-historical3` **143/143，6.396秒**，限定Java21编译及源码/依赖指纹通过。两者不累加为全系统成绩；SLF4J NOP及编译警告保留。
- 有效红证据：初始84项6失败，审查缺数量/缺实际分析92项2失败，旧实管校验107项1失败。早轮编译输入、SIM会话/事务资源及测试类路径缺依赖问题如实保留，补本地依赖后重跑，不计业务缺陷、不删断言绕过。独立终审确认限定候选无剩余提交阻断。
- 13文件（12源/测＋验收）与候选逐字一致；不夹带普通保存、两参数旧匹配和旧fulfill改造。16份工作树文件归档逐条回读，SHA-256 `11ab3c61d72fe71e90ab18f2f95ead311192230ba739c052d16a7fc2a337e21e`，提交后工作树指纹未变；3份历史兼容测试与其他历史差量保留未提交，无推送。
- **下一唯一主批：补齐同一current读取中的采集上下文/前置状态和有效主数据名称，再让用户明确确认采用，接入本次专用保存及逐管请求ID。** 保留已采集事实与剩余请求，未知结果重新核对、切会话清除操作资格，禁止旧整单GET/写入回退。当前DTO不是完整编辑表单，已有只读面板尚不解锁采集。获准隔离库后验证首次保存→刷新核对→部分采集→再次恢复及并发/撤权；不回到已完成的回执、只读展示或事务工具。
- **仅SIM内存事务与DAO证据，无SQL/真实数据库/实际审计表、完整认证过滤链或前后端联测。** 本批未改前端、无浏览器验收；真实锁/快照/并发取消、提交断连、持久审计与旧入口安全仍待。完整Maven/Spotless、历史全量类型436条及发布备份/固定回退条件未闭合，不部署。未启动Docker、业务库、HTTP，未动真实患者、设备、签发、打印或18080；源码归档不是DB/配置/运行回退。AC09/AC14、报告P0及M0～M7父里程碑均不关闭。
- 定时只读核实仍唯一lis、ACTIVE、每小时接续此任务并读取v3及最新顶部，无第二份开发计划。本轮未新建/改频率/启停；并非开发必需，当前尚有安全代码工作所以暂保留接续，仅余外部条件时应暂停，不重复无变化通知。

详见[本批限定验收](collection-write-acceptance-2026-09-13.md)，证据 `tmp/collection-write-20260913-emsEB2/`。下列检查点均为历史，不覆盖本段唯一下一步。

### 上一检查点：当前申请逐管核对界面（2026-09-13）

本地提交 `6ad167aab`，接续原恢复主批，将已完成的 current GET 接入现有“核对当前申请”面板；没有另起回执或菜单。

- 主动查询后验证原标识/患者/申请与完整逻辑管、实管及分析关联，区分当前待采集/已采集/已取消与首次历史回执。所有当前事实白名单投影，不透传能力指令；生日保留日期，事件限定UTC真实日历日期。
- 展示逐管状态、实管异常与物理状态编号，不将两个异常标志为否推断成正常。无效/缺管/409/会话失效明确停止展示，不清marker、不改草稿、不调用旧GET或写入。类型与物理状态名称尚待有效主数据映射，未称临床可用。
- 工作树最终六套件 **142/142，23.057秒**；独立HEAD `2e6f3542a`＋本批12文件 `candidate-v3` **142/142，23.297秒**，0失败/跳过；9份指定源/测格式通过，候选完整前端构建 **40.98秒**。审查新增边界48项中6失败，修复后终审通过；早轮新模块/格式及沙箱启动失败保留，不混算业务缺陷。
- 真实候选Provider＋面板＋Carbon/Intl的SIM浏览器1440/768两项 **8.7秒**通过，逐管展示/缺管和409重查/会话失效/原marker及草稿保留/零业务写入/整页不溢出已验；截图人工查看。仅局部面板，不是全开单页、Safari、200%缩放或全流程验收。临时18785已停止。
- 13文件（12源/测/语言＋验收）限定提交与候选逐字相符；Context只增14行左右的当前读取分流，原采集积压不夹带；语言仅本批27键。13份工作树归档逐条回读、暂存/提交及工作树指纹通过，SHA-256 `93476de7311e9907381a63b07a3adc4f9c9848d14402ea7bb598e1d4dd3ccb21`。历史差量保留，无推送。
- **新确认恢复阻断：受控候选的旧采集页面仍经整张申请saveOrder写入，且有实管时跳过剩余请求；current DTO不是完整可编辑表单。直接回填默认表单并解除保护可能丢待采集管或覆盖原申请资料。** 本批因此只读，不开放“采用/继续采集”；工作树现有collection-only保护尚未独立入候选，不能当作已交付。没有证据表明本批修改了真实患者。
- **下一唯一主批：沿用、验证并收口现有采集专用写入＋请求ID绑定链，禁止采集覆盖患者/申请资料；然后在同一current读取中补齐采集上下文/前置状态及有效主数据名称，经明确确认采用后恢复操作，禁用旧整单GET/写入回退。** 先覆盖剩余管、错管、重复、取消/作废/拒收、撤权/失败回滚，再获准隔离库验证保存→刷新核对→部分采集→再次恢复。不要回到已完成的回执/事务工具或扩展其他菜单。
- **未执行数据库、迁移、真实认证过滤链或前后端实际联测。** 完整Maven/Spotless与历史全量类型436条阻断未变，未重复重型空转、未记通过。无Docker、真实患者、签发、设备、打印或18080更新；源码归档不是DB/配置/固定运行回退。AC09/AC14、报告P0及全部父里程碑仍未关闭。
- 定时仍接续唯一v3最新顶部，本批未变更启停/频率或新建调度。下一采集收口仍有安全可推进工作；定时只负责接续，不决定范围或放行，无变化不重复通知。

详见[本批限定验收](entry-current-ui-acceptance-2026-09-13.md)，证据 `tmp/entry-current-ui-20260913-LwSP7T/`。

### 上一检查点：当前申请与完整逐管只读查询（2026-09-13）

本地提交 `53b2f53c5`，接续同一恢复主批，新增既有提交核对码的 current GET。不另起回执模型或菜单。

- 同一专用读取事务核对原提交者、原回执权限、患者/申请关联、全部当前逻辑请求及实管/分析。保留取消、作废、拒收、额外分析项目；权限覆盖原历史＋全部当前请求＋实际分析。额外管/分装明确为暂不支持，不能丢弃后报成功。
- 同类型多管、同项目多分析和独立实际数量保留；审查修正合法追加管同排序误拒绝及生日UTC日期偏移。成功/错误均no-store，底层异常无患者片段；读后及事务提交前操作人复核保留。
- 工作树 `green2` **141/141，7.478秒**；独立HEAD `86726fd81`＋本批7份源/测 `candidate-final1` **141/141，7.412秒**，输入稳定。未注册路由及两项领域问题有失败证据；移除权限检查3项失败、移除新入口边界2项失败。早轮编译输入/测试声明错误如实保留，不计业务缺陷。
- 独立终审未见阻断。8文件（7源/测＋验收）提交逐字匹配候选；Controller只新增13行current GET，历史采集差量不夹带。8文件工作树源码归档逐条回读与提交/工作树指纹通过，SHA-256 `ff6123c161e358785c467a411de2f421bc6c5bd8d9c3f42eeda37932b36cc464`。
- **仅SIM DAO/协调层、真实MVC分发和Spring/Hibernate连接生命周期，未执行SQL、真实数据库或认证过滤链。** SLF4J NOP警告保留；全Maven/Spotless及全量类型阻断未变，未重复无变化重型检查、不记通过。没有前端变更或浏览器验收，未运行Docker、真实患者、签发、设备、打印或替换18080；无推送。源码归档不是数据库/配置/运行回退。
- **下一主批：将现有current接口接入前端“核对当前申请”，完整校验响应/原标识/身份，展示逐管状态，经用户明确确认采用后才恢复适用操作。** 不用历史回执恢复，不静默清标记；无可处理管、取消/作废/拒收/额外管或未知状态不能默认放行。恢复编辑另待；真实迁移、保存→恢复→采集、并发及撤权依获准隔离库验证。AC09/AC14、报告P0和全部父里程碑未关闭。
- 定时任务只读核实仍唯一 `lis`、ACTIVE、每小时接续当前任务，读取最新v3与台账，无另一计划。本轮不改频率/启停，不新建任务。当前可安全推进开发，建议暂保留；定时不是必需，若仅剩外部条件应暂停而非重复空转。

详见[本批限定验收](entry-current-read-acceptance-2026-09-13.md)，证据 `tmp/entry-current-read-20260913-NuDPwT/`。

### 上一检查点：保存核对入口的独立读取事务（2026-09-13）

按用户要求继续最新计划并评估定时必要性，完成当前恢复主批的事务前置，本地提交 `9d3b6cca7`。不再绕回首次开单或另建回执模型。

- 既有 `recover` 明确使用专用命名管理器、REQUIRES_NEW、REPEATABLE_READ、只读及受检异常回滚；新 Session 在创建前指定 HOLD，以同一原工厂/数据源为资源键。默认工厂、provider、事务管理器及 `submit` 不改。
- 真正外层事务挂起/恢复；仅预绑定而无活动事务的旧上下文直接拒绝，不清除/关闭他人缓存及待保存修改。创建时失败释放新 Session，开始/提交失败不进入业务或返回成功。
- 新19项＋既有回执/身份70项：当前树 `green3` **89/89（4.193秒）**，独立HEAD候选 `candidate-final1` **89/89（7.466秒）**，输入稳定、退出0。撤去入口边界后3项失败；只去掉受检回滚后1项明确检测到错commit。早轮两种测试假设错误已纠正，不计真实缺陷。
- 独立审查未见新增阻断。5文件提交（4份源码/测试＋验收），四份逐字匹配已测候选；归档、暂存、提交、工作树内容一致。归档SHA-256 `2e50d527dc69cc415beb208ffce97ed0dae171007c64c4e09b084a396d7e9b3a`，`tmp/recovery-transaction-20260913-4vjim9/` 保留证据。历史积压保留，没有全量暂存或推送。
- **限定真实框架＋SIM连接生命周期，不是数据库验证。** 无SQL、数据库、Docker或实际前端验收；驱动只读失败、开始失败后连接池复位、真实PostgreSQL快照/并发/撤权仍待。当前管状态和解除保护未实现，不关闭AC09/AC14或报告P0/任何父里程碑。
- **下一主批仍为当前申请与完整逐管读回＋明确恢复。** 复用现有事务边界，读取当前患者/申请、全部逻辑管（含取消）、物理管（含作废）、实际检验项目；结合原提交者与完整当前权限验证，不允许过滤缺项或拼接两个旧GET。前端明确核对采用后才能解除保护；真实保存→采集与并发效果依获准隔离库验证。不继续扩展本次专用事务工具。
- 本地18080未启动或替换，无真实患者/签发/设备/打印变更。全构建/格式/类型、数据库备份与固定可运行回退未闭合，不放行新候选。
- 只读核实唯一 `lis` 每小时接续同一任务，指向最新v3及顶部检查点；与计划无冲突，本轮未新建/改频率/暂停。当前仍有可安全推进工作，建议暂保留；定时不是必须，仅剩外部条件时应暂停而非空转。

详见[本批限定验收](entry-recovery-transaction-acceptance-2026-09-13.md)。

### 上一检查点：采集关联身份校验与一致读前置阻断（2026-09-13）

按用户“按最新计划执行，并评估定时是否必要”接续申请恢复主批。完成同一采集链的身份安全前置，本地提交 `537aad9eb`，不另起菜单或回执模型。

- HEAD 单管关联/重复确认在检查实际管之前就可能写入或返回成功。候选先验证实际请求/管 ID、同申请和类型、作废/拒收、取消及待采集请求已有绑定等；正常首次关联更新一次，原关联重放无修改。
- 3文件限定提交：已有服务及私有检查、14项新真实服务SIM测试、限定验收。未夹带锁查询、批量匹配和异常类积压；当前树另在原锁定读取后复核身份，原日期/状态和匹配保护全部保留。
- 冻结HEAD红轮42项11失败；最终独立候选43/43（3.382秒），当前树含原采集回归58/58（3.109秒），源码/依赖指纹稳定。审查发现试改影响已取消物理管保护后已恢复原检查；不以虚构物理管状态证明合法重放，旧测试未删除。初始缺源码的编译错误不计业务红证据。
- 3份工作树文件归档逐条回读通过，SHA-256 `bb01b61c85b40fea944acbccf218b34443f3a9d0b83c6999e33b835368cec1df`；候选逐字对应已测试输入，当前服务未改方法均保留。仅同机源码保存，非数据库/配置备份或运行回退。
- **新确认前置阻断：当前Spring JPA默认方言不支持拟议的一致读隔离级别。** 不能直接添加注解声称安全可用。既有两个GET没有共同快照且过滤部分实体管，不能拼接解锁；未新增不可靠current接口、未清除浏览器保护。
- **下一主批仍为当前申请与完整逐管读回＋明确恢复。** 先补可执行的事务/持久化上下文方案和回归，再以原回执所有者＋当前全部请求/实体管项目权限校验完整读取，接前端明确确认采用。获准隔离库须验证迁移、一致性、并发、保存→采集；不继续扩展本次身份helper或重复首次开单。
- HEAD旧物理状态约束、并发占管、完整权限、批量匹配、前端恢复/编辑仍待；工作树其他保护不冒充已入候选。报告P0、AC09/AC14整体及M0～M7未关闭。无前端改动/浏览器验收，未运行Docker/数据库、未改真实患者、未签发/打印/推送或替换18080。
- 核实仅一个每小时 `lis`，接续本任务最新v3及顶部检查点，无冲突旧顺序。本轮建议在仍有安全代码工作期间暂保留，未新增/暂停/改频率；定时非开发必需、不能绕过门禁，仅余外部阻断时应暂停而非空转。

详见[限定验收](fulfillment-identity-acceptance-2026-09-13.md)与 `tmp/order-current-recovery-20260913-KW9UI3/`。

### 上一检查点：刷新防重开与历史保存只读核对（2026-09-13 15:16）

本地提交 `e313b30f8`，继续同一开单主批，不新增菜单或平行回执模型。

- 首次POST前仅保留key/hash/version；同一标签页刷新、Provider重建、工作区重置仍阻止重复开单，存储不可靠则不派发。明确完整首次回执/输入拒绝才清理，未知/404/重新登录不自动解锁。
- 已接已有按核对码GET，校验历史整单回执，25秒覆盖响应体读取；原账号新会话重新查询仍由后台当前权限决定。会话/工作区失效、查询替换及同身份短暂失效后旧结果永久失效。手工查询无marker时只查历史，不占用当前草稿或授予写权限。
- 删除旧编辑“保存后把所有管再次追加”链，普通/静默开单编辑拒绝，字段锁定；原确认且内容未变继续零重写。**这是暂时禁用不安全编辑，不是申请编辑功能完成。** 查询只展示历史，不覆盖草稿/管/步骤，不清除保护、不开启采集。
- 工作树 `final-v2-worktree-tests.json`、独立候选 `candidate-v2-tests.json`各143/143，0失败/跳过；候选完整前端构建22.45秒，12份指定源/测试格式通过。真实候选Provider/开单/子表单SIM浏览器1440/768两项11.2秒通过，刷新禁用、404重查、只读历史、零新POST和无横向溢出已验；临时18784已停止。
- 初始红轮刷新保护、旧编辑重复管为有效失败；无效存储spy和旧RTL适配失败不计业务红轮。审查补“同身份失效恢复后旧查询复活”选中1项失败，已补暂停代数绑定；最终143项无跳过。旧编辑阶段测试随旧链移除改为8项零派发保护，首次生命周期由原子组合覆盖。不是全系统回归。
- 独立审查同意冻结；16文件提交，15份源/测试/语言与候选逐字一致。16份工作树归档逐文件回读通过，SHA-256 `6569a8c635752993e74517e9b79ab41d16a9cd63c5f28f05c48475f2889ca667`；没有夹带采集/App/整份Utils历史积压。仅同机源码保存，非数据库/配置备份或运行回退。
- **下一主批：当前申请＋完整逐管状态读回及明确恢复。** 后台恢复返回不可变首次快照，原管REQUESTED不代表今日状态；HEAD普通loadOrder未可靠等待/合并部分已采集与待采集请求。必须校验患者/申请/管归属、REQUESTED/COLLECTED/CANCELLED、读回完整性、当前工作区/会话代数后原子应用，才能清理标记及继续采集。暂备打开分支已完整移除，不能用旧快照恢复或普通GET/404解锁；不重新扩展页面锁或其他菜单。
- 获准隔离库、081迁移、真实持久化/并发/撤权、保存→采集、受控编辑、关闭标签页/跨浏览器恢复仍待。报告P0、完整类型既有436条及完整发布门禁不关闭，AC14及M0/M1/M2～M7整体未完成。未启动Docker/业务库，未动真实患者、设备、签发、打印、推送或18080。本轮未改定时任务。

详见[本批限定验收](order-entry-recovery-acceptance-2026-09-13.md)与 `tmp/order-entry-recovery-20260913-j9FZuK/`。

### 上一检查点：首次整单保存前端接入（2026-09-13 14:11）

按最新计划继续，本地业务提交 `96c44d2d9`。只接续当前开单链，没有另开菜单、报告模型或工程工具主批。

- 首次申请一个带唯一标识的POST携带患者/申请/全部逻辑标本/申请级标签，冻结原JSON及摘要；成功回执按患者/申请/逐管/标签严格核实，不用普通查号和逐管HTTP当提交确认。草稿已确认且本工作区同会话内容未变时，继续采集不再建管。
- 补回归修复：新工作区覆盖旧模板modified:true；不明编辑身份不得绕回无键写链；发送前摘要10秒失败与发送后未知区分；400/422只接受明确后端输入拒绝；真实标签组件snake输入与camel回执匹配；有患者/EQA不静默转环境。未知原命令保留在Provider私有内存，界面显示同会话核对码；没有把患者报文放进浏览器持久存储。
- 当前树最终 `final-frozen-tests.json` 142/142；HEAD739551c6c＋14文件独立候选 `candidate-v3-tests.json`100/100；候选完整前端构建21.13秒，11份指定源/测试格式通过。早轮实际Provider/开单及子表单SIM浏览器1440/768共2/2，静态服务已停止；仅导航到采集探针，不代表采集保存或后续新环境禁用行为的浏览器验收。完整类型既有436条、后端及完整发布门禁保留。
- 红证据：初始16项15失败；边界26项8失败；实际标签29项2失败；环境切换选中2项2失败。历史sessionGate同7失败已用本批前归档独立复现，不能归为本批引入，也不称全回归通过。历史空草稿保存夹具已修正，完整有效自动编号场景移入本批独立测试。
- 独立审查同意限定检查点；15文件已本地提交，14份代码/测试/语言与候选逐字一致。备份15文件全部回读一致，SHA-256 `45a0585a8441d4f6fe698df81f61a206b28c0fbd35eefe59c66be6ea6f7e40b3`。没有全量暂存，其余历史修改保留；仅同机源码备份，不是数据库/配置或运行回退。
- **下一主批：接现有按核对码恢复接口，完成明确重新授权、未知查询与刷新/跨工作区恢复；同时收口首次保存后修改申请走旧多请求链的重复管风险，再获准隔离库验证首次保存→采集。** 原生环境命令已验，但环境-only默认类型的旧表单同步仍待；不要重新实现后台回执或继续叠局部锁。布局密度/禁用视觉仍在同一工作区后续，不称全UI已优化。
- 081未执行、真实PostgreSQL持久/并发/断连恢复未验；没有启动Docker、业务数据库或18080，没有真实患者、设备、打印/签发/推送。未部署，M0/M1、报告P0、AC14整体及M2～M7不关闭。
- 既有lis仍ACTIVE，每小时读取此唯一计划/顶部检查点，与计划无冲突。本轮不改调度；定时不是必需，已建议暂停集中逐批推进，尚无暂停指令。

详见[本批限定验收](order-entry-client-acceptance-2026-09-13.md)，证据目录 `tmp/order-entry-client-20260913-n5W35r/`。

### 上一检查点：首次开单持久回执代码（2026-09-13 08:07）

按最新要求继续唯一计划，已本地提交 `c8e6e2e17`，没有另开业务模型或菜单主批。

- 原首次开单POST带唯一提交头进入已有多管事务：完整原始JSON捕获及摘要、当前操作人绑定、持久记录认领并立即flush、原校验/保存、实际患者/全管/标签回读核对、回执完成。未带头旧入口不改；重放先于首次创建校验，不重复开单或STAT通知，恢复重新授权并返回原快照。
- 修复审查复现的少/多管、错误患者或管归属、保存过程中丢标签以及损坏标签快照接受问题。已有患者、新患者、EQA和无患者环境样本按各自合同验证。完整字节限制、重复头/键/尾部报文、400/403/409/503、未知保存与谨慎404提示、禁止缓存均有分层限定回归。
- 当前工作树 `final2` 178/178、16.424秒；HEAD `94d20cf2e`只叠本批的独立候选 `candidate-final2` 178/178、16.467秒。49项精确本地依赖；指定源码/迁移/依赖前后指纹一致。实际MVC绑定用模拟服务；真实协调器和原开单Spring代理共用SIM内存资源；Hibernate SessionFactory及继承版本、DAO调用顺序、XML结构检查不连接数据库。
- 初始带头旧分支120项1失败；完整性 `review-red2`142项6失败。反向去掉实际认领的 `candidate-no-claim2`178项14失败，含缺认领导致模拟回执不存在的连带失败。中间夹具/显式类路径/嵌套事务测试资源/Mockito重设失败均保留并修正后重跑，不累加为产品缺陷或总通过成绩。
- 独立审查允许限定本地检查点；15文件提交，14份代码/迁移暂存与候选逐字一致。Controller只选本批入口/恢复/STAT提取，base只登记081，没有带入旧采集或080导航积压。工作树15文件冻结归档逐条回读，SHA-256 `57a50255f43c43a4a2d17877af5fbbfa69f880d3f2a196b4d342e4a4cf7a571a`；源码归档含历史未提交片段，不是数据库/配置备份或固定运行回退。
- **下一主批：前端整单保存接入、冻结原标识/报文、刷新及未知状态恢复，确认回执后进入实际采集；然后获准隔离库验证持久并发/断连/回滚及保存→采集链。** 不再叠另一个页面局部锁或回执体系；布局/禁用外观继续在同一开单工作区整理。
- 081迁移已登记但未执行；真实PostgreSQL时间往返/唯一键并发/提交断连/跨进程恢复和完整认证链未验。前端未切换，没有新UI可验；Spotless已知停滞/历史类型436条诊断及完整发布门禁仍未齐。未启动Docker/数据库/HTTP、未动真实患者/设备/签发/打印/推送，未更新18080。M0/M1、报告P0、AC14及M2～M7整体不关闭。
- 定时核对：既有lis每小时同任务读取本计划和顶部检查点，没有另一份计划。用户询问必要性后建议先暂停并发出选择问题，尚无确认，因此本轮没有改变ACTIVE状态、频率或新建调度；当前开发不依赖定时触发。

详见[本批限定验收](order-entry-receipt-acceptance-2026-09-13.md)，证据位于 `tmp/order-entry-receipt-20260913-Kk1T1F/`。下列为历史检查点，不覆盖当前下一步。

### 上一检查点：显式开单身份绑定（历史证据）

2026-09-13 06:58（北京时间）：继续同一首次保存主批，在确定持久回执的创建者/授权恢复时发现当前认证和旧OE会话用户可分裂；先修复实际显式入口的必要身份前置，已本地提交 `a1ead34b7`。不是另起身份平台或回执框架。

- 显式`requestedSpecimens`保存前绑定唯一活动用户；本地账号状态/当前DB角色复核，SSO沿用现有身份提供方权限。认证、OE会话、线程权限请求须同一用户/会话/完整权限/权限来源；不创建会话，不用旧用户或守护账号兜底。同名的另一会话仍拒绝，正常包装器允许。
- 保存末尾及事务`beforeCommit`复核；认证、OE用户、专业组、权限请求等变化退出事务；外层事务在方法返回后变化也回滚，不发布最近申请上下文。Controller固定中文403，不重载或写成功标记。缺省旧入口未改，不能外推为全系统安全；无实际患者受影响证据。
- 初始旧入口`red`82项3失败；审查新增边界`review-red`116项6失败，修复合法SSO无本地角色被误拒和权限来源/线程请求混用。工作树16份源/测、41项既有依赖显式Java21编译，`final`119/119、4.475秒；HEAD `05ea13a91`＋本批独立候选13份源码，119/119、5.902秒。源码/依赖前后指纹一致。
- 反向仅移除真实入口绑定，`candidate-no-actor`119项8失败。`green2`新测试构造器编译错误修复后重跑，保留失败证据；中间111/116项成绩不累加。真实服务/Spring代理使用SIM内存资源，guard单测手动事务同步状态，Controller直接调用；不是Hibernate/PostgreSQL、实际MVC/SSO链或并发撤权验证。Spotless既有停滞及全量类型436条诊断条件未变，未重复、未记发布门禁通过。
- 独立审查通过；7文件限定提交，6份Java暂存与独立候选逐字匹配。历史采集等差量保留，未整文件暂存。七文件工作树归档逐条回读通过，SHA-256 `c99b6dddae3ab3d15a4c80dd5f075be6293db714f03a0e9fad4cf84aeadfc7f2`；含历史未提交源码，不是数据库备份或固定运行回退。
- **下一主批仍为已有开单事务路径的持久唯一提交标识、完整结果及重新授权的重放/恢复，然后切换前端。** 不再扩展通用登录/旧会话旁支。应在事务内先认领持久提交记录、失败整批回滚；重放不重发STAT等提交后事件，恢复返回原提交的权威结果而非当前普通GET；具体设计/唯一冲突和真实隔离验证仍待实现，不把本批actor绑定当成回执。
- 前端多HTTP保存尚未切换，未声称界面已改善。未启动Docker/数据库、未操作真实患者/设备/签发/打印/推送、未部署18080。既有每小时任务本轮未改；仍读取该唯一计划及最新检查点，只有外部阻断剩余时暂停。M0/M1、AC14、报告P0及M2～M7整体状态不变。

详见[本批限定验收](order-entry-actor-acceptance-2026-09-13.md)及 `tmp/order-entry-actor-20260913-oxkj7p/`。

### 上一检查点：2026-09-13多管标本事务（历史证据）

2026-09-13 06:01（北京时间）：按最新计划继续首次保存主批，已本地提交多管标本事务接入 `0bb650ed8`；不另建平行入口，未切换前端或部署。

- 显式 `requestedSpecimens` 仅用于首次开单，缺省保留旧合同；编辑/已有申请/采集XML/逐管标签混入会被拒绝，Controller采集早分支也不能绕过。复用原 `saveEntry`，新增批量方法要求已有事务，以实际申请与当前用户保存全部逻辑标本请求；同类型两管保留两个身份。
- 整批插入前校验数量/顺序、禁止客户端指定归属与状态、活动类型/单位、项目可下单及类型关联、当前登记权限与组合有效成员。第二管失败、插入无效/重复ID或后续标签失败向外抛出，不以部分成功返回。申请级标签保留；逻辑请求ID不是实体标本ID，逐管标签仍须采集后进行。
- 工作树14份指定源码/39项既有依赖显式Java21编译，`green6` 79/79、5.717秒；HEAD `aa6fb51d9`＋本批独立候选11份源码，`candidate2` 79/79、5.947秒。源码/依赖前后指纹稳定。实际新批量方法的Spring代理已验证无外层事务拒绝、一次共同提交及第二管失败回滚；事务资源为SIM内存，不是Hibernate/PostgreSQL。
- 反向移除首单门禁与批量调用，`candidate-no-batch3` 79项12失败。早期构造器/候选import编译失败及裸ObjectMapper缺既有FHIR注册的1项失败保留；`candidate-no-batch2`误选正向源，不算反向证据。限定JSON绑定使用AppConfig原FHIR反序列化注册，Controller直接调用，不是实际HTTP认证链。未重复条件未变的Spotless挂起操作或完整类型436条诊断，未记格式/全量门禁通过。
- 独立审查通过；9文件限定提交，8份源/测暂存与独立候选逐字匹配。必要Form既有9行 `collectionOnly` 标志/访问器及服务引用明确纳入，其余历史采集/DAO差量保留，未整文件吞入。九文件工作树归档逐条回读通过，SHA-256 `7b3aed5d4002a5aa45557745f4dbcc4ce85190fba118b52c35ac003738833d7d`；含历史未提交代码，不是候选运行包、数据库备份或固定回退。
- **下一主批：在已有多管事务路径补持久唯一提交标识、完整权威结果及重新授权的重放/恢复，再切换前端多HTTP保存链。** 已有编号预查不解决并发窗口，DTO不等于持久回执；不能将前端未接入的路径称为用户操作已修好。组合按本管类型全部有效成员校验，较旧展示权限交集更严格，前端需明确解释权限/主数据变化。历史采集展开组合全部成员的按管类型边界仍待同链核对。隔离库条件确认后验证并发、唯一约束、真实回滚及提交断连。
- 现有唯一每小时任务 `lis` 已核对与最新计划一致，本轮未新建/改动/暂停调度；不是开发必需条件，安全源码工作仍可推进时暂留，仅剩外部阻断时应暂停避免空跑。未启动Docker/数据库/HTTP，未动真实患者、设备、签发、打印或远程推送，未更新18080。M0/M1、AC14及M2～M7整体仍未完成，报告P0不关闭。

详见[本批限定验收](order-specimen-transaction-acceptance-2026-09-13.md)及 `tmp/order-specimen-transaction-20260913-XrhoIi/`。

### 上一检查点：2026-09-13普通REST事务入口（历史证据）

2026-09-13 04:52（北京时间）：沿既定首次保存主批，已本地提交普通REST事务入口修复 `f4e2c0f9c`。本轮未新增未调用的回执对象或平行计划。

- 实际REST改为单次调用服务 `saveEntry`，事务早于可变初始化，患者prototype每次获取；患者/程序/EQA/通知配置/接触追踪/申请及可选标签沿原模型保存。校验以BindException退出代理，受检异常也回滚；显式环境采样不准备患者。STAT保持提交后尽力通知，通知异常不反转已提交保存。
- 工作树最终11份指定源码显式Java21编译，35/35、3.835秒；HEAD cab3ba0f3＋本批5文件独立候选7份源码、35/35、5.069秒。39项依赖及源码前后指纹稳定。无外层事务红轮25项10失败；仅去掉受检异常回滚规则的候选35项3失败。详见[本批验收](order-entry-transaction-acceptance-2026-09-13.md)。
- 独立审查通过；6文件限定提交，候选5份源码与暂存逐字匹配。其余Controller21增2减、接口7行、服务253增3减的历史未提交差量完整保留。六文件工作树源码归档回读通过，SHA-256 `e59238393ac0b871bcfcc0aa9562f94f7f94b3a79dbdf9122a0b46321428c541`；包含历史未提交源码，不是直接发布包、数据库备份或固定运行回退。
- 测试仅真实Spring代理/协调＋内存资源，下层初始化/标签为mock；Controller直接分派，不是HTTP认证链或数据库回滚证明。首次夹具及依赖错误保留，未计入业务红轮。限定5份拷贝Spotless离线仍无完成结果，已中断130；人工整理新段落、diff-check通过，不能记格式或全量门禁通过。
- **下一主批：复用已前移的事务入口，将全部标本申请纳入同一保存，并补持久唯一提交标识、完整回执及重新授权的重放/恢复。** 不再另起相邻会话工具或菜单；程序/EQA/标签/外部申请字段不能静默丢弃。普通GET和session不是提交证据。隔离数据库范围确认后验证唯一约束、并发、真实回滚与提交后断连。整单AC14、报告P0、M0/M1及M2～M7仍未关闭。
- 现有唯一每小时任务 `lis` 已核实读取最新v3及顶部检查点；未新建或改动调度。仍有安全可推进源码工作时可保留，若仅余外部阻断应暂停而非重复检查。未启动Docker/数据库/HTTP、未动真实患者/设备/打印/签发/推送、未更新18080；隔离环境待确认及运行门禁不变。

### 上一检查点：2026-09-13提交后会话上下文（历史证据）

2026-09-13 03:43（北京时间）：按用户继续最新计划并询问定时任务的要求，接续同一开单保存主批；事务回滚残留“最近申请/患者”上下文已修复，本地提交 `fa7b80687`。

- 既有 `persistData` 在事务提交前写session，后续/外层失败仍留下新编号；旧标签入口缺编号时会读取它。本批仅该调用点改为提交后发布，原事务内事件位置不动；原会话/编号快照、环境采样清旧患者、会话失效不误报失败、无事务不立即发布均受保护。
- 冻结HEAD8207002f5原服务13项10失败；候选13/13通过，独立审查补两项后15/15、2.441秒。当前工作树8份相关源码显式Java21编译、同15项3.397秒通过。候选32项/工作树35项依赖与源码指纹稳定；这是实际服务方法和Spring同步回调＋内存事务管理器，不是数据库或真实认证/打印测试。
- 4文件限定提交，服务仅一处替换，不夹带253行历史采集新增。提交后候选服务逐字相同、当前8份源码指纹均保留。四文件工作树归档逐条回读通过，SHA-256 `69e2d09402dcbc2013d9f856b6e28ff6538caee5c40bebdcf3c1a41bd98449a3`；归档保留服务历史未提交代码，非可直接部署的提交包或运行回退。
- 首轮缺依赖、云占位读取中断、夹具初始化失败及扩展编译缺三个依赖均保留；只以有效红轮和最终稳定输入为证据。两个限定Spotless尝试无结果均主动中断、退出130，不能记格式通过。`git diff --check`通过，没有全项目/数据库/浏览器通过结论，也未重复完整类型已知436条诊断。
- **下一主批仍为后端整单事务入口、唯一提交标识/持久回执和可核实恢复。** 入口须早于可变对象初始化；须覆盖患者/申请和全部标本请求，重放重新授权，不重发事务事件。程序/EQA/标签/外部申请兼容不能静默丢失。隔离库再验证并发、唯一约束、回滚及提交后断连。session值不是回执，不准普通GET解未知。
- 旧编辑入口仍有相同会话写法，本帮助器与旧读取方不保证两个旧键整体原子可见性；只记录积压，不另起平行主批。整单原子保存、持久去重、AC14、M0/M1及M2～M7均未因此完成。
- 本轮核对 `lis` 仍每小时接续当前任务、读取唯一v3/最新检查点，没有冲突的固定旧下一批；保留且未新建/修改调度。未启动Docker/数据库/HTTP、未动真实患者/设备/打印/签发/推送、未替换18080。已知隔离环境范围仍待用户确认；调度不放宽门禁。

详见[本批限定验收](order-session-commit-acceptance-2026-09-13.md)与 `tmp/order-session-commit-20260913-acpNad/`。

### 上一检查点：2026-09-13单管创建回执（历史证据）

2026-09-13 02:40（北京时间）：接续同一开单回执主批，单管创建回执验真已本地提交 `e2b3dd647`。

- 发现真实适配器合同错误：通用POST第二回调参数是附加参数，不是JSON；原创建链即使未取得请求记录也可能继续。改用HEAD已有完整响应方法并读取JSON，按派发快照核对请求ID及申请/类型/顺序/数量/单位/项目/组合/状态；显式失败、错配、同批重复ID停止后续管，进入既有未知保护。后台无有效正整数插入ID不返回201。
- 当前工作树和HEAD1c658f0a8＋本批8文件独立组合各113/113通过；包括真实Provider及真实传输＋内存fetch组合。后台当前Controller等Java21显式编译25/25、HEAD接口与候选Controller组合4/4，均不连接数据库。真实传输原红轮7项4失败，独立审查显式失败标记42项1失败；后台冻结旧源25项3失败，原失败均保留。
- 独立候选完整前端构建21.92秒；实际开单/Provider SIM页构建16秒，1440/768×空回执/错申请/正常两管共6/6浏览器场景通过，16.2秒。两份API的工作树/候选源映射各匹配；限定6份前端格式及1835中文引用键通过。未跑全后端、真实认证/事务或完整类型；原436条诊断不降低基线。
- 9文件限定提交及工作树字节保留核验一致；备份归档逐条回读通过，SHA-256 `24d17341959c10e009f22fbed66eb88bbcb64420e601c4176f964c32d71ad668`。API其余历史GET/映射、Controller历史fulfill分支及其他未完成修改未夹带。仅是源码备份，非运行回退。
- **下一主批仍是后端原子保存服务、持久回执/唯一提交标识及可核实恢复，再接隔离数据库首次保存→采集。** 本批只验证单管响应，不证明患者、申请、多管请求与标签共同原子提交；刷新/跨工作区/跨客户端仍需持久证据，普通GET不能解除未知。可复用既有FHIR原子接收模式，但不混用FHIR单项目/已有患者的业务合同。不再重复前端页面锁或相同客户端字段保护。
- 实际浏览器仅内存SIM接口，真实网络均为18784静态GET，CSP禁业务网络；初次绑定权限失败保留说明，限定授权后通过并已停止。截图中文未知提示有效且关键按钮实际禁用；表单长度/禁用视觉一致性仍待，不称UI全面通过。
- 未启动Docker、未连接数据库/真实患者/设备、未签发/推送/更新18080；运行环境范围等待用户答复。M0/M1、AC14整体及M2～M7状态不改完成，报告P0及全量发布门禁保留。现有每小时任务未改频率/未新建。

详情见[本批限定验收](sample-request-receipt-acceptance-2026-09-13.md)及 `tmp/sample-request-receipt-20260913-KqpTZF/`。初始测试驱动错误、后台红轮输入漂移和默认日志权限失败均原样保留，不算通过证据；采用冻结原源红轮及最终指纹稳定的结果。

### 上一检查点：2026-09-13跨子页提交保护（历史证据）

2026-09-13 00:58（北京时间）：按唯一v3继续同一开单主批，已完成并本地提交 `734d73f14`。

- 工作区内子页卸载重入保留未知提交编号；切申请/换患者/超时/重复回执不能串写或续派标本请求，草稿和普通保存不能绕过在途锁。临床新患者回执缺有效患者主键阻断，明确环境采样合同保留。
- 当前工作树9文件170/170、独立提交组合5文件66/66通过，均0失败/跳过；独立组合以父提交6786cff04仅叠加8份源码/测试构造。当前前端构建1分19秒、独立组合1分15秒成功，4份实现源映射与对应源码逐字一致。不是完整后端/数据库或全项目回归，历史完整类型436条诊断仍保留。
- 真实Provider、开单及子表单的1440/768双视口SIM浏览器2/2通过（11.7秒），含保存中/重入后的保存、新患者、添加标本禁用，以及迟到回执不续链、正常回执进入采集占位页。仅内存SIM接口，观察到的真实网络全部为18784静态GET；不等于采集业务验收。首次浏览器权限失败保留，限定授权后运行；临时18784已停止。
- 本批9文件归档逐条回读，提交与冻结指纹相同，当前工作树字节保留；SHA-256 `8f9e46befbefb78eecdee0ddc426c7c105f1549c8ecfa11d8fada6f9d92f28b7`。Context/API仅选本批差量，不夹带历史会话、采集、存放功能；其余修改不删除。源码备份不是数据库或固定运行回退。
- **下一主批：后端权威保存回执、持久去重及可核实的恢复。** 当前Provider仅在 `/order` 工作区存活；刷新、离开整个工作区和跨客户端仍未保护，普通GET不得自动解除未知锁。接续已授权隔离数据库首次保存→采集签收；无隔离库时只能推进对应可独立验证的代码/契约，不把模拟当事务证据。
- UI复核发现部分继承禁用的按钮外观仍像可用、表单偏长；实际禁用已测，但视觉一致性/布局密度仍待本工作区后续处理。不能据本批两张宽度验收全部页面。
- 现有 `lis` 每小时任务与唯一计划一致，本轮仅核对并保留，未新建任务、未改变频率；不固化旧下一批，不另开竞争写入。报告P0、运行环境授权、真实医院输入及全部发布门禁不变；未启动Docker、未接真实患者/设备/签发报告、未推送、未替换18080。

详见[本批限定验收](order-entry-lifecycle-acceptance-2026-09-13.md)和 `tmp/order-entry-lifecycle-20260913-QpyZtY/`。M0/M1、AC14整体及M2～M7均未因此标记完成。

### 上一检查点：2026-09-13首次保存入口（历史证据）

按用户“按照最新计划执行，定时任务有必要继续吗”要求继续同一开单场景；报告P0保留，不另建菜单/工程工具计划。

- 本地业务提交 `8114087a2`：编号分配和保存三入口共用同步锁；失效/超时/畸形编号不得保存，错误或未知回执不得进入采集；保存中、待确认状态明确呈现并保留输入。仅当前挂载页的提交保护，不承诺跨页面和跨客户端完整防重。
- 当前工作树6文件152/152限定回归通过；从父提交 `c66fa5ce2` 单独叠加本批8份源码/测试/语言输入，35/35通过，候选Context逐字等于父提交。当前前端最终构建15.34秒通过；完整类型仍退出2、436条诊断；格式及1835个中文引用键检查通过。不同范围不相加为交付成绩。
- 真实开单组件及子表单在1440/768双视口SIM浏览器2/2通过；覆盖保存忙碌、缺回执保留编号/阻止继续、有效回执跳转及无横向溢出。仅内存SIM数据，无接口网络、无数据库，不是整条主流程或全界面验收。静态18784测试服务已停止。
- 9文件冻结归档逐条回读，提交后内容与指纹一致；SHA-256 `bc6b1e9aa875bdc2b7af8dfeb620aa840b83a3a379d6418e7fac3218bf704e9b`。Layout和语言仅纳入必要小块；其余历史Context/Layout/语言及其他修改保留。源码备份不是数据库/配置备份或固定运行回退。
- **下一主批：共享Context的未知提交状态和已发送请求跨申请回写保护。** 当前OrderEnter卸载重入会丢失本地未知锁，HEAD Context旧回调仍可能串申请；先补共享生命周期合同及父级回归，再接权威回执/持久去重并衔接隔离数据库首次保存→采集。刷新/跨客户端必须服务端证据，不能再靠另一个页面局部锁代替。
- 保留既有 `lis` 每小时任务和原线程，仅更新其指令为读取唯一v3及本顶部最新检查点，不再硬编码旧下一批；一个主批＋独立审查、及时本地提交。没有新增任务，调度不放宽权限。
- 未启动Docker、未改真实患者、未签发报告/连接设备/推送、未替换18080；全量门禁、授权隔离库、配置/数据库备份及运行回退未齐，不发布。M0/M1及M2～M7整体仍未完成。

本批关联AC07/AC14/AC18/AC19子集，详见[限定验收](order-entry-submission-acceptance-2026-09-13.md)，证据在 `tmp/order-entry-submission-20260912-Z4ni55/`。

### 上一检查点：2026-09-12患者入口（历史证据）

本次是用户“继续”的普通开发回合，未改定时任务。按2026-09-12计划纠偏，主目标转为可解释、可连续操作的SIM业务闭环；报告P0不降级，独立可测的患者/开单入口安全修复不等待无关数据库条件。旧流水保留证据，不再把反复扩充工程工具作为下一批。

- 本地业务提交 `8090742e6`：申请页患者选择和新建草稿隔离，2实现＋1回归＋1验收共4文件。清旧患者身份、区分失败与无匹配、拒绝错ID/迟到回填、只读保护；草稿可见可恢复，首次自动编号不中断新建，已知电话错误随草稿保留/换患者清除。
- 初始12项11失败；独立审查新增编号/草稿2失败，再新增电话2失败，均保留红证据。最终13文件71/71通过、0失败/跳过；1440/768两种视口真实组件SIM浏览器2/2通过；3文件格式及1836中文引用键检查通过。不是全前端、数据库或正式业务验收。
- 冻结源码完整前端构建17.83秒通过；源映射核对两份实现与当前文件相同。原完整类型仍退出2、436条诊断，未改基线、不按全部门禁通过处理。
- 4文件同机归档回读通过，SHA-256 `5942eb7b808a3c11acf03b1e8d20ea960b18136e489da72ffb47325c4e92c531`；原源码备份及所有历史未提交修改保留。本批代码已独立提交；本页/计划/简明状态随后单独提交，不夹带其余源修改。
- 未启动Docker、未连接数据库/设备、未改真实患者或签发报告、未推送、未更新18080。隔离18783仅提供模拟静态组件页面，测试后已停止。完整备份/运行回退和环境条件未齐，不发布。
- 下一操作：真实父级开单表单“患者→送检方/项目→首次保存编号→进入采集”，覆盖生成/保存失败及结果未知；同一工作区布局仍待收敛。并行准备仅限该业务及M1必需依赖/隔离库条件。报告安全主体/持久化、临床主数据、医院接口、打印和UAT缺口不变。

本批只推进AC07/AC18子集，M0/M1及M2～M7整体状态不改为完成。详见[本批限定验收](patient-order-selection-acceptance-2026-09-12.md)；原始证据及指纹在 `tmp/patient-selector-20260912-8IJf9M/`。

历史调度记录（非本轮操作）：2026-09-10 23:42（北京时间）按用户“按照计划定时迭代”授权恢复既有 `lis` heartbeat，每小时一次、保留原线程，未创建重复任务。此次明确授权覆盖 v3 计划及旧记录中的定时暂停状态，不放宽任何开发、发布或真实数据操作门禁。09-11批次只修复此前登记的类型基线门禁错误放行问题，不扩展业务功能。主体绑定A2及共享模型前置A3限定已验，实际持久读取及授权连接仍未完成，A1/A2/A3均不关闭M1或报告P0。

## 工程基线与阻断

用户已明确要求及时本地提交。当前功能分支为 `feat/017-lis-product-delivery-m1-report-foundation`；A3 的6个文件已提交为 `ceec5935a`，未包含其他未完成修改。07:56提交前重跑对应30项无数据库模型/映射/XML测试通过，输入指纹一致，并独立确认既有支持类已在父提交中。完整构建和数据库验证仍未通过，不冒充发布候选。后续按独立修复及时提交，不等待全项目交付，也不擅自推送。

08:14将既有A1/A2的6个变更文件独立提交为 `fbf40e144`（2实现、2新增测试、2日期化进度记录），没有加入其他工作区修改。提交前显式编译当前2个实现和3组测试，再次158/158通过（JUnit耗时1.399秒）；5份源码及25份依赖指纹稳定。原47项回归已在父提交中，未重复添加。独立审查确认41个直接项目依赖及登录/基础服务实现、pom均已在HEAD且无差异，范围可独立提交。新证据位于 `tmp/report-principal-binding-20260911/final-green/`；这是既有修复的版本检查点，不是新增业务功能、完整格式/构建门禁、真实认证/数据库验证或运行版更新。

同轮重新核对本机源码备份：归档SHA-256匹配，7,702份文件无缺失，源码均与备份一致；核验时仅简明进度页与开发计划为有意更新的文档，本次台账修改另行提交。尚有历史修改待分批验证、提交，没有删除或回退。没有远程推送或异机备份，不能承诺整机故障下仍可恢复。

09:12按既有积压整理顺序，将M0-01实现、单测、使用与安全说明及日期化验收记录共4文件本地提交为 `5490198fa`；没有重做预检工具或新增业务功能。3份工具文件与09-10冻结指纹逐字一致，本次语法检查通过，限定单元18/18通过、0失败/跳过，603.506015毫秒；测试仅创建并清理自身SIM临时文件，进程与HTTP边界使用注入，未请求实际Docker或HTTP。独立只读复核确认仅依赖Node内建模块，不需夹带业务代码；补入原未跟踪的使用说明，避免验收记录出现仓库内断链。

本次[测试日志](../../tmp/readiness-checkpoint-20260911-fnRi2W/node-tests.tap) SHA-256为 `939dce39b932e3620ebcb09f22cca10eb52f73a68cc07f809ff95a8489e67043`；同目录 `source-before.tar.gz` 保存提交前4文件，压缩包清单及逐文件回读均通过，内容与当前源和Git提交一致，SHA-256为 `0d0e2db6a55601c2ef5dbeea3f237427748919bb5c23ce359d8f89d06f1dbce1`，不是数据库备份或运行回退版。本轮没有改写这4个既有文件；前端Prettier入口依赖仍带云占位，未重复启动已知不可用的格式/完整构建，也不将源检查点称为完整门禁通过。环境恢复、真实数据库、临床与运行版状态没有新的通过证据。

12:22更新（上段09:12状态保留为历史）：11:13已实际提交固定15,595项原前端文件下载，12:13后验全部本地，原元数据、逐文件哈希与7,702保护输入通过独立核验；12:15测试后再次后验一致。没有重新安装依赖或用旧源码覆盖现树。此前未能启动的患者日志6项组件交互，加原2项兼容、11项本地化单元，19/19通过、0失败/跳过，Vitest总耗时12.78秒；不代表真实后端或浏览器验收。

12:19已将既有组件、6项测试及限定记录3文件本地提交为 `1856cff46`，没有夹带后台或共享文件修改。独立审查确认所需接口与消息合同已在HEAD；原后端严格范围保护仍另有待收口修改。源码归档 `tmp/patient-audit-checkpoint-20260911-dGvao5/source-final.tar.gz` 逐文件回读通过，SHA-256 `eb09eb36e1f99e6f9ecd3bf129319d91b8cbc6425f2af5cf3e3e05cdb5fb4a8c`。代码与09-10冻结候选一致，本次是恢复验证与提交，不是重新开发或部署。范围和告警见[本次限定验收](patient-audit-scope-acceptance-2026-09-11.md)，恢复证据见[恢复记录第5节](environment-recovery-2026-09-11.md)。

14:22收口更新：格式忽略规则误将业务报告源码排除，已从 `reports/` 改为根级 `/reports/`，其他排除不变。新增14项真实Prettier正反断言，旧规则3失败/11通过、新规则14通过；真实报告目录检查暴露的2份组件只机械折行，前后AST相同。14:21将规则、测试、2份纯格式源码和本批记录共5文件本地提交 `db3fc016b`，没有继续叠加功能。14:18同范围连同原患者日志组件/本地化重新执行33/33通过、0失败/跳过，11.54秒；报告目录及新增测试格式通过，3源码格式化均为unchanged。独立复核确认提交边界及证据。

本次补齐先前未汇入台账的13:22结果：当前源码前端独立构建成功，20.78秒、2636模块；输出仅在 `tmp/frontend-gates-20260911-oeWU0p/final-dist`。静态中文检查1836个引用键通过；原 `npm run typecheck` 退出2，44个文件共436条诊断，完整类型门禁失败。`typecheck:delivery` 仅表示相对1209基线无增加，不可替代完整类型通过。已有日期控件、分块大小及构建插件告警保留，不因构建成功关闭。14:18再次核对1,060个源码/公开资源/7个配置输入与已构建候选一致，本轮未重跑完整构建或类型命令。尚未执行全量前端回归、其他目录完整格式、后端完整门禁、浏览器或数据库验收。

针对代码保存风险，14:18重新备份指定主前后端源码、脚本、配置与文档，共7,710文件；元数据确认非云占位，备份前后哈希一致，完整归档回读通过。`tmp/format-scope-checkpoint-20260911-Za7oOa/current-source-before.tar.gz` SHA-256为 `200064065b52ed4f9486b448e1c28f0e7c98dde6dac1605daa9142eb0ddeef4d`；逐文件清单保存在同目录 `source-backup-integrity.json`。本批5文件最终归档 `batch-source-final.tar.gz` 回读通过、与提交一致，SHA-256为 `2eff45b321486d648954ae829a5ad77add7c8dbb72257455d8495a22bd2644bf`。本页、简明进度页及验收记录的后续明确文档更新另随本地提交保存。旧源码备份均保留；这些仅是同机源码保存，不含数据库/全部依赖/全部子模块/Git历史，也不是可运行回退版本或异机容灾。

本轮提交前前后端源码/测试为131个已修改、275个新增未跟踪文件；提交后129/274，其余积压保留，不清理也不算已提交。限定回归记录 `tmp/format-scope-checkpoint-20260911-Za7oOa/precommit-tests.json` SHA-256为 `fe651cd3fb240718da73654d26343f3fba7b1d81caa8a74f6b4ebd26659fa6ee`。详细范围、原失败及构建证据见[格式范围验收记录](format-scope-acceptance-2026-09-11.md)。本轮没有启动Docker、连接数据库、改动真实患者、签发报告、连接设备、远程推送或替换HTTP运行版。

15:29更新：本批针对已登记的类型门禁失败保护完成3文件本地提交 `39d59d777`。旧脚本可能将信号终止、非零空输出、全局错误等当作诊断减少而放行；现固定无彩色输出，区分合法0/1/2退出与诊断数量，拒绝信号/未知状态/stderr/全局或未识别错误，并验证基线版本、非负整数及总数。未修改实际1209项基线（SHA-256 `af9c28bcf1c9f03a68978fbb5350e4f243fba9e7d5ad5a640a6b5284a9394519`）。完整原类型命令的436条/44文件失败没有被修复或隐藏。

红轮33项27失败/6通过，首次绿轮32/33，唯一失败为macOS临时目录别名与真实路径差异，以规范化SIM根修正且保留失败证据。新增3项真实已安装TypeScript的隔离SIM工程组合后，本批36项＋原格式保护14项共50/50通过；最终冻结候选再次50/50通过、0失败/跳过，35.70秒；语法及两源码限定格式通过，1,067份输入哈希一致。其余33项门禁回归使用SIM编译器子进程，未mock门禁CLI本身。独立代码审查通过；没有临床界面变更或业务数据库测试。生产进程尚无超时设置，不能将测试外层限制算作已实现的生产超时防护。

修改后的当前项目 `typecheck:delivery` 已实际退出0，仍报告436项当前诊断/1209项基线，同时明确完整类型检查仍失败；仅通过基线不增长比较，不替代原完整类型命令。证据 `tmp/typescript-gate-20260911-SJyptJ/current-project-baseline.log` SHA-256 `d858718f18d8039934c90c5774a5acbdb541456976dfaf9727394056d3604230`；最终50项 `final-tests.json` SHA-256 `b3e2e1dd334a176d57d6716dbeec05efe6bd9b7ecb9825d199aa8c1191070fc5`。本轮没有重跑应用构建、启停服务或更新HTTP版。

本批3文件归档 `tmp/typescript-gate-20260911-SJyptJ/source-final.tar.gz` 回读及提交核对一致，SHA-256 `9a467b63fe1b1fc6a35ef447b2bdcbe5fdc8bfe102bec45659b92c117edad0c2`；原脚本和基线另有备份。再核对上批7,710份保护清单，无缺失或非预期变化；只保留先前3份文档更新与本次脚本修改。历史源码积压129/274未清理。源备份仍不含数据库或运行回退制品，完整门禁及现场条件未齐，不部署。详细验收与语义边界见[类型门禁验收记录](typescript-gate-acceptance-2026-09-11.md)。

| 任务 | 状态 | 产物或证据 | 下一步/解除条件 |
| --- | --- | --- | --- |
| PLAN-01 统一风险优先级与依赖计划 | 文档独立复核通过 | v3实文件经两轮独立审查；已将工程就绪M0-E与范围确认M0-S分离，补齐外部契约期限及历史失败规则 | D01确认后冻结首期范围并排日历；不等于M0或医院验收通过 |
| M0-01A 只读环境预检及回归 | 开发已验；本地提交5490198fa；实际环境仍阻断 | 既有18/18测试及Java21首发版本误判修复保留；09-11提交前再次18/18通过、0跳过，原工具指纹一致；没有重跑实际环境探针 | 预检任务完成不等于环境恢复，继续M0-02；[原验收证据与边界](delivery-readiness-acceptance-2026-09-10.md) |
| M0-02 完整源码/Git/依赖恢复 | 进行中；主源码及本批前端原依赖已恢复，后端/其他输入仍未完整 | 原18关键文件/1761源码与主Git对象恢复保留；新增15,595前端原文件全部本地，原inode/尺寸/mtime稳定，两次恢复后SHA一致；不将工具单元算业务回归 | 继续按拟构建范围恢复精确后端依赖、子模块等；不重装覆盖旧树。[恢复证据](environment-recovery-2026-09-11.md) |
| M0-03 当前源码完整构建/回归 | 前端独立构建、限定回归及门禁失败保护已验；完整类型失败、全门禁未通过 | 格式修复db3fc016b；类型基线失败保护39d59d777，50项限定工程回归通过，当前项目仍436诊断；后台历史专项不是当前完整门禁 | 继续收口既有后台改动及必要后端精确输入；前端类型/全量格式与测试仍待，不因基线通过放行部署；已完成格式及基线保护不重复开发 |
| M0-04 本地版本恢复与实际核验 | 待开始/环境阻断 | 07:32限定预检仍为Docker socket缺失/HTTP不可达；未启停服务。静态配置声明自动重启，旧业务容器可能自动恢复，实际容器及其策略尚未核验；已向用户确认是否仅本地测试且无真实医院/仪器连接 | 答复前不启动可能恢复未知业务的引擎；确认后限定本机引擎/旧版本及SIM隔离条件；新候选仍须过发布门禁 |
| M0-05 首期范围和责任冻结 | 阻断（待D01） | 已向用户询问常规优先还是专科全部首期；未得到选择 | 用户确认；保留全部原需求，不擅自缩小 |

M0整体未完成。M1的受控增量实现依对应范围M0-E工程就绪，不依M0-S范围确认或HTTP启动。已建立“当前显式归属定义＋真实授权服务（含主体绑定）＋三组JUnit单元”的限定M0-E：Java21显式编译当前源，旧应用Jar及原缓存仅作为依赖。不能推广为全部报告源/真实数据库可验证。00:47旧预检18通过/7阻断/2未知保留为历史；07:32关键文件恢复后同一限定预检为25通过/0阻断/2未知，完整扫描仍发现大量依赖占位，不能凭探针判定全环境就绪。本普通用户回合按最新执行要求获准调用系统原文件下载，仅恢复精确清单；没有绕过此前中断状态反复重试相同探针。完整回归需完整源码及依赖，部署需完整M0和发布门禁；后续主优先级不得自动返回存放相邻增强。

## 已验子批：M1-A1 内部显式归属/成员授权基础

| 任务 | 状态 | 证据与未闭合边界 |
| --- | --- | --- |
| 不可变显式组定义＋真实归属加载＋整份REPORTS校验 | 限定单元及独立审查通过；本地提交fbf40e144，未接入口/未部署 | 55新＋47旧＝102项通过；本次与A2合并重跑158/158。真实红轮5项合法场景失败后修正；旧授权不改。新增方法行覆盖96.15%，定义100%，不是整个服务或项目覆盖率。详见[实现与验收边界](report-explicit-scope-progress-2026-09-10.md) |
| 当前主体与数据库用户ID绑定、持久完整报告组解析 | A2主体绑定限定已验；持久组待开始 | 身份绑定见下表；仍不接受客户端自选项目代替完整组 |
| 申请级版本/签名前快照/旧写旁路/真实事务 | 未完成，仍属M1技术P0 | 新增内部方法不改变旧患者级签发风险，不关闭M1-A整体 |

## 已验子批：M1-A2 主体身份与用户权限绑定

| 任务 | 状态 | 证据与未闭合边界 |
| --- | --- | --- |
| 当前主体→唯一活动系统用户；本地登录关联/状态；SSO分支 | 限定单元及独立审查通过；本地提交fbf40e144，未接入口/未部署 | 56新＋55显式组＋47旧入口＝158/158；本次提交前再次158/158通过。首红155/49失败，补强红158/1失败，均保留；不把mock唯一查找当真实数据库约束 |
| 在途身份变更及畸形权限集合 | 限定回归通过 | 身份映射后及最终成员权限加载后复核；带REPORTS的daemon及损坏集合仍拒绝。新增4个helper行覆盖均100%，其中上下文分支90%，其余100%；整类行覆盖78.87%，不冒充全局100% |
| 产品交互/部署/真实认证链 | 尚未验收 | 没有修改页面、替换运行版、签发报告或访问真实患者；完整构建/回退及真实数据库、认证链门禁未齐 |

详见[A2实现、验收及边界](report-principal-binding-progress-2026-09-11.md)。下述A3不改变A2未接真实入口的状态；M1-B～D仍未完成。

## 已验子批：M1-A3 持久报告归属模型前置

| 任务 | 状态 | 证据与未闭合边界 |
| --- | --- | --- |
| 唯一报告文档＋当前成员模型（共享M1-B前置） | 限定映射/模型及独立代码审查通过；本地提交ceec5935a，未部署 | 新增2实体，不新增平行发行模型；真实SessionFactory装载新模型和既有发行实体，零数据库连接；原19项ORM/模型＋11项离线XML＝30/30通过；07:56提交前同范围再次30/30通过，输入指纹一致 |
| 增量结构及受保护回退草案 | 草案结构已验，数据库执行未验 | 081未纳入标准changelog，旧058及发行链不改；稳定申请/组唯一、组内成员唯一及RESTRICT外键；回退草案非空即拒绝。未执行升级/回退、真实约束或并发；新实体实际自动发现尚未核验，不能部署 |
| 两实体限定Java覆盖 | 限定已验 | 同一冻结class再执行30/30；Document行35/35、分支2/2，Member行13/13、分支N/A；不是持久化、整个报告模块或交付覆盖率 |
| 完整组持久解析及A1/A2连接 | 尚未完成，数据库验证前置阻断 | 没有新增DAO/服务/HTTP写入口；患者/申请/Analysis关系、完整组读取、父组修订及事务须真实隔离库验证。未将旧签发链切换到新模型 |

详见[A3实现、证据与下一批出口](report-persisted-scope-foundation-2026-09-11.md)。A3当时复用原依赖的纯注解映射路径已实际验证；07:30起原JAXB runtime及本批主源码已恢复并另有校验，但其他依赖和已确认隔离PostgreSQL尚未就绪，仍不能扩展为完整M0-E或数据库验证通过。未将A3接入真实发行链、未启停Docker或更新HTTP。

## 里程碑依赖顺序（具体下一主批以顶部检查点为准）

| 顺序 | 任务 | 状态/关键依赖 | 退出证据 |
| --- | --- | --- | --- |
| 1 | M1-A 申请/报告组/成员与资源授权合同 | 进行中；A1/A2内部基础与A3模型前置限定已验，持久组及实际入口未接 | AC04内部子集已验；AC01各自V1及旧替代逻辑失败回归尚未完成 |
| 2 | M1-B 封存快照、增量结构与映射 | 共享归属模型前置限定已验；快照/发行关联/真实迁移未开始，依M1-A | AC03/AC06、确定性摘要、真实迁移及保留检查 |
| 3 | M1-C 签名/签发事务与并发 | 待开始；依M1-B | AC03/AC05、真实双会话与故障回滚 |
| 4 | M1-D 旧入口收敛与历史兼容 | 待开始；与最终约束切换统一 | 无写旁路、AC06、完整授权和历史保留；M1技术P0才可关闭 |
| 5 | M2 主数据及临床主/异常流程 | 待开始；字典采集可并行 | AC07～AC16、数据库一致性；真实字典另签字 |
| 6 | M3 全岗位/路由/页面清单与工作区 | 待开始；界面盘点可并行，状态依M2 | AC17～AC19逐页逐角色，非仅管理员 |
| 7 | M4 正式模板/签章适配/打印 | 待开始；依M1/M2及D07 | AC20及真实样张签字 |
| 8 | M5 HIS与设备 | 待开始；已有受限接口成果复用，D05/D06待收集 | AC21/AC22及逐台/逐场景签字 |
| 9 | M6 发布候选/安全/恢复/容量 | 待开始；安全检查全过程，最终冻结依前项 | AC23/AC24、同一候选指纹与全门禁 |
| 10 | M7 UAT/试运行/交付 | 待开始；真实条件未齐 | AC25、G0～G11适用出口及最终签字 |

## 固定遗留收口包

备注审计限定源码已冻结：后台274项专项为历史证据；12:13患者日志新增6项、既有2项组件及11项本地化实际通过，前端检查点已提交1856cff46。后台包尚未完整收口，不能用本次前端通过替代后台/整包验收。未部署、未应用037到运行库。专用历史页面等不在本收口包继续扩张；按M2/M3正式排序。

原证据：[备注审计进展](storage-note-audit-progress-2026-09-10.md)。不得将v3预检工具测试数混入该274项，也不得将源码存档当数据库备份或回退版本。

## 当前阻断及责任

- B01 源文件/依赖云占位：开发已建立受控原文件恢复方式，负责继续补齐拟构建范围、校验改动及回归；仅账户/系统权限等确需用户操作时求助。主源码批已恢复，依赖/子模块等仍影响完整构建/发布，不阻止具备对应M0-E条件的受控工作。
- B02 本地容器连接/HTTP不可用：开发在授权范围内查清选定本机引擎和启动方案；不得触发未知远程Docker上下文、删除卷或重载数据。
- B03 首期范围未冻结：用户/业务负责人确认D01；其他D02～D09依计划提前收集。
- B04 报告技术P0：开发负责M1；医院负责实际分组/签章规则确认。不能把技术归属、权限或内容绑定推给医院解决。

定时迭代仅使用既有 `lis`，不新增或恢复其他定时任务；读取本文顶部检查点决定当前一批，不按历史流水的“下一轮”分叉。仍不得推送代码、操作真实患者、签发真实报告或连接未知设备。每次发布单独保存同一候选的备份、回退、测试、实际运行核验。真实医院输入、Docker使用范围与隔离数据库待确认项不变，不重复询问。报告P0及前端436条类型和完整门禁缺口继续保留；已完成格式与基线保护不重复开发，不擅自调整基线或继续扩展工程工具。
