# 结果工作站异常标本与本页输入保护：限定验收

日期：2026-09-14。基线：`d59b34d74`。接续后台 `83a7acf27`，不新增菜单或平行结果流程。

## 本批产品结果

- 拒收、作废、取消、处置和不允许录入状态显示对应中文，按实际 `sampleItemId` 锁定同管分析及分项，不锁同申请的其他管。项目定义异常只限制对应行；未知原因不显示服务器内部文本。
- 重新查询时把本页输入与当前服务器记录分开展示，不自动覆盖、回填或提交。原实管、分析、项目、完整定义、版本及服务器原值均一致，且结果明确未保存时，才提供“继续填写本页输入”。明确未保存的本页输入可二次确认后放弃，不删除或修改服务器记录。
- 已发出的请求结果无法确认时，保留输入并锁定同一分析的所有组件。查询到相同值不当作原请求成功。签名未知与结果保存未知分别显示，不自动重签或重发；不通过放弃草稿绕过未知保护。
- 延迟签名绑定原会话、所见行、查询代数和编辑版本。编辑、重查、会话变化或卸载后旧回调不能提交旧值；明确签名关闭才可不弹签名仪式，空开关不放行。
- 本工作站的读取、保存、协同提示、电子签名采用有界、无自动跳转的请求路径；业务写入及签名未知结果不自动重试。协同按既有周期刷新，拒权后停止同凭证请求，失效明确提示，不伪造协同正常。首次认证按实际数字时间响应兼容，仍只消费严格核对后的认证ID和用户ID。
- 同凭证的迟到401/403优先撤权，不因查询已更新而忽略；旧用户或旧凭证不能撤销新会话。清理包括表格、本页输入、申请号查询、地址筛选和授权目录，取消在途签名也不能把患者输入重新带回。

## 本轮证据

证据目录：`tmp/result-ui-20260914-hNp1Bs/`；最终候选：`candidate-v3/`。

| 检查 | 限定结果 |
| --- | --- |
| 最终精确候选回归 | **182/182**，0失败、0跳过，9测试文件，工具汇总14套，41.049秒；19源测/语言文件测试前后指纹一致 |
| 保留历史差量的当前工作树 | **249/249**，0失败、0跳过，9测试文件，82.966秒；19文件指纹一致。多出的历史用例不计入已提交候选成绩 |
| 真实前端组件链 | 包含7项工作站＋真实签名弹窗＋真实协同hook＋模拟fetch集成，不只验证被mock的按钮回调；无真实业务HTTP |
| 完整候选前端构建 | 通过，52.18秒；仍有既存大包和构建性能警告 |
| 浏览器 | 最终白色Theme与应用一致；1440/768两宽度 **2/2**，0 flaky，11.4秒。核对区、值0、服务器新值、拒收中文、放弃确认及取消、零结果/签名写、无整页横向溢出已验；窄屏表格内部滚动 |
| 类型检查 | 同HEAD基线467条→候选463条。3条旧可选属性错误因类型展示展开，按相同文件/代码/缺失属性核对后无新增；**全量类型检查仍不通过**，不是放宽发布门禁 |
| 格式及范围 | 仅指定候选文件格式化。三语言各43个必需新键，不覆盖既存翻译；旧患者展示/表列样式、旧全局Utils/App/签名api差量不夹带提交 |

主要报告：`candidate-v3-verified-tests.json`、`candidate-v3-verified-manifest.json`、`working-verified-tests.json`、`types-comparison.json`、`browser-results.json`、`result-1440.png`、`result-768.png`。

保留了失败证据：初始11个真实界面用例全失败；协同旧实现39失败及迟到撤权修前6失败；签名闭包、同步卸载及数字认证时间的红绿记录。浏览器第一次受沙箱限制、第二次模拟拦截遗漏相对路径；另一次模拟夹具缺少应用白Theme，经复核修正。最终指纹验证曾拦住格式化与测试并发的源码变化，已在格式完成后重新执行并通过；不把该轮当作稳定候选通过。

## 独立复核与保存边界

一个主批内并行实现签名适配器和协同hook，由独立只读审查负责收口；主代理整合、真实组件联测和精确候选验证。独立复核发现并修复旧请求强制刷新旁路、首次认证时间误判、同步卸载窗口、同凭证迟到撤权及撤权后草稿回流；最终代码限定审查未见本批剩余P1。

提交范围为19源测/语言文件及本验收文件。工作树和精确候选分别归档，按已测候选逐文件暂存，并核对提交对象；历史未提交内容保留，未全量暂存或远程推送。备份及Git对象清单见同目录 `staged-manifest.json`；此清单由归档/暂存核对生成，不等于数据库备份或可运行版本回退。

## 不算已交付的部分

- 输入仅保留在当前页面内存；页面离开、完整刷新、浏览器崩溃或跨客户端长期未知恢复仍未闭合。离开提示不是持久草稿，不承诺未知结果可自动恢复。
- 只复核现有Context可提供的身份/凭证变化；HEAD App没有完整会话检查代数，未在本批引入历史App改动，不宣称所有全局会话竞态消失。
- 签名回执与目标核对不等于结果内容摘要签名，也不等于结果提交和签名记录的后台原子事务。签名未知需后续受控核实方案，不可重复操作猜测结果。
- 没有连接真实数据库、真实厂商设备或医院系统，没有修改真实患者、打印或签发报告。后台规则、逐管明确验收结论/原因/审计、多岗位及旧专科入口仍需接续验证。
- **没有启动Docker或更新18080运行版。** 隔离数据库、授权DB/配置备份、可运行回退、报告P0和完整发布门禁未齐备；临时18789纯模拟静态服务结束后停止。本批不提高M0～M7或G0～G11的正式验收等级，不称正式医院交付。

## 唯一下一主批

沿同一申请工作区继续“逐实管明确接受/拒收、原因、操作人和审计”合同与交互；复用已有签收/核对/结果准入，不把未拒收、签收或整申请勾选当逐管临床合格。不再重做本批结果页保护或扩新菜单；真实临床规则、医院主数据、角色及厂商资料缺失继续列待确认。
