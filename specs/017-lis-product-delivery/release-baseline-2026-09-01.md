# G0 发布基线审计记录

> 日期：2026-09-01  
> 分支/提交：`develop@c6c6d61`  
> 结论：前后端既定自动化回归基线及管理员核心业务端到端已通过，但工作区尚未达到受控发布候选标准。

## 1. 工作区规模

| 状态 | 数量 |
| --- | ---: |
| 已修改 | 453 |
| 未跟踪 | 189 |
| 合计 | 642 |

| 一级目录 | 已修改 | 未跟踪 | 说明 |
| --- | ---: | ---: | --- |
| `frontend` | 338 | 110 | 主要为中文产品化、页面、测试和构建脚本 |
| `src` | 109 | 66 | 后端业务、FHIR、主数据、报告及测试 |
| `docs` | 0 | 5 | 中文手册、QA记录和设备指南 |
| `volume` | 2 | 1 | 本地运行配置，需与生产模板隔离 |
| 其他 | 4 | 7 | Docker、代理、数据库、脚本、规格和临时证据 |

已跟踪变更统计约为新增28941行、删除15435行。变更面过大，必须先建立所有权、发布范围和回归证据，不能直接把当前工作区作为正式发布包。

## 2. 2026-09-01 前端交付检查

执行 `npm run check:delivery`：

- 中文交付引用检查：1826个消息标识在 `zh` 与 `zh-CN` 中均存在，通过。
- TypeScript回归门禁：当前1160条诊断，历史基线1209条，没有新增诊断类别，通过现有基线门禁。
- 单元测试：204/204个测试文件通过；2240条通过，13条跳过。
- 生产构建：Vite构建成功。

本轮修复一项失效菜单测试：申请工作台作为唯一主入口时，不再要求侧栏重复展示“新建检验申请”。目标测试36/36通过后，全量前端检查通过。

## 3. 尚未达到生产门禁的工程问题

1. TypeScript仍有1160条诊断。现有门禁只保证“不增加”，生产交付目标必须降为0。
2. 生产资源包过大：主要布局包约7.65MB，主入口包约2.74MB，存在首屏和低配置终端性能风险。
3. 通用样本模块原有静态/动态重复导入已整改；笔记本相关页面延迟加载后，主入口包由约3.13MB降至约2.74MB，三个无效动态导入警告已清除。
4. 存在两个疑似冲突副本：`LandingPage 2.tsx`、`reports/Index 2.jsx`，且与正式文件内容不同，必须人工归并后处理。
5. `tmp/pdfs/` 包含FHIR模拟报告和字体测试证据，应归档为QA证据或排除出发布包，不能混入生产镜像。
6. 后端全量测试和管理员核心端到端已完成；数据库迁移演练、依赖安全、干净环境构建、多角色异常场景及真实外围联调仍未完成。

## 4. G0 风险分级

| ID | 级别 | 风险 | 处理门禁 |
| --- | --- | --- | --- |
| G0-R01 | P0 | 642项变化没有受控发布边界 | 建立变更所有权清单和可复现发布基线 |
| G0-R02 | P1 | 1161条TypeScript诊断可能隐藏运行时错误 | 分批归零，禁止增加新豁免 |
| G0-R03 | 已整改 | 无效CSS选择器可能导致页面生产样式缺失 | 已修复并通过相关页面测试及生产构建 |
| G0-R04 | 已整改 | 直接执行配置表达式存在安全和稳定性风险 | 前后端均改为受限解析器，非法保存返回400，专项测试通过 |
| G0-R05 | P1 | 超大资源包影响首屏和弱终端体验 | 路由拆包并建立性能预算 |
| G0-R06 | P1 | 疑似冲突副本可能造成错误代码被遗漏或误发布 | 差异审查、归并、测试后再决定是否移除 |
| G0-R07 | 已整改 | 后端全量和管理员核心E2E原无当前证据 | 后端4998条零失败零错误；真实Chromium核心链路及可见菜单专项7/7通过 |

## 5. 下一步

1. 按工作区归属清单隔离临时证据和两个已确认的旧版冲突副本。
2. 生成完整业务路由、接口和设备清单，并按角色逐页验收。
3. 将1160条TypeScript历史诊断归零并建立资源包性能预算。
4. 继续主数据、中文报告、危急值、标本拒收重采和报告修订闭环。

## 6. 本轮已完成整改

- 已将患者历史、趋势、筛选和数据表中的无效 `:global(...)` 选择器改为构建器支持的全局 Carbon 选择器。
- 相关患者结果查看测试7个文件、18条用例全部通过。
- 生产构建重新通过，原有8组 `:global` 样式警告已清除。
- 已将计算项目的浏览器 `eval` 和后端 JavaScript 脚本引擎替换为受限表达式解析器；只允许数字、四则运算、比较、逻辑和括号，非法定义保存返回400。
- 新增前端表达式专项测试，连同计算规则交互测试共12条通过；新增后端解析器、恶意内容、除零和接口拒绝测试，等待后端全量测试确认。
- 计算规则下拉项和反馈文案已中文化，暂不展示后端尚无真实数据来源的“患者体重”。
- 已修复Maven动态版本警告、重复 `spring-security-test` 依赖和PostgreSQL错误依赖范围；离线 `mvn validate` 通过。
- 已移除受限表达式解析器上线后不再使用的 Nashorn 脚本引擎依赖；离线 `mvn validate` 再次通过。
- 生产构建重新通过且不再报告直接 `eval`；当时仍保留超大资源包、通用样本模块无效拆包和插件耗时风险，后续已继续处理通用样本拆包。
- 笔记本实例和关联样本页面改为按需加载，专项15/15通过，生产构建通过；通用样本三个无效拆包警告已清除，主入口包减少约386KB。7.65MB布局包和其他大包仍需继续拆分，不能因此关闭性能风险。
- 新增只读主数据完整性门禁并完成本地盘点：183个启用可开立临床项目中23个缺本地编码、15个重复编码、28个缺标本映射；42个启用单位缺本地/UCUM编码，12个启用方法缺编码，当前无启用医生及检验设备。该结果明确阻断真实接口与生产交付，不能用模拟种子数据替代医院签字目录。
- 当前本地部署完成真实 Chromium 专项验收：登录、完整申请→采集签收→标签→标本验收→结果录入→审核签发→PDF 报告链路、全部中国版可见菜单路由四分片巡检、患者列表搜索及打开档案共7/7通过；新链路实验室编号为 `DEV01260000000000025`。该结果证明管理员正常链路可运行，不替代多角色、异常/危急值、真实接口和打印样张验收。
- 后端全量回归在独立测试容器中完成：547个Surefire测试报告、4998条测试，0失败、0错误、14跳过。重点覆盖FHIR患者/机构/申请/结果、真实申请持久化、分析仪配置与结果接收、样本编辑、检验目录、搜索、审计和权限等模块。运行中的错误日志包含故障分支断言，最终结论以Surefire XML和进程退出码0为准。
- 新增 `workspace-change-inventory-2026-09-02.md`，将当前705项变化全部归入产品代码、测试、文档、部署配置、QA脚本、临时证据或冲突副本；`tmp/**` 和两个已确认的旧版副本已从Docker构建上下文排除。
- 建立 `outputs/lis-product-delivery/OpenELIS-中国医院版主数据采集与验收模板-v1.0.xlsx`，覆盖15张主数据、校验和验收工作表、关键引用校验、自动汇总和四方签字门禁；空模板保持“未完成”，不能用模拟值冒充医院生产目录。
- 患者报告接口已增加发布安全门禁：仅返回已审核且配置为可报告的结果，默认字段改为中文临床列并补充单位、参考范围和危急/异常提示；独立单元测试1/1通过。正式发布状态、报告版本、签名和现场打印样张仍为G2阻断项。
- 新增 `/rest/reports/patient-results.pdf` 中文PDF预览接口：使用与报告数据接口相同的已审核/可报告过滤，嵌入可再分发的 Noto Sans CJK 简体中文字体，包含患者、标本、结果、单位、参考范围、危急提示、检验人员、审核状态、生成时间、预览版本和页码。Java 21专项测试3/3通过（数据过滤、中文单页、多页表头/页脚），A4样张完成文本提取和逐页视觉复核。该接口明确标记 `PREVIEW-1`，审核人签名、正式报告版本、发布/撤回、打印审计和医院现场样张仍是G2阻断项。
- 中文报告预览入口已并入患者历次结果页，并按结果/报告岗位显示；患者结果路由同步允许这两个岗位进入。专项页面测试2/2、中文资源门禁和TypeScript回归门禁通过，当前诊断由1160降至1158且未新增诊断类别。
